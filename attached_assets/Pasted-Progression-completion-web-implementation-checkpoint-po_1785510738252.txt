Progression completion, web implementation: checkpoint polish (flag-gated dark) + conversation capstone (live). Web (artifacts/gujarati-coach) plus api-server for scenario content and chat prompt assembly. Mobile explicitly untouched — a full parity sync happens later after all web updates settle; write every design decision into docs/specs/progression-completion.md as you build so the parity batch has a reference document. Consult docs/CODEBASE-FACTS.md, agent memory, docs/specs/pronunciation-scoring-v2.md, docs/specs/token-economy.md.

Step 0 first, report before building: (1) trace the web stop-completion flow — where the completion moment renders, what data it has (per-phrase bands for the stop), how best-score storage works (can a worse retry downgrade a stored best?); (2) trace zone-completion detection on web (does an explicit zone-complete moment exist, or does the last stop's completion imply it?); (3) trace chat's system prompt assembly server-side (file, how language/context enter it) and the web chat surface's entry params; (4) confirm where the completed-stop and completed-zone UI live on the journey map for re-entry points. Then build.

Part A, checkpoint polish — BUILD DARK behind a flag:

Server flag polish_enabled (env or config, default OFF) exposed to the client; all polish UI renders only when on. It stays off until scoring v2 ships (do not wire it to anything else).
At stop completion with any phrase below the top band: a "Polish your phrases" card listing sub-top phrases with their band chips, one action to re-run exactly those phrases through the normal practice loop, and a skip that is zero-friction. Never blocks progression, never costs anything.
Best-band semantics: improved attempts overwrite, worse attempts never downgrade the stored best — if Step 0 finds today's storage lacks that guarantee, add it server-side (this part is safe to ship live since it only makes storage kinder).
Re-entry: a completed stop on the journey map offers "Polish" alongside its existing UI (flag-gated).
"All top band at this stop" recognition: a small visual stamp state on the stop; Chai bonus left as a documented hook, not implemented.

Part B, conversation capstone — LIVE:
6. Scenario definitions server-side where content conventions suggest (per Step 0): id, zone/category id, title, framing copy, target phrase ids drawn from the zone, Bolo steering instructions. Write zone 1 fully: Greetings & Manners — greet Bolo at the platform chai stall; natural use of the zone's phrases. Ship with zone 1 only; the definition shape must make adding zones content-only.
7. Chat scenario mode: a scenario param on the chat surface; server injects the scenario framing and steering into the existing system prompt assembly (Bolo stays in character, steers toward target phrases, warmly acknowledges when the learner uses one — never grades, never scores; the capstone is judgment-free performance). Voice pipeline, barge-in, and TTS unchanged.
8. Web chat renders scenario dressing when in scenario mode: a scenario banner (title + one-line framing) and target-phrase chips (romanized) that light up as used — detection via transcript match server-side, sent as part of the turn response; keep it lightweight and forgiving.
9. Completion semantics: scenario completes when a majority of target phrases have been used OR Bolo's reply signals scene completion (server decides, simple rules); award XP consistent with existing XP conventions (quote them; modest amount), record a zone conversation stamp (persisted per user+zone), and show a warm completion moment. Chai hook documented, not built.
10. Entry points: the zone-completion moment on the journey offers "Have a chat with Bolo at the chai stall" as the celebratory next action; completed zone headers offer replay. Free tier: zone 1's capstone is FREE (deliberate conversion moment); zone 2+ scenarios gate behind the existing All-Access entitlement pattern — 402 handling consistent with current gates.

Constraints: one owner per file surface — do not touch practice.tsx beyond what polish item 2's re-run entry strictly requires (report if more is needed); no em dashes anywhere; no mobile files. Tests: polish card renders flag-on only and lists correct phrases, best-band never downgrades, scenario prompt assembly injects framing (server test), chip lighting on phrase use, completion awards once (idempotent), zone-2 scenario 402s for free users, zone-1 scenario reachable free. Enumerate test counts before and after on web and api-server (api baseline: the documented 14-failure set unless the cleanup task has retired it — check CODEBASE-FACTS and state which regime applied). Report per item with file paths and web-verified status, plus the updated spec document.