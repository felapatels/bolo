Full consolidated prompt, everything pending in one task, paste as-is:

Batch accepted with follow-ups. Run this as one task, report when committed.

(1) Polish item (a), decision you didn't receive: use the existing plain Bolo bird mascot for the sign-in card logo and the web logo, replacing flag logo.svg. Use the wave pose unless another of the five poses is clearly better suited for a static logo mark; state your choice and why in the report. The bird-holding-Indian-flag composite stays deferred under #801 pending artwork. Do not generate or composite anything.

(2) Polish item (c) addendum: the slashed-icon state fix stays, but the underlying issue is that the toggle's purpose is not self-evident to a new user. Add a short text label or equivalent so it reads as controlling spoken feedback. Propose the exact treatment in your report; implement it if trivial, otherwise wait for my confirmation on that sub-item only.

(3) Run #800 now: the contact form sender points at noreply@boloapp.in, an unverified domain, and 403s silently, so the contact form notification email is broken in production. Point it to hello@bolo-india.app via its existing RESEND_FROM variable, set in both dev and production environment vars, keep it env-configurable with no hardcoded fallback, consistent with the invite sender pattern. Send a test contact-form submission and confirm the notification email actually sends.

(4) Extend the post-deploy verification query to also check migration 0025's daily_goal default, and include the updated query in your report.

(5) Invite email template fixes, applied to both invite types (referral and family): (a) The CTA currently links to the App Store, which has no live listing; anyone invited today hits a dead end. Point the CTA to https://bolo-india.app, with the destination URL env-configurable (e.g. INVITE_CTA_URL, set in dev and production) so it can switch to the App Store link at iOS launch without a code change. Update the button copy to match the web destination. (b) The preheader text renders as a visible gray box over the header banner in the received email; make it properly hidden preview text. (c) Replace the parrot emoji in the header with the actual Bolo mascot image asset, embedded so it renders consistently across email clients, not emoji. After the fixes, send one fresh test invite to aakesh_patel@yahoo.com for my visual verification.

Report must include: logo pose choice and rationale, the (c) label proposal, #800 test result, the extended verification query, and confirmation of the fresh test invite send. Typecheck and test baselines per the facts doc. Commit when verified.