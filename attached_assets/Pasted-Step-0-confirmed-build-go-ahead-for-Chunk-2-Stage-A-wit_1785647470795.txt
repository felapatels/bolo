Step 0 confirmed, build go-ahead for Chunk 2 Stage A, with these rulings:

RULING 1, integration design: approved as you specified. Single call inside compareToTarget, bridge attempted only when the raw result is incomparable or weak, similarity taken as max(raw, bridged), comparable marked true on rescue, nothing removed from the raw path, no module changes. The [xscript] rescue log line is approved.

RULING 2, "weak" needs a definition before you build: state in your build notes the exact condition under which you attempt the bridge on a comparable-but-weak raw result, and keep it conservative: bridging must never LOWER an outcome, which max() guarantees, but I also do not want the bridge attempted on strong raw matches where it is pure waste. Attempt the bridge when raw is incomparable, or when raw is comparable with sim below the fast-path threshold (0.93). If the module's own integration notes specify a different trigger, follow the module and say so.

RULING 3, Perso-Arabic: the documented honest limit stands as designed: skeleton comparison for both-sides Perso-Arabic, bridged false for mixed pairs, raw path kept. Do not improvise a ur table. Record the limit verbatim in CODEBASE-FACTS per the manifest (your item b: yes, in scope).

RULING 4, manifest gate wording (your item a): my gate governs, with one addition from the manifest: include the boot smoke (server starts clean and serves one scored request in dev) as part of the end gate. Skip "tripwire" as a named step if it means anything beyond what the post-restart verification already covers; the post-restart allowlist-environ check plus tee probe IS our tripwire.

RULING 5, A2 details, all approved as reported, with these specifics locked: similarity values recorded in place of confidence (name the field similarityValues, not confidence, so nobody later mistakes what it is); the :707 unsupported-language site writes a sidecar with cause unsupported_language and null transcripts; cause taxonomy is your reported set (empty_audio_or_silence, dual_pass_uncorroborated, script_mismatch, latin_low_sim, unsupported_language) plus no_match_after_bridge for the case where the bridge was attempted, bridged true, and similarity still failed, so we can see the module's residue directly. Sidecars at the nocatch exits as new post-response fire-and-forget calls, same shape as teeAudioToPilot. Include in each sidecar the bridged boolean and both comparable strings when a bridge was attempted.

RULING 6, baseline: 704/15 is accepted as the new canonical api baseline, with the pilot-capture unlisted-userId case's env-trap failure documented as known-environmental. Record 704 in CODEBASE-FACTS. Do not fix the test in this chunk; if a one-line env isolation fix exists it goes on the debt list, not in this batch.

Proceed to build. Gate and commit message per the task paste, plus the boot smoke from Ruling 4. Stop before committing as specified.