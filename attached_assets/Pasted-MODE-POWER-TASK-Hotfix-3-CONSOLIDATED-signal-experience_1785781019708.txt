MODE: POWER

TASK: Hotfix 3 CONSOLIDATED, signal experience + game feel (7 items, client only). One commit to main. This paste SUPERSEDES the earlier Hotfix 3 paste and its Addenda 2 and 3; if any of those already arrived in this pane, this is now the single authority: keep conforming work, fold in the rest, discard nothing that matches this spec.

HARD RULE: ZERO git commits before the owner's approval message arrives in this pane. Tree stays UNSTAGED until approval. ONE report, then STOP. No task cards; suggestions bank as report lines.

SESSION RULES: scope = artifacts/gujarati-coach plus docs/CODEBASE-FACTS.md; no api server, no lib/, no bolo-mobile. No em dashes anywhere including new copy. attached_assets never staged. New test files only for genuinely new surfaces (one-line justification each). Failure identity before any re-run; every full-suite run pipes through tee to a log before any display truncation. No browser probes EXCEPT item 1's tap verification if static reading cannot prove it. Divergence goes under DEVIATIONS.

STEP 0 (verify, quote, misses hard-stop):
A. git log -1 --decorate. Acceptable HEAD: b84d53e, or exactly one "Published your App" commit atop it. Else STOP.
B. Manual signal taps are still dead on prod near stop labels despite the postcard-wrapper fix. Quote every layer that can overlap a signal button near a stop point (stop labels, the "Bolo is waiting here" tooltip, progress elements, any wrapper added since), with z-index and pointer-events for each. State the residual occluder in one sentence.
C. Quote the SignalGlyph SVG and how the planner computes each signal's x/y within its gap.
D. Quote the phrase-card audio button's active/inactive treatment (the green active pattern) from practice surfaces.
E. Quote the quick-game frame's countdown capability and Signal Lights' round timer plus the audio-trigger effect, enough to show ordering between audio start, round advance, and timeout.
F. Quote the postcard fact strip render and the house reduced-motion gate pattern.
G. Quote how a signal's held state derives from the preceding stop's completion, and what state a signal has when the preceding stop is NOT complete.

STATE MODEL (governs items 2 through 5; railroad crossing motif):
RED, bar down = any signal not yet cleared, INCLUDING future signals whose preceding stop is incomplete. Every red signal renders full-color and attractive, never dimmed or desaturated.
RED ACTIVE (preceding stop complete, playable now) is distinguished from red future by a motion-safe attention pulse and blocking emphasis; tappable; auto-opens per the standing soft-stop rules.
RED FUTURE: not tappable, never auto-opens, no dead-feeling cursor affordance.
GREEN, bar up, green lamp = cleared by passing the game; replay behavior unchanged; replay dialog NEVER shows a Chai chip.
YELLOW, bar up, caution lamp = waved through; session-scoped like the existing wave memory; tappable at ANY later time and reopens the full encounter, because the first-clear Chai is still unclaimed.

ITEM 1, kill the residual tap blocker: fix the Step 0 B occluder so every signal is tappable at every position, 44px minimum targets. If static reading cannot prove the fix, ONE lean built-app probe verifying a tap near a stop label lands; delete probe artifacts.

ITEM 2, railroad crossing redesign: replace the signal glyph with a railroad crossing gate in the app's flat playful style using the established palette (indigo, teal, slate, plus red, amber, green lamps) implementing the STATE MODEL above. Position each signal at the departure edge of its preceding stop (visually adjacent, just past it) instead of floating mid-gap. VISUAL ONLY: gap-N contextRefs, seating data model, and server payloads unchanged.

ITEM 3, encounter dialog upgrade: compact scene header using the existing TrainEngine art plus the new crossing motif (no new raster assets, no AI art). Title stays "Signal ahead"; body copy may be lightly revised (report verbatim). Under the play action, one fun blurb per game; write all five, report verbatim; example register: Ticket Check: "Punch tickets to their matching script before the whistle blows."
REWARD CHIP: the dialog shows "+1 Chai" with the chai glyph (same glyph family as the wallet) near the play action, so the learner sees what clearing pays BEFORE playing. The chip renders ONLY when the signal's first-clear grant is unclaimed (red active and yellow reopen). It NEVER renders on a green replay dialog: no promising Chai that will not be paid. Chip text reported verbatim; the claimable-only render rule gets a test pin.

ITEM 4, wave-through receipt: choosing "Wave me through" fires a house toast (branded bottom toast) confirming the skip and the open invitation, never-shamed voice. Draft, agent may polish, report verbatim: "Waved through. The signalman kept your Chai warm, come back anytime." Must convey that returning is possible; zero guilt language.

ITEM 5, yellow re-entry: binding restatement of the STATE MODEL: yellow signals reopen the full encounter with the reward chip visible. If the yellow reopen wants its own one-line dialog variant, write it and report verbatim.

ITEM 6, live fact strip: the postcard "Did you know?" fact cycles to the next fact after roughly 6 seconds of continued visibility with a gentle crossfade, looping the zone's rotation; tapping the strip advances immediately. Reduced motion: no auto-cycle, tap-to-advance still works with an instant swap. No new network calls.

ITEM 7, Signal Lights game feel: (a) audio race fix: round phrase audio fires when the round STARTS and is cancelled the moment the round advances or times out, never firing at or after expiry; (b) the audio/mute button adopts the Step 0 D green active treatment while playing; (c) a 3-2-1 countdown before round 1 via the frame's countdown capability, applied to Signal Lights AND Express Listening (the two timed games); (d) the per-round timer becomes prominent with an urgency treatment in the final 2 seconds, motion-safe gated.

GATE: typecheck; targeted test files for touched surfaces during development (enumerate); ONE full suite at the end through tee, baseline 458/63, runner line verbatim, delta by file. Failure = identity first, report, no commit.

REPORT (one message, then STOP for approval): Step 0 A-G quotes including the occluder sentence; per-item summary 2 lines max; one line on how red-active vs red-future are visually distinguished; EVERY new or changed user-visible string verbatim (dialog copy, five blurbs, chip text, wave toast, countdown, timer strings); files changed one line each; new test files with justifications; DEVIATIONS or none; gate results with the tee'd runner line; git status with the exact proposed unstaged list; proposed commit message: "Hotfix 3: crossing signals with traffic-light states, tap fix, reward chip, wave receipt, live facts, timed-game feel"
END OF REPORT is the final line. After approval only: stage, commit, confirm with git log -1 --decorate and git show --stat HEAD, DEVIATIONS or none, END OF REPORT, stop.

END OF TASK. If this line is not the last line you received, the paste truncated; stop and say so.