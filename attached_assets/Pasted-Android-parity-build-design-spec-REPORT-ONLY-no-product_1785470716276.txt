Android parity build design spec. REPORT ONLY — no production code changes, no dependencies, no EAS builds triggered; deliverable is docs/specs/android-parity.md plus findings. Consult docs/CODEBASE-FACTS.md and agent memory. Known groundwork: Google Play Console account is ready, Expo/EAS supports Android, adaptive-icon.png exists from the canonical mascot, android.versionCode is 1. Sequencing: implementation starts only after iOS build 31 clears external beta and stabilizes; Aakesh wants the eventual execution to be step-by-step guided, so the spec must be written as an ordered walkthrough he can follow, not just an architecture document.

Spec sections:

Native parity risk audit — the core section. Walk the codebase and inventory every place behavior was built or verified iOS-only, with file paths: the audio session layer in lib/audio.ts (RECORDING_MODE/PLAYBACK_MODE flips, playbackModeToken, keepAudioSessionActive — all tuned against iOS AVAudioSession; report what each maps to on Android and what is likely to behave differently), recording preset (16kHz mono m4a — Android encoder equivalence), haptics usage, safe-area handling (journey header inset fix was iOS-notch-driven; report Android status-bar/display-cutout behavior), the ticket/train SVG sizing contract (the build-28 Yoga regression class — state whether Android rendering needs its own device verification), reduced-motion detection on Android, keyboard behavior on the chat/name-prompt inputs, and the mobile parity batch's own flagged Android notes (glow shadows possibly invisible, tear halves relying on elevation). For each item: expected-fine, needs-verification, or needs-work, with reasoning.
Auth and services config. Clerk: Google OAuth Android redirect configuration (what changes in the Clerk dashboard and app config; also Apple sign-in availability on Android and what the sign-in screen should show there), production instance only. RevenueCat: Google Play products and entitlements setup mirroring the iOS All-Access offering — list the exact products to create, how the webhook handling differs for Play events, and grace-period/acknowledgment rules specific to Play billing. Sentry and PostHog: confirm Android is covered by current init or list changes.
Build and signing pipeline. EAS Android production profile: keystore management via EAS, versionCode strategy alongside iOS buildNumber, .aab output, and the equivalent of tonight's Transporter step (EAS submit vs manual Play Console upload — recommend one for a first release).
Play Console workstream, as the step-by-step walkthrough: app creation, store listing (reuse the iOS metadata pack: name, subtitle-equivalent short description, full description, what changes for Play), store assets (icon, feature graphic dimensions, screenshots — note the screenshot task's device-capture approach extends here with Android-frame variants), content rating questionnaire, data safety form (map our actual collection honestly: account email/name, audio recordings sent for processing, analytics via PostHog, crash via Sentry, coarse location if the iOS privacy label's Diagnostics + Coarse Location entries apply — produce the draft answers table), target audience declaration, and internal testing track setup as the Android mirror of TestFlight internal.
Device test plan. The Android-specific device pass: everything from the iOS 31 checklist plus the items section 1 flagged needs-verification, organized so one pass on a physical Android phone covers it; note minimum a mid-range device and ideally one small-screen device.
Task breakdown. Ordered, sized, file-owned implementation tasks with the gate stated (post-iOS-31 stabilization), first task being the section-1 needs-work items, last being store submission.

Findings section: anything discovered in the codebase that contradicts the assumptions above, and open questions needing Aakesh's decision (e.g. Android minimum OS version, whether F&F beta uses Play internal testing or open testing). No em dashes anywhere.