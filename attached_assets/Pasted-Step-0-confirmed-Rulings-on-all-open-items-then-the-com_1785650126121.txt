Step 0 confirmed. Rulings on all open items, then the complete build instruction. This message is self-contained; if your context compacts, this paste alone governs.

RULING (a): Confirmed, that is exactly the point of the task. The cap made the perfect band, its flash, and both confetti variants dead code; at 91 they come alive for capped 91-92 attempts. Intended.

RULING (b): FSRS Easy MOVES to 91. This is a deliberate product ruling that overrides the code's original score-pin intent: an Easy rating that cannot fire under the cap means memory scheduling has been running without its top rating since the honesty cap landed, and that is a bug wearing a design's clothes. Keep it a separate literal in fsrsScheduler.ts, change 93 to 91, and update its comment to read that it is deliberately coincident with the Perfect band threshold and moves only by owner ruling.

RULING (c): The verify-time clamp re-derives. Replace the hardcoded clampedBand = "great" with bandFromScore(safeScore) so a clamped replayed 92 and a fresh 92 both show perfect. One consistent derivation path everywhere.

COPY RULINGS on the flagged strings:

FAST_PASS_RESPONSES "Excellent work! Your pronunciation was spot on. ...": CHANGE. Replace the claim sentence with "Excellent work! Every sound was spot on." keeping the rest of the pair intact. Pronunciation-quality claim becomes sound-scoped, which is what the scorer actually measures.
FAST_PASS_RESPONSES "Perfect! You hit every sound in that phrase. ...": KEEP unchanged. "Hit every sound" is transcript-scoped and honest, and the band is literally named Perfect.
The borderline native-speaker tip: KEEP unchanged. It frames native speed and melody as the NEXT level, which implies the learner is not there yet; that is aspiration, not a claim.
Web "Perfect!" flash, mobile 'Perfect 🌟' flashes, ladder labels: KEEP. They display the band's name.
HONESTY_CAP_RESPONSES: KEEP as-is; they are already correctly hedged.
No other copy changes anywhere. One string changes in this entire task.

HISTORY NOTE: the old-92-shows-great vs new-92-shows-perfect display inconsistency for stored five-band rows is ACCEPTED as-is. Record it as one line in CODEBASE-FACTS; no migration, no re-derivation of stored bands.

THE CHANGE, build now:

scoreBands.ts: BAND_THRESHOLDS.perfect from 93 to 91. 80 and 55 untouched. Remove or update the TUNING PENDING marker to reflect the ruling.
band-pill.tsx line 64 and doc comment: 93 to 91.
bolo-mobile/lib/ui.ts line 72 and comment: 93 to 91. Note in the done report that devices see it at the next iOS build.
fsrsScheduler.ts per Ruling (b).
evaluationToken.ts clamp per Ruling (c).
Contract tests: all three sharedConstants suites pin perfect: 91; scoreBands.test.ts boundary cases updated (91 lands perfect, 90 lands great).
The one copy change per Copy Ruling 1.
Do NOT touch: the 0.93 sim fast-path threshold, BRIDGE_SKIP_THRESHOLD, game animation 0.93 scale values, HONESTY_SCORE_CAP itself, the audioJudged exemption wiring, dual-pass, capture, or any other copy.

GATE, ONE, AT THE END: typecheck clean; full web suite green (report count against 391/55 or 407/57 depending on whether task #1003 has merged); full api suite green against 720/15 with the documented PILOT_CAPTURE_USER_IDS env-trap failure as known-environmental; mobile suite for touched files if runnable here, else deferred-to-build note; manual browser verification that a dev attempt scoring 92 renders the Perfect band, the Perfect flash, and confetti on web. Then STOP, report done with the copy before/after, the FSRS and clamp diffs quoted, and the observed test counts. Wait for my confirmation, then commit ONCE with message: "Perfect band reachable: threshold 93 to 91 across surfaces, FSRS Easy aligned". Because api-server files changed: post-restart verification per the established pattern (allowlist IDs in live environ, one tee probe under the diagnostics prefix, probe deleted). No suggested or draft task cards. No em dashes anywhere.