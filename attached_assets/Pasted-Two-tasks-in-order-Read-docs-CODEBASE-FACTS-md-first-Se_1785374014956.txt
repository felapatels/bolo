Two tasks, in order. Read docs/CODEBASE-FACTS.md first. Server-side rules apply to both: additive only, no bolo-mobile files.

**TASK 1 — Test-out rate limiting (hardening, execute now)**

Throttle test-out submissions per user per lesson group using the existing `lesson_group_testouts` log: max 3 attempts per group per rolling hour; further attempts return 429 with a retry-after. Server-side only. Add two tests: one asserting the throttle triggers on the 4th attempt within the hour, one asserting the 429 response shape including retry-after. Update the facts doc debt table row (test-out rate limiting) to resolved. Commit and report.

**TASK 2 — Web chat bug fix: mic permission auto-record + blocked first message**

Web chat only (gujarati-coach chat page); do not touch bolo-mobile.

**The bugs:** For a first-time user on web chat, (a) the text input cannot be used to type the first message, and (b) the moment Chrome's mic permission prompt is granted, recording starts automatically — the user never pressed record.

**Step 0 — trace before writing any code, then STOP and report:**

1. Find the permission-grant callback path: what code runs when getUserMedia resolves after the user grants mic permission, and does anything in that path start recording as a side effect?
2. Distinguish the two flows sharing this path: a user who pressed record (permission prompt appears, grant continues into recording — deliberate continuation) vs. a first-time page load where permission is requested without any prior record press (grant must NOT start recording). Identify where the code fails to distinguish these.
3. Explain why the text input is unusable for the first message: disabled pending mic state, focus-trapped, or blocked by the recording state machine?
4. Confirm whether this code is chat-page-only or a shared surface (e.g. the practice speaking flow), and the blast radius of a change.

**Fix requirements (implement only after I approve the Step 0 report):**

1. Granting mic permission never starts recording, under any circumstances.
2. Recording starts only from an explicit record action. If permission was requested *because* the user pressed record, the grant may continue that deliberate action — but a grant with no prior record press in the same interaction does nothing.
3. The text input is usable at all times regardless of mic permission state — denied, undecided, or granted.
4. Web chat only. If Step 0 shows the surface is shared, stop and report instead of changing shared code.

Add a regression test or QA-harness check: first load → grant permission → assert no recording started and the input accepts typed text.

Task 1 runs to completion without a stop. Task 2 stops at Step 0 for my approval.
