→ NEW TASK

Build 30 fix batch 1: practice integrity + journey/ticket layout. Consult docs/CODEBASE-FACTS.md and agent memory. Step 0 traces are done; file paths below are from them. Fix exactly these, nothing else. All fixes apply to shared components so verify web where a web surface exists; native layout claims require device, so enumerate what is deferred-to-device in your report.

Nocatch must never celebrate. Badge awarding currently keys on totalAttempts with no band filter, so a nocatch first attempt fires the First Words badge celebration. Gate badge awarding so attempts with band nocatch never trigger newlyEarnedBadges (server side, badges/progressMetrics award path). Keep the nocatch attempt insert for analytics. Add a server test: first attempt nocatch awards no badge; first non-nocatch attempt awards First Words.
Nocatch is neutral, not negative. The full-bleed flash colors nocatch destructive red in the fColor else-branch. Give nocatch a neutral treatment consistent with the existing rule that nocatch gets nothing negative (haptics/cue/shake already correctly skip it). Pin with a test.
Retry-band CTA flip. On the band retry result card: primary ChunkyButton becomes "Try again" (retry action), secondary becomes "Next phrase". Hard eval failure state keeps its existing "Record again" primary. Update tests.
Journey header safe-area. Journey's map render uses Screen padTop false with a fixed paddingVertical 10 header and no insets. Apply top inset to the header (match how the loading/error branches behave). Do not double-pad those branches. Jest layout assertion for the inset.
Ticket right stub geometry. Two verified defects in TicketParts/JourneyPassCard: (a) ZoneStamp third-line text cap (maxWidth 0.8 x size) cannot fit real geoNames, ellipsis is guaranteed; (b) rotated 48px stamp bounds ~57px exceed the 56px slot, protruding toward the name strip. Fix so the full geoName renders un-truncated and the stamp stays inside its slot at both home (size 48) and journey header (size 44). Respect the written sizing contract: no percentage-sized SVG in flow. Note the memory constraint that Expo-web passed while device broke here before: mark stub rendering as deferred-to-device in your report.

Rules: one owner per file surface, enumerate test counts before and after (never bare "tests pass"), no em dashes in code comments or copy, report per-item with file paths, and list every item's web-verified / jest-verified / deferred-to-device status.