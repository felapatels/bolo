BRIEF B is now active; the build 32 binary is installed on device. Your scope is the WEB section (W1 through W8) plus compiling the combined findings table. Run W2 and W5 via the existing probe scripts where they apply, the suites are already certified, and flag which W items need human feel-checks (those will be done by the owner and folded into your table). The MOBILE section results will be pasted to you as the owner and tester complete them. Compile everything into the findings table (item, PASS/FAIL/BLOCKED, classification blocker/fast-follow/cosmetic, evidence) and WAIT.

BRIEF B: Release verification pass, build 32. Two sections: MOBILE runs against the installed TestFlight binary on a real device (never Expo web); WEB runs against dev web at the release lineage (post Brief A's commit). This is verification: findings are reported and classified (blocker / fast-follow / cosmetic), not fixed inline. Each item gets PASS, FAIL with description, or BLOCKED with reason. Screenshots or screen recordings for any FAIL.

=== MOBILE (on device) ===

M1. Install and cold-start: app launches clean, splash shows the resized mascot (~48% frame height, no stretch or crop), no crash, no blank frames.
M2. Boarding pass: tap tears the pass (jagged stub, recoil), haptic fires, navigation lands on the journey. Reduced motion (device accessibility setting): instant navigation, no tear, no haptic surprise.
M3. KNOWN DEFECT CHARACTERIZATION, punch-circle alignment (JourneyPassCard): document the misalignment precisely: direction and rough magnitude of the offset, whether it worsens at larger accessibility font scales, and screenshots at default and large text. This feeds the fast-follow fix; classify fast-follow.
M4. Journey map: comet renders as a single bright traveling head with glow on the active run (not the old dash pulse); scenery present with zone theming progressing urban to river; depth shadows consistent; parallax subtle on scroll; nothing overlaps stations or cards; scroll stays smooth on device. Reduced motion: comet absent, parallax absent, scenery static.
M5. Practice prev/next: chevrons present, free navigation both directions, disabled at edges and during recording/evaluating, hidden in test-out, hold-to-talk circle never shifts under the finger during recording. Attempts survive back-and-forth visits.
M6. Resume ruling on device: enter a station with interior unmastered phrases (or set one up): session starts at the first unmastered, not phrase 1; an all-mastered station starts at phrase 1. No query param needed.
M7. Games hub: 2-column grid, five distinct per-game hues, vignettes idle slowly and wake on press, locked cards washed-out same-hue holding a static frame until pressed, entrance cascade with bounce, press squash + hue glow, scrolling the grid never fights press feedback. Reduced motion: fully static.
M8. Band labels everywhere: home Recent plays shows band labels (item 8 fix), no raw numeric score anywhere in the app (spot-check practice results, progress, badges).
M9. Mic permission: on a fresh install (or after resetting mic permission in Settings), granting the permission mid-flow never auto-starts a recording; an explicit hold is required afterward.
M10. Entitlement gates: free account on a locked language: phrase entry and test-out are content-gated with copy that reads as a language/All-Access unlock, never as paying for attempts; the teaser station serves exactly the taste set. All-Access account on an entitled language: test-out entry sails through with no paywall.
M11. One-path structure: phrasebook door present on home and routes into the gated learn path; no tour anywhere.
M12. General stability: ten minutes of normal use (practice a few phrases, navigate, play a game preview, background and foreground the app) with no crash, no frozen audio, no stuck states.

=== WEB (dev at release lineage) ===

W1. Boarding pass: tap plays the RECORDED paper tear (not a cymbal), synced to tear start, subtle at normal volume; haptic on supporting browsers; tear hand-off overlay carries across the route swap and gusts away; reduced motion instant and silent.
W2. Journey map: comet, scenery, 2.5D depth, parallax, all per M4's checks at desktop and phone widths.
W3. Practice prev/next: chevrons and arrow keys, same disable/hide rules as M5.
W4. Resume ruling: same check as M6.
W5. Games hub: full #986 energy model per M7's checks, at 320/480/640/1280 widths.
W6. Entitlement gates: same pair of checks as M10 (free/locked and All-Access/entitled), on web surfaces.
W7. Band labels: no raw numeric scores anywhere on web surfaces.
W8. Smoke of the core loop: sign in, boarding pass, complete one phrase with audio feedback, see the result band, return to journey, progress reflected.

=== CLOSE ===
Compile the findings table (item, PASS/FAIL/BLOCKED, classification, evidence link). Blockers stop the external submission; fast-follows queue as the first post-release batch; cosmetics join the ledger. Report the table and WAIT.