Chunk 1 rulings on your Step 0 report. Files are now uploaded; confirm all three are present and readable, read the INTEGRATION-MANIFEST and the helpers module comments, then report any manifest item not covered below and wait for my ruling on those only. Everything ruled here is a build go-ahead.

RULING 1, earlier standalone paste: the file Pasted-TASK-Update-the-Data-retention-section... in attached_assets is superseded. Ignore it entirely; this chunk's Section D plus these rulings govern.

RULING 2, Section D "contacting us": overriding the plain-text default. Link the phrase "contacting us" to the /contact form page route. Not a mailto.

RULING 3, Section D scoped addition, authorized because it is the same file and a dead-address fix: in the Contact us section of privacy.tsx, replace the privacy@bolo-india.app mailto with a link to the /contact form page route. Adjust the surrounding sentence minimally so it reads naturally as pointing to a contact form rather than an email address; keep the change to that one sentence. Do not add any email address anywhere on the page. Report the exact before and after sentence in your done report.

RULING 4, Section D date: update LAST_UPDATED to 'August 2, 2026' in the existing format.

RULING 5, Section C mascot: your recon shows touch-callout, user-drag, user-select, and draggable=false already applied from the hold-to-speak work. Add onContextMenu preventDefault on the mascot image element only, then consider Section C code-complete. It goes on the iOS-device-only verification list for my device pass; do not claim it verified.

RULING 6, item 4a: your wiring plan is approved: onClick priming additions at journey.tsx 1081, 1108, 1188, home.tsx 507, 412, 232 (handlePassActivate), and category-detail.tsx 135, 148. In the playCoach catch at practice.tsx 677, distinguish NotAllowedError (and NotSupportedError) from synthesis failure: play() rejection sets the tap-to-hear affordance on the existing speaker control; synthesis failure keeps current behavior plus the deck's copy if the deck covers it. Do not touch lib/sound.ts; cue-sound rejection swallowing stays as designed.

RULING 7, item 4c: your provisional wiring is approved, subject to the module's own comments where they differ: first playing_coach entry per mount, gate on isIosSafariWeb() and shouldShowIosAudioHint(), render the hint near the speaker control with the deck's hint copy, markIosAudioHintShown() when shown. Native app never sees it.

RULING 8, Section A: apply the deck verbatim per the manifest mapping to the strings you enumerated in practice.tsx and chat.tsx. Where the deck has no entry for an existing string, leave the existing string alone and list it in the done report. Do not invent copy.

GATE unchanged from the task paste: one gate at the end, one commit, message: "Chunk 1: web polish batch (error copy deck, iOS audio priming and hint, mascot long-press, privacy policy retention)". Include in the done report: new web test count against baseline 384/54, the Ruling 3 before/after sentence, the deck-unmapped strings list from Ruling 8, and the iOS-device-only verification checklist for my device pass. Then STOP for my confirmation before committing. No suggested or draft task cards. No em dashes.