TASK: Tiny UI batch (3 items, one commit to main)

SESSION RULES (binding):
1. Report, then confirm, then commit. Produce exactly ONE report at the end (Step 0 confirmations + done report + gate results), then STOP and wait for approval before committing. No narration in between.
2. Hard-stop exception: if any Step 0 verification MISMATCHES what is stated below, stop immediately and report the mismatch only. Do not infer intent, do not proceed past a mismatch.
3. Main is the only writer right now; work directly on main. One commit at the end containing ONLY the files this task intends. attached_assets are never staged or committed; if any appear in git status, leave them untracked.
4. Do not touch artifacts/bolo-mobile in any way. Scope is the web client and one schema-file comment only.
5. No Suggested or draft task cards. No em dashes anywhere: code, comments, copy, or reports.
6. No new test files, no speculative tests. Update existing assertions only where item 1 changes rendered copy.
7. Do not run migration generation at any point in this task.

STEP 0 (verify this stated map; do not explore beyond it):
A. Locked-stop label: the web journey screen renders locked stops with a label of the form "Locked · N phrases". Confirm the component file and the exact field that distinguishes a PLAN-locked stop (premium content not plan-visible to the caller; planLocked or the equivalent derived from the access field) from a PROGRESSION-locked stop (sequence-locked but plan-visible). Confirm plan-locked stops currently render a count of 0.
B. Tear SFX: confirm where the web boarding-pass tear sound is loaded and played today, and confirm the prewarm pattern from commit ffccd0e and the blessed HTMLAudioElement singleton discipline from commit ca8fe04 (singleton per audio type, blessed on entry gesture, never per-play) as the house patterns to follow.
C. Schema comment: confirm the file where the contactSubmissions table is defined and quote the current comment text on or above it.
If A, B, and C all match, proceed without stopping and fold the confirmations into the end report. Any mismatch: rule 2.

ITEM 1: Plan-locked stops omit the phrase count.
When a stop is plan-locked, render the locked label WITHOUT the count segment: "Locked" plus the existing lock treatment only, never "· 0 phrases", never any count. Progression-locked stops keep their current label including real counts. Rendering change only: zero changes to entitlement logic, server responses, gating conditions, or any API call. If this cannot be achieved as a pure rendering change, that is a rule 2 stop.

ITEM 2: Prewarm the web tear SFX.
The tear sound lags the tear visual because the audio is fetched or decoded at play time. Apply the ffccd0e prewarm pattern so the tear SFX asset is warmed when the boarding-pass surface mounts or on the preceding user gesture, and route playback through the ca8fe04 singleton discipline (reuse the existing blessed element for this audio type if one exists; never create per-play elements). Result: sound starts with the visual. Web only.

ITEM 3: contactSubmissions comment refresh.
Rewrite the stale comment on the contactSubmissions table definition so it accurately describes the table as it exists today, including that anonymous submissions are accepted and that rate limiting is currently the in-memory limiter pending the DB-backed migration. Comment text only. No column, type, index, or constraint changes. No migration generation (rule 7). The diff for this file must show comment lines only.

GATE (lean tier; nothing here touches entitlements, scoring, money, or migrations):
1. Typecheck across every touched workspace (web plus the workspace owning the schema file).
2. Targeted tests only: run the existing test files covering journey stop rendering (updating any assertion that hard-codes the old plan-locked label) and any test importing the schema file. Do NOT run the full web or api suites. Do not add tests for SFX timing; that is owner-eyeball territory.
Reference baselines, do not run them: web 411/58, api 726/15 with the one documented PILOT_CAPTURE_USER_IDS env-trap failure, mobile targeted 17/17.

REPORT (one message, then STOP for approval before commit):
1. Step 0 A/B/C confirmations with file paths and the exact plan-lock field name.
2. Per item: files changed, one-line description.
3. Item 1: exact rendered label for a plan-locked stop and a progression-locked stop, before and after.
4. Item 2: where the prewarm hook was placed and which singleton path playback uses.
5. Item 3: old comment text and new comment text.
6. Gate results: typecheck summary and targeted test counts, enumerated by file.
7. git status summary and the exact file list to be staged, confirming zero attached_assets.
8. Proposed commit message: "Tiny UI batch: plan-locked stops omit count, tear SFX prewarm, contactSubmissions comment refresh"
After approval, commit exactly that file list to main and confirm the hash. Nothing else.