TEAR SFX LANDING (by intent, serialized after your current work)

Sequencing: finish your in-flight work FIRST: the pre-brief commit, the Task 985 landing verification with the tree scan between, and the single full web run certifying the combined baseline (expected 364). Post those results. THEN execute this landing as one additional small commit before the mobile parity brief arrives.

Context: Task #984 built the recorded-asset tear SFX swap in its checkout; its apply is orphaned in the platform merge queue (same ghost state as #967) and will never complete. Its payload lands by intent on main, the same pattern as the #973/#978 re-lands. The synthesized burst in src/lib/tearAudio.ts (landed at 67222f7) reads as a cymbal, not a paper tear; the recorded asset replaces it. The module's exported interface (preloadTearAudio, playTearSfx), the home.tsx wiring, the silent-failure contract, the fire-and-forget guarantee, and both existing test pins are UNCHANGED; only module internals change.

ASSET: attached to this message as tear-sfx.m4a (13,080 bytes, 0.985s, mono, 44.1kHz, loudness-normalized, -1dB true peak). Place it byte-identical at the bundled path #984's diff references. If the diff references a different filename or an mp3, keep the diff's path but use THIS file's bytes; report any discrepancy rather than re-processing audio.

PAYLOAD from #984 (apply by intent; if git apply rejects on whitespace, apply per-file by intent from the labeled contents):

[PASTE #984's DIFF OR FILE CONTENTS HERE, VERBATIM, WHEN IT POSTS THEM. If #984 has not yet posted its payload, stop at this point and say READY FOR PAYLOAD; it will be forwarded.]

IMPLEMENTATION REQUIREMENTS (verify the payload satisfies these; fix by intent where it deviates):
1. preloadTearAudio(): keeps the suspended-context pre-warm; adds fetch + decodeAudioData of the bundled asset into a module-cached AudioBuffer at mount, so first play has zero fetch or decode lag. All failures silent.
2. playTearSfx(): plays the cached buffer via AudioBufferSourceNode through a GainNode. Use #984's reported final gain as a named constant; if none was reported, start at 0.55 and note it for ear-tuning on dev. If the buffer is not ready (preload failed or never ran), fall back to SILENCE, never to a fetch-then-play that would lag the tap.
3. The noise-synthesis code path is DELETED entirely; no dead fallback remains.
4. Preserved: fire-and-forget, never throws, never delays animation or navigation, warm-context reuse, reduced-motion path unchanged (calls never fire there).
5. Both existing pins in home-boarding-pass.test.tsx pass unchanged (the module is mocked there); add no heavy new tests (decodeAudioData is not jsdom-testable; skip rather than mock-heavy).

GATE: typecheck clean, both boot smoke tests pass, full web suite green at the certified baseline (the tear swap adds no test-count change unless #984 added a cheap module test; report the number either way), api canonical unchanged (682 / 681 / 1, pilot-capture fence only). Commit with a clear message crediting the by-intent landing of #984's work, post the hash, and confirm zero markers via the tripwire pre-step. Then report READY FOR BRIEF A.

Afterward, for the record: task #984's checkout is superseded; it will be told to stand down separately. Do not interact with its Ready card or the platform's merge state for it.