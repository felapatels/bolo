BRIEF: Free-tier content policy (owner-ruled). Two tiers, data-first, smallest code surface. No em dashes.

POLICY (binding):
1. HINDI IS THE DEFAULT FREE LANGUAGE: every free account has Hindi open regardless of any chosen-language state: hi is the universally-open starter line. If the current model locks free users to a single chosen language, hi becomes additionally open for all free callers; a free user whose chosen language is Gujarati has Gujarati's free tier PLUS Hindi's.
2. HINDI FLAGSHIP SCOPE: all Fare Zone 1 (New Delhi / Greetings & Manners) Hindi groups serve FREE: a free user rides the full first zone to the Aligarh gate. The Hindi paywall lands at Zone 2.
3. ALL LANGUAGES: the first stop (position-1 group, ~10 phrases) serves free in every language, ratifying the current seed where true and correcting any language where the index-seed left the first stop premium.
4. Teaser taste-sets on locked languages remain byte-identical; attempts are never gated, only content.

STEP 0 (report, STOP before changing anything):
1. Report how the current model expresses free-language access (chosen-language column, entitlement derivation, language 402 path) and the exact smallest change that makes hi universally open for free callers per policy 1: state whether it is pure data, a derivation tweak, or both.
2. Premium-flag distribution: per language, which Zone 1 groups are all-premium today, and the exact phrase counts that flip free under policies 2 and 3.
3. Confirm plan-visibility (S2) derives purely from the premium flags plus the language gate, so the remainder is a DATA migration with zero serving-code change; identify any code path that hardcodes assumptions this breaks (boarding pass next-stop derivation, listing counts, capstone entitlement, home hero pass).
4. Confirm the Zone 1 hi capstone chat is reachable by a free account or report what gates it (chat-minutes metering interplay: report only, change nothing).

THEN (after my confirm on Step 0): the smallest hi-open change per item 1 plus a generated, reversible data migration reflagging per policies 2-3. Pins: free caller with a NON-hi chosen language serves full hi Zone 1 (every group nonzero plan-visible count); hi Zone 2 402s locked-with-upsell; one non-hi language serves Stop 1 free and Stop 2 locked; teaser pins unchanged. Gate: typecheck plus targeted tests, no full suite. Report, WAIT, commit on my confirm.

Device check after commit (owner): the free Gmail account rides Hindi Stops 1-9 unlocked on the map regardless of its chosen language, boarding pass advances past Stop 2, Aligarh renders as the locked gate, and one non-hi language shows Stop 1 open / Stop 2 locked.
