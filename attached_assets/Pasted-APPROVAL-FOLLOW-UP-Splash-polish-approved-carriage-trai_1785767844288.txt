APPROVAL + FOLLOW-UP: Splash polish approved; carriage train match ordered next. Execute in exact order.

PART 1, COMMIT NOW: Pre-check git log -1 --decorate; if HEAD is not 9485672 on main, STOP and report. Stage exactly these four files and nothing else:
artifacts/gujarati-coach/src/components/brand-splash.tsx
artifacts/gujarati-coach/src/index.css
artifacts/gujarati-coach/src/test/home-brand-splash.test.tsx
docs/CODEBASE-FACTS.md
Commit message: "Splash polish: minimum hold and compose-then-reveal"
Confirm with git log -1 --decorate and git show --stat HEAD (exactly those four files). Include this confirmation at the top of the Part 2 report.

PART 2, NEW TASK (owner-ruled, ONE canonical train): the splash carriage must match the boarding-pass train users already see. ONE report, then STOP for approval before the second commit. No task cards.

STEP 0 (verify, misses hard-stop): locate the train graphic on the home boarding pass ("Ride the Ganga Line" card). Quote how it is rendered (asset path if an image, component/SVG source if drawn, character if an emoji) and list its colors/palette.

BUILD: redraw artifacts/gujarati-coach/public/splash/carriage.svg to match the boarding-pass train's livery, palette, and silhouette. BINDING: keep the transparent evenodd window aperture (Bolo composites into it), keep the same viewBox and slot dimensions so brand-splash.tsx needs zero code changes. If the boarding-pass asset is raster, approximate its palette and silhouette in SVG. Steam puffs untouched. Expected diff: carriage.svg only, plus a docs/CODEBASE-FACTS.md line recording the ONE-train rule (splash carriage derives from the boarding-pass train; owner final art supersedes both via SPLASH_V2_ASSETS paths). Anything beyond goes under DEVIATIONS.

GATE (asset-only change, lean per cost rules): web typecheck; run src/test/home-brand-splash.test.tsx only (9 tests). No full suite.

PART 2 REPORT: Part 1 commit confirmation; Step 0 quotes; one-line description of the redrawn carriage; files changed; DEVIATIONS or "none"; gate results; git status + exact staged list; proposed commit message: "Splash carriage: match the boarding-pass train"
END OF REPORT is the final line. Then STOP and await approval for the second commit.

END OF TASK. If this line is not the last line you received, the paste truncated; stop and say so.