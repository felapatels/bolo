Good report, and I was wrong about one thing: I expected "Keep trying" and the token decoding to be the same bug. They weren't. The token removal held, and the label was just a title ternary falling through on an undefined band. Your diagnosis is better than my guess.

Two of the three test fixes are right. The second one is not, and it's a real bug.

Spec 0 rule 40 sets the band thresholds: 80 and above is nailed, 55 to 79 is close, below 55 is retry. A score of 50 is below 55, so it should be retry, which renders Try again. That test assertion is correct. If the code is producing close for a score of 50, the threshold is implemented wrong and changing the test would hide it.

This is exactly the failure mode worth catching: making the test match the code instead of checking which one is right.

Paste this:

Approve two of three, stop on the second.

Apply now:

The celebrations factory fix, copying band and xpAwarded through instead of dropping them.
The unanchored regex fix in practice-score-trail.test.tsx, adding ^...$ anchors.

Do NOT change the score=50 assertion. That test is correct and you are about to hide a bug.

Spec 0 rule 40 defines the thresholds: 80 and above is nailed, 55 to 79 is close, below 55 is retry. A score of 50 is below 55 and must produce retry, rendering Try again. The test expecting Try again is right.

Investigate and report, without changing anything: show me the actual band threshold implementation in openai.ts, the exact boundary values, and whether the test is passing a raw score of 50 or a hardcoded band. If the thresholds are wrong in the code, fix the code, not the test. If the test mock hardcodes close alongside a score of 50, the mock is internally inconsistent and the mock is what changes.

Also, your Spec 1a report covered placement but not these. Confirm each:

Were todayXp and dailyGoal added to GET /progress/summary, summing xp_ledger.xp using localDayKey(), and was the schema regenerated?
Was the sound preference toggle added to both account screens with the soundPref reader modules, and does nothing play audio?
Did review.tsx have its own header component, which was the one stop-and-ask?