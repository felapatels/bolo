Right — batch 2's in review, so the evidence can't ride it, and a fix task would collide with batch 2's file ownership. So the independent task is investigation-only: no code changes, which makes it collision-free by construction. Findings then either confirm batch 2's item-3 fix already covers it, or spec a small respin after batch 2 merges.

→ NEW TASK

Investigation only. Report findings. Do NOT change any code. This runs alongside a code-review-stage task that owns chat.tsx, tabs _layout.tsx, and lib/audio.ts — you must not edit any file. Consult docs/CODEBASE-FACTS.md and agent memory (ios-earpiece-routing, chat-voice-streaming, tts-provider-switch).

Subject: chat reply loudness asymmetry, now reproduced cold-start on iOS build 29 device: force-quit, reopen, straight to chat. Greeting reply plays loud and clear. After the first recording, the next reply and all subsequent replies are consistently quieter. Reproducible, not intermittent.

Working hypothesis from the prior trace: playStreamingAudio/playBase64Audio perform the iOS PLAYBACK_MODE flip only when recordingSessionActive is true. The greeting plays before any recording activity (flag false, default session, full volume); every post-recording reply plays through the flip path and lands quieter.

Answer with evidence, file paths, and quoted conditions:

Flag lifecycle: trace recordingSessionActive from app cold start through greeting playback, first recording, and second reply. Confirm or refute: greeting plays with the flag false and skips the flip; reply 2 plays with the flag true and takes the flip. Include the mount-time pre-warm at chat.tsx:322-324 in the timeline — does the pre-warm set the flag true BEFORE the greeting plays when mic permission was already granted (as it is on this device)? If so, the greeting should also take the flip path, which would weaken the hypothesis — resolve this explicitly.
What the flip actually configures: enumerate the exact setAudioModeAsync payloads for PLAYBACK_MODE vs RECORDING_MODE vs the app-default session. Identify any iOS-level consequence of each (output route, category, mixing behavior) that plausibly attenuates volume. Distinguish verified-from-code vs inferred-iOS-behavior.
Race audit, read-only: the fire-and-forget restore on playback end and the post-turn idle pre-warm — lay out the exact ordering on the queue for the sequence: recording stops, reply playback starts, playback ends, pre-warm fires. Identify any interleaving where recording mode is active while reply audio is still playing.
Provider check: determine whether both replies in a cold-start session come from the same TTS provider. Where is provider/format decided and is it logged or derivable per reply (server logs, response fields, ttsCache rows)? If the greeting is cache-served ElevenLabs and live replies can fall back to gpt-audio, say so — that would make source mastering a candidate cause instead of session routing.
Verdict: rank the candidate causes with your evidence and state the minimal fix shape for each, so a fix task can be specced the moment the in-review task merges. Do not implement.

Report format: numbered, verified vs inferred, no code changes, no task proposals beyond the verdict.