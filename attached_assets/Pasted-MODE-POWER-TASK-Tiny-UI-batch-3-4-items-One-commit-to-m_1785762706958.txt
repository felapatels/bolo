MODE: POWER

TASK: Tiny-UI batch 3 (4 items). One commit to main. Main HEAD at start: cf4cade.

HARD RULE: ZERO git commits before the owner's approval message arrives in this pane. ONE report, then STOP. If any commit exists that you did not make after approval, stop and report it. No task cards; bank suggestions as report lines.

SESSION RULES (binding):
1. Scope: artifacts/gujarati-coach only, plus docs/CODEBASE-FACTS.md. Do NOT touch the api server, lib/, or bolo-mobile. Work on main directly; you are the only writer.
2. No em dashes anywhere. attached_assets never staged. No new test files unless an existing file already asserts a changed surface. Zero generated-file strays before the report.
3. Any divergence from this paste goes under a DEVIATIONS heading with rationale.

STEP 0 (verify, fold into report, misses hard-stop):
A. Toast plumbing. Quote the code at and around the Toaster mount you previously reported at src/App.tsx line 495, with enough surrounding JSX to show which route tree or layout branch renders it. Then enumerate the layout components the authenticated /app surface actually renders and state plainly whether that tree contains a Toaster. Routes are truth, comments are hints.
B. Grep the web workspace for every Toaster mount and for any second toast system (sonner, react-hot-toast, notistack). List all hits.
C. Quote the subscription mechanism in src/hooks/use-toast.ts: how toast() dispatches and how the Toaster subscribes.
D. Review upsell card: locate it, quote its current badge/title layout block.
E. Badge card that currently dead-ends: locate it, quote its wrapper and what tap currently does.
F. Home stats row: quote the current StatCell signature and the Chai cell usage from ebf2993.

OWNER-VERIFIED EVIDENCE (do not re-litigate): on dev web, POST spend returns 409, the tokens refetch fires (onSettled runs), console shows no errors, and NO toast paints, including the phrase-report toast. Toasts are broken app-wide on web. The failure is between toast() dispatch and the Toaster painting.

ITEM 1: Toast repair. From Step 0 A-C, state the root cause in one sentence. Fix with the minimal wiring change: exactly one Toaster, rendered in the layout every authenticated route actually renders. Do not switch toast libraries, do not restyle. Record the root cause and the one-Toaster rule in docs/CODEBASE-FACTS.md.

ITEM 2: Review upsell card responsive collapse. At narrow widths the badge yields before it overlaps the title: badge wraps or hides at the breakpoint where overlap would begin; the title never truncates under the badge. Use the card's existing responsive vocabulary; no redesign.

ITEM 3: Badge card navigation. Tapping the badge card navigates to Progress, using the same navigation mechanism the app uses elsewhere. The card becomes a real interactive element (button or link semantics), not a div with an onClick.

ITEM 4: Chai stat cell affordance. In the home stats row: (a) a chevron-right glyph trails the CHAI label at reduced opacity, same icon set as the row; (b) the Chai cell gets a pressed state using the row's existing tap-feedback vocabulary; (c) with onClick present, StatCell renders as a real button element, keyboard accessible, not a motion.div with a click handler; (d) the four static cells stay exactly as they are, no new hover or pressed states. If the chevron causes layout shift or wrap at phone width, report under DEVIATIONS with a description; do not improvise a redesign.

GATE (lean during, one full run at the end):
1. Typecheck the web workspace; mandatory.
2. Grep-first: enumerate test files asserting any touched surface (stats row, badge card, upsell card, toast). Update only where genuinely affected; run those during development.
3. At the end: ONE full web suite run. Baseline 413/58; report the runner line verbatim with any delta enumerated by file. Any new failure: report, do not commit.

REPORT (one message, then STOP for approval before any commit):
1. Step 0 A through F with quoted blocks.
2. Item 1 root cause in one sentence, then the fix in one sentence.
3. Files changed, one line each.
4. DEVIATIONS or "none".
5. Every user-visible string added or changed, verbatim (expected: none; confirm).
6. Gate results including the full-suite runner line.
7. git status and the exact staged list; zero attached_assets, zero strays, zero files outside gujarati-coach plus the facts doc.
8. Proposed commit message: "Tiny UI batch: app-wide toast repair, upsell badge collapse, badge card to Progress, Chai cell affordance"
END OF REPORT is the report's final line. After approval only: commit, confirm with git log -1 --decorate and git show --stat HEAD, stop.

END OF TASK. If this line is not the last line you received, the paste truncated; stop and say so.