Rulings on Step 0, plus one addition:

RULING 1, chip: proceed on station.planLocked === true, and yes, the consequence is the intent, confirmed explicitly: for Plus and Family callers no chips render anywhere, and the diamond becomes purely an upsell marker on stops the caller cannot ride. That is correct product logic: an entitlement badge shown to someone who already owns the entitlement is noise, and showing it on stops a free caller can now actually ride was the lie we're removing. The stage-based rail marker stays untouched as you noted.

RULING 2, review upsell card: accepted as already shipped, item is a no-op, remove it from this batch. Note it in the done report as verified-shipped so the ledger closes it.

RULING 3, badge celebration overlays: proceed on both platforms. Add a secondary action to the celebration overlay, web BadgeUnlock navigating to /progress and mobile BadgeUnlock router.push to the Progress tab, alongside the existing dismiss, matching each platform's existing button styling. The primary dismiss behavior is unchanged; do not make the whole overlay tappable-to-navigate, keep it an explicit labeled action (suggested label: "See progress"). Do not touch the home latest-badge card or the mobile Badges entry, which already navigate.

RULING 4, rail pulse: confirmed shipped on both platforms, out of this batch, closed on my ledger.

ITEM 5, ADDED to this batch, owner-found bug: on the /privacy page, the "contacting us" link (and the Contact us section's "contact form" link from the earlier fix) currently lands on an anchored homepage spot instead of the contact form page. Diagnose first and report in one line: whether the /contact route on web renders the contact form or redirects/falls through to the homepage, and whether the links are correctly href /contact or actually pointing at /#contact or similar. Then fix so both links land on the rendered contact form page. If the root cause is the route rather than the links (contact.tsx not actually mounted or shadowed), fix the route mounting; the links themselves were verified as Link href /contact in the earlier task, so suspect the route. Scope stays surgical: the contact page and the two links, nothing else on either page.

GATE unchanged from the task paste, now covering items 1, 3, and 5: typecheck; full web suite green against 411/58, report count; mobile targeted suites for touched files; browser checks: chip absent on a free-served Hindi sentence stop and present on a locked one, badge overlay's See progress lands on /progress, and /privacy's contacting-us link lands on the rendered contact form. Then STOP, report done, await confirm, commit ONCE with message: "Tiny UI batch: All-Access chip keyed to planLocked, badge celebration links to Progress, privacy contact links land on contact form". No task cards, no em dashes.