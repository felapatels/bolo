→ NEW TASK

Build 30 fix batch 2: chat record trigger + reply audio quality. Consult docs/CODEBASE-FACTS.md and agent memory (mic-prewarm, ios-earpiece-routing, chat-voice-streaming). The Step 0 trace is done; this task starts from its findings plus device evidence. Fix exactly these, nothing else.

Device evidence to reason from: on iOS build 29, holding the center nav Bolo bird on the chat page gives visual press feedback but NO haptic and NO recording. Per the trace, BoloTabButton.handlePressIn fires hapticLight() then startRecordingRef.current?.() — no haptic means handlePressIn is not executing. The visual feedback likely comes from the button's own press styling.

Find and fix why handlePressIn never fires on device. Prime suspects from the trace: the focused detection for the chat tab (nested navigator focus state), the chatRecording.register timing (ref still null or registration not yet run when the user presses), or the press handlers not being attached to the actual touch target on native. Trace which link is broken, fix it, and add a test pinning that when the chat tab is focused, press-in on the nav bird invokes the registered start handler. The on-screen chat mascot uses the same handler chain — verify it is reachable on native too and state which of the two triggers your fix repairs.
Barge-in through the same path. The trace confirms barge-in logic exists (phase 'playing' is an allowed start state; playbackRef.stop, turn orphaning, mode flip all present). Once item 1 makes the trigger fire, verify the interrupt path end to end in tests: hold during phase playing stops playback, orphans the in-flight SSE turn, and transitions to recording. Pin the playbackModeToken race: a stopped playback's mode restore must not override the new recording's ensureRecordingMode.
Reply loudness/quality mismatch. Device evidence: first greeting reply is loud and clear, subsequent replies are noticeably quieter and lower quality. The trace found no explicit gain anywhere, and identified these seams: (a) greeting played before mic permission is granted skips the PLAYBACK_MODE flip and plays in the default full-volume session, while every later reply plays after mode flips; (b) the flip's catch plays anyway on setAudioModeAsync failure; (c) restore on playback end is fire-and-forget; (d) the post-turn idle pre-warm re-asserts playAndRecord right as playback ends. Also the source asymmetry: ElevenLabs mp3 44.1k/128 vs gpt-audio fallback. Determine which seam produces the device symptom, fix so ALL chat replies play under the same full-volume playback configuration as the first greeting, and report which cause it was with evidence. Do not remove the "quiet audio beats no audio" fallback; make the primary path reliable instead.

Rules: one owner per file surface (this task owns chat.tsx, tabs _layout.tsx, lib/audio.ts — do not touch practice/[id].tsx, journey, or ticket components, batch 1 owns those). Enumerate test counts before and after. No em dashes in code or copy. Report per item: root cause found, fix, web-verified / jest-verified / deferred-to-device status. Audio session routing outcomes are deferred-to-device by definition — say so explicitly and list the exact device checks I should run.