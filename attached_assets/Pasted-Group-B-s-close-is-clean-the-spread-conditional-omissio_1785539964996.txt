Group B's close is clean — the spread-conditional omission keeps entitled responses byte-identical while showroom drops the key (the honest contract: showroom is counts-and-statuses only), tests untouched and green, ruling recorded, and the api suite's steady state is now **680/679 with pilot-capture's env-induced failure as the sole fence**. Accepted.

Here's the **#905 brief**, in the same release format:

---

RELEASE: Task #905 (ticket tear finishes smoothly across the route swap). Build fresh against current main HEAD (2309fca) — your Ready card predates the recovery; do not apply any pre-recovery branch. The Apply button is not the release mechanism; you report and WAIT.

Scope: web only, home.tsx tear markup/handler + index.css tear constants + home-boarding-pass tests + the qa tear probe. The final ~90ms of the tear (torn-stub fade tail) is currently cut when navigation at --tear-nav-delay unmounts home. Make the tear complete or hand off deliberately across the swap.

Approach preference: let the torn piece finish OVER the incoming route — a fixed-position/portal overlay for the stub that outlives home's unmount and removes itself on animation end. Do NOT cross-fade into the journey header ticket (different element, different geometry; a morph implies false object identity). If the overlay approach fights the router's unmount/transition timing, report the conflict rather than forcing it.

Invariants from #899, all preserved: navigation never blocked and never slower than today's ~360ms activation-to-nav; reduced-motion = instant navigation, no tear; animation failure falls through to immediate navigation; transforms/opacity only; all timing/geometry constants stay in the :root tuning block (add any new overlay constants there); tear fires on the home pass only, never the journey header ticket.

Tests: existing 8 home-boarding-pass tests stay green; add pins for overlay-cleans-up-after-animationend (no orphaned DOM node), reduced-motion instant path, and failure-fallback navigation. Gate: typecheck clean, full web suite green (336 + your additions, report exact count), both boot smokes pass in the canonical run, api-server canonical unchanged (680 with the single pilot-capture fence). Report results by name and WAIT for confirmation before apply or markTaskComplete.

---

One orphaned-DOM note baked in deliberately — a portal stub that outlives its page is exactly the kind of element that leaks if animationend never fires (backgrounded tab), so the cleanup pin includes a timeout fallback if the agent finds animationend unreliable.

And the commander question has answered itself — the queue is running through this thread again. Fine by me; just keep it singular. Remaining after #905: the boarding-pass tap, the user-id env var, and the session your voice owes the pilot.