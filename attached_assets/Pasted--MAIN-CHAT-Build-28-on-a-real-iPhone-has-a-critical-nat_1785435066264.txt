→ MAIN CHAT:

Build 28 on a real iPhone has a critical native-only layout bug that Expo-web verification missed, on two surfaces sharing the ticket component:

Symptoms (device screenshots):

Home: the boarding-pass hero (JourneyPassCard) stretches to fill ALL remaining screen height — a full-screen red slab below its content. Content renders correctly (बोलो रेल eyebrow, train, stamp, progress, CTA); the card just never stops growing downward.
Journey screen: the header ticket consumes the ENTIRE viewport — pale striped ticket fills the screen, the fare-zone stamp floats mid-screen, and the map itself (zone postcards, track, stops) is pushed off-screen or collapsed to nothing.

Both surfaces render correctly on Expo web, so this is a native Yoga layout divergence in the shared ticket component: its height is unconstrained on native while web contains it.

Diagnose in this order:

The diagonal-stripe pattern layer and every absolutely-positioned decorative fill inside the ticket — is any of them sized to a parent with no bounded height, or does the pattern layer itself establish height?
The absolutely-positioned stamp strip and measured vertical line-name text from the July 30 stub fix — absolute children can force parent growth on native; check whether the measured strip's positioning inflates the card.
flexGrow/alignItems stretch defaults on the card, its wrapper, and the ScrollView contentContainerStyle on both screens — anything web's flex engine treats as content-sized that Yoga treats as fill.
Whatever the root cause, the fix must make the ticket's height intrinsic/content-driven on native. Add an explicit sanity constraint as a belt (e.g., maxHeight or fixed content-derived height) so no future absolute-child change can reproduce a full-screen ticket.

Verification requirements — Expo web is NOT sufficient for this bug:

Render the two screens in a native-representative way if any path exists in this environment (iOS simulator or native layout snapshot); state honestly what is and isn't verifiable here.
Add jest assertions on the resolved layout: ticket height must be bounded/content-derived, and the journey screen must render zone 1 and stop 1 within the initial viewport tree.
Report the root cause precisely, the fix, and what remains device-only verification.

After the fix commits: bump the build number and trigger the EAS iOS production build (respin of the tester build). No eas submit — .ipa link for Transporter. I re-verify on device before any tester assignment.

Nothing else in the build is affected — nav, scripts, copy, icons all verified good on device. This is one component's height. Scope strictly to the ticket sizing; no other changes ride this respin.