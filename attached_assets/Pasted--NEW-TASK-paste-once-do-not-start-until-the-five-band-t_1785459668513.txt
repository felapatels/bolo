→ NEW TASK — paste once — do not start until the five-band task has merged (it rebuilds the result card this touches)

Web practice: retry-path recording race + romanized transcript. Consult docs/CODEBASE-FACTS.md and agent memory (mic-grant guard patterns, the chat.tsx unobserved-release fix, tech-debt item on practice.tsx sharing that hole). Web practice surface plus one contained api-server change; mobile touched only to render the new romanized line.

Step 0 then fix: first hold after retry self-cancels. Device evidence (web, production): after a retry-band result, the first press-and-hold on the mascot enters the listening state then immediately reverts to idle without recording; the second hold works. Trace the retry path's state reset against the hold-to-speak lifecycle: what tears down or re-prepares between result card and next attempt, and what could release or abort a press that begins during/just after that window. Check specifically whether practice.tsx carries the unobserved-release hole that chat.tsx fixed (per the debt note). Report root cause with quoted conditions, then fix so a hold that begins at any point after the result card renders records reliably. Pin with a test simulating retry then immediate hold.
Server: romanized transcript in the eval response. The pronunciation eval response carries raw transcript only; romanization exists server-side in compareToTarget but is not returned. Add a romanized transcript field to the response (schema + generated types), populated from the existing romanization, no new computation paths. Confirm no band/score behavior changes.
Web: render "We heard" on the feedback card — raw transcript plus the new romanized form, matching mobile's placement/tone; integrate with the five-band card layout now in place. Mobile: add the romanized line to the existing "We heard" display. Nocatch handling unchanged (whatever each platform does today when there's no usable transcript stays).

Constraints: no scoring/band/mastery changes; regenerated api-client types must be reported explicitly (other queued tasks consume those hooks). Tests: retry-then-immediate-hold records (web), romanized field present in response (server), card renders both transcript forms (web + mobile). Enumerate test counts before and after, all three suites. No em dashes. Report per item with file paths and verification status.