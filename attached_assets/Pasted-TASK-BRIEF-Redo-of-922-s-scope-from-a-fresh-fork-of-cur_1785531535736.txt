TASK BRIEF: Redo of #922's scope from a fresh fork of current main (HEAD 360b45a). Your predecessor branch was stale (pre-freeze, byte-identical openai.ts to b78d828, deleted the 0033 migration snapshot) and its apply was reverted in 360b45a. Nothing from that branch is reusable; do not cherry-pick from it.

Step 1, baseline measurement before any edit: on clean current main, run the full api-server suite AND the full web suite (pnpm --filter @workspace/gujarati-coach run test) with canonical flags. Post both failure lists by name. This settles whether the web x17 failures pre-exist on the base. Do not fix anything in this step.

Step 2, scope:
a. Shared @workspace/db mock factory: one canonical mock object all tests consume, so schema additions update one place. Update the language-hint test to consume it.
b. Close known namedExports gaps: dailyQuizCompletionsTable, dailyQuizzesTable, lessonGroupProgressTable, lessonGroupTestoutsTable, phraseReportsTable, scriptTraceProgressTable, userItemMemoryTable; remove phantom phraseScheduleTable.
c. Group A (freePhraseReplenish x3): stale mock is layer one; layer two is the FK cleanup gap (after() deletes phrases while user_item_memory rows still reference them). Make cleanup idempotent against ANY starting DB state, including a fresh validation database. Note: these tests currently pass on main's long-lived dev DB; that is environment luck, not health. Your fix must pass on a fresh DB.
d. api-client-mock.ts hook stubs (useGetScenario, useListZoneStamps, QueryKey helpers) and the shared web test helper consolidation. None of this ever reached main.

Hard fences: showroom/teaser tests (owned elsewhere), openai.ts and ALL production route files, migrations and drizzle meta, the 3 TTS cache tests (passing, do not touch), pilot-capture test (Task #970 owns its env hardening).

Gate: typecheck clean, both boot smoke tests pass, full canonical suite, remaining failures BY NAME classified against your Step 1 baseline. Expected: Group A robustly fixed, Group B still failing (not yours). Report and WAIT for explicit confirmation before any apply or markTaskComplete. Your Ready-card Apply button, if one appears, is not the release mechanism; only a confirmation message in this pane is.