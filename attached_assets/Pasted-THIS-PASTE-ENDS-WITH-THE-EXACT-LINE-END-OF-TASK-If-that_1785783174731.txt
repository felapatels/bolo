THIS PASTE ENDS WITH THE EXACT LINE "END OF TASK". If that line is absent, the paste truncated; stop and say so before doing anything.

MODE: POWER

TASK: Hotfix 3S, server persistence + receipts for the signal system (5 items). One commit to main. Main HEAD at start: de33f68.

HARD RULE: ZERO git commits before the owner's approval message arrives in this pane. Tree stays UNSTAGED until approval. ONE report, then STOP. No task cards; suggestions bank as report lines.

SESSION RULES: scope = artifacts/api-server, artifacts/gujarati-coach, docs/CODEBASE-FACTS.md, PLUS lib generated client/zod outputs strictly as codegen artifacts of the api change (post-codegen rule: stage the entire generated directory, git status must show zero generated strays). No other lib/ edits, no bolo-mobile. No em dashes anywhere. attached_assets never staged. New test files only for genuinely new surfaces (justify). Failure identity before re-runs; full-suite runs tee to a log first. Never leave a committed-unapplied migration in dev: apply the new migration to the dev database as part of this task. Divergence goes under DEVIATIONS.

STEP 0 (verify, quote, misses hard-stop):
A. git log -1 --decorate; acceptable: de33f68 or exactly one "Published your App" atop it. Else STOP.
B. Quote the ledger refId shape for signal first-clear grants and closeout grants as actually written by the shipped grant code.
C. Quote the session-complete flow: which endpoint/response powers the "Session Complete" page, and where attempt-side-effect Chai earns (streak day, zone complete) are written during a session.
D. Quote the client's current wave memory (sessionStorage helpers) and where journey derives cleared state today.
E. Quote the payload the journey already loads that could carry per-signal reward amounts with zero new requests, and the codegen pipeline commands that regenerate the client types.
F. State the next migration number after 0038 and quote the migration tooling invocation.

ITEM 1, wave persistence: new table (next migration number) storing per-user waved signals, keyed user_id + a ref matching the existing signal ref conventions from Step 0 B, unique per user+ref, created_at. Endpoints: record a wave (idempotent) and read the user's waves, riding an existing journey-adjacent fetch if one fits (Step 0 E) rather than inventing a new route family. A later clear supersedes a wave for display; rows never need deletion.

ITEM 2, server-truth journey states: the client derives cleared from server data (ledger-backed, per Step 0 D findings) and waved from item 1, merging with sessionStorage as an optimistic cache only. Result: yellow and green survive devices and reinstalls. Auto-open and soft-stop rules unchanged.

ITEM 3, Session Complete Chai receipt: the session-complete surface reports total Chai earned during that session (attempt-side-effect earns aggregated server-side per Step 0 C; zero if none). Client renders a "+{n} Chai earned" pill directly under the XP pill, styled as the XP pill's sibling with the chai glyph,