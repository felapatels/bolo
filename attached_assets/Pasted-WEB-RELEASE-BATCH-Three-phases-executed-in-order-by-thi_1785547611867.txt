WEB RELEASE BATCH: Three phases, executed in order by this pane, gate between each phase. Web app and test files only, plus the named tooling items; no api-server route changes except where a phase explicitly says otherwise. Do not run until the #967 games hub merge has landed and its post-merge check passed. Task #972 (journey open sweep) runs separately and owns journey.tsx; do not touch journey.tsx in this batch. Consult docs/CODEBASE-FACTS.md at start. No em dashes anywhere.

==================================================
PHASE 1: Completed-station resume fix (practice loader)
==================================================
CONTEXT: A station at 8/10 mastered with status Completed, when revisited, starts at phrase 1 instead of the first unmastered phrase. Ruling made: revisiting ANY station lands on the first unmastered phrase (bestScore null or < 80) when one exists, regardless of completion status; phrase 1 only when all mastered or when no per-phrase data exists (including tested-out stations without per-phrase attempts). The reason a learner reopens a completed station is the unmastered phrases; this aligns with the checkpoint-polish direction.

STEP 0 (report, then proceed): locate the start-position derivation (Task #954 CODEBASE-FACTS section documents the loader locus). Report the branch structure: does completed/tested_out status short-circuit to index 0 before the firstUnmastered scan? Identify the mechanism of the reported wrong answer (status short-circuit, stale score data on the revisit route, or a bypassing entry path). If the current behavior turns out to be a documented deliberate spec choice, STOP and report before changing.

ACCEPTANCE CRITERIA:
1. The system shall start a practice session at the first phrase whose bestScore is null or below the mastery threshold, regardless of station completion status.
2. The system shall start at phrase 1 only when every phrase meets the threshold or no per-phrase score data exists for the session.
3. The system shall preserve the full phrase set; mastered phrases stay reachable via back-navigation and the prev/next controls.
4. The system shall apply identical behavior on every entry route into the session.
5. The system shall leave teaser sessions and test-out mode start behavior unchanged.

IMPLEMENTATION NOTES: fix loader-side per the documented locus so the mobile parity work inherits the rule; use the existing mastery threshold constant, no second definition; append the ruling to the CODEBASE-FACTS #954 section so mobile inherits it as spec.

TESTS (minimum): completed station with unmastered at interior positions starts at the first of them (regression pin for the report); all-mastered starts at 0; unmastered at position 0 starts at 0; tested-out with no per-phrase scores starts at 0; existing #954 and #973 practice suites pass unchanged.

PHASE 1 GATE: typecheck, boot smokes 2/2, full web suite green (348 + new pins), api canonical 682/681/1. Report by name, WAIT for confirmation before proceeding to Phase 2.

==================================================
PHASE 2: Error-copy and voice sweep (practice, home, chat)
==================================================
CONTEXT: Consolidated copy-and-behavior sweep, previously drafted and fenced until practice/home files were free. They are now free (Phase 1 done, #905/#978 landed, #972 owns only journey.tsx).

ITEM 2.1 Railway-voice error cards: audit every user-facing error state on practice, home, and chat surfaces (load failures, evaluation errors, network errors, mic errors). Rewrite copy into the app's railway-warm voice: plain, kind, in-world where natural, never technical jargon, never blaming the user. Keep error semantics and retry affordances identical; copy only, plus card styling consistency where an error card visibly diverges from the app's card language.

ITEM 2.2 Kitchen-metaphor purge: search all user-facing copy for cooking/kitchen metaphors (known legacy from early drafts) and replace with railway/journey-consistent phrasing. Report each occurrence found and its replacement.

ITEM 2.3 Em-dash and emoji sweep: remove em dashes from ALL user-facing copy app-wide (standing rule); replace with commas, colons, or rewrites. Remove emoji not part of established app conventions. Code comments included where trivially adjacent; do not churn files solely for comments.

ITEM 2.4 Chat mic-grant fix verification and completion: the known web chat bug (first-time user cannot type; recording auto-starts the moment mic permission is granted, without an explicit record press). A fix prompt was drafted previously (grant-never-starts-recording, explicit record action only, typing available regardless of mic state, chat surface only). Recent test work touched chat-mic tests, so FIRST verify current behavior: if already fixed, report evidence and skip; if not, implement per those requirements. Also audit practice.tsx for the same unobserved-release hole (tech debt item 6, folded here since you are in these files): report findings, fix only if the hole exists, matching the chat pattern.

ITEM 2.5 Stats-banner tap: the home stats banner is currently inert on tap; make it navigate to the Progress view with a pressed state, back navigation preserved, no progress writes.

ITEM 2.6 Paywall copy check (new, from tonight's testing): review the paywall/upsell copy shown when a free user hits a locked-language gate (including via test-out entry). The copy must read as a content/language unlock (All-Access or language purchase), never as paying for attempts or paying to test out. Report current copy; fix only if it misframes the model.

PHASE 2 GATE: typecheck, boot smokes 2/2, full web suite green at its then-current count plus any new pins (2.4 and 2.5 need pins minimum), api canonical unchanged. Report every copy change by surface, WAIT for confirmation before Phase 3.

==================================================
PHASE 3: Tech debt remainder (items 2 through 7; item 1 tripwire already landed as 1af4082)
==================================================
ITEM 3.1 /choose-language route disposition: the route survives navigable with tests but nothing links to it. Decision rule: fold into settings as a change-language view IF an existing settings surface can link it with a one-line addition; otherwise delete the route and its tests. Report which applies, then act. Web side here; note the mobile twin for the mobile brief.
ITEM 3.2 Splash mascot sizing: reduce from 78 percent frame height to a smaller treatment; propose the value from the canonical mascot rule and existing splash layout, web side (note mobile twin).
ITEM 3.3 Daily-limit client dead code: strip client-side daily lesson limit dead code and spec fields (server-side removal tracked in the monetization spec item (g), untouched here). Grep lesson_generations and daily_lesson_limit in client code and tests before deleting; report what remains server-side.
ITEM 3.4 /contact rate limiter: migrate from in-memory to the DB-backed pattern used elsewhere; same limits, same response shape; add a test for limit enforcement surviving a server restart. This item touches the api-server contact route and is the batch's one sanctioned server change.
ITEM 3.5 Web test typecheck coverage: web tsconfig excludes src/test/** from typecheck, which hid a committed syntax scramble for weeks. Include test files in the canonical typecheck or add a dedicated tsc pass over src/test/** to the test command. Report the compile-error fallout count first if nonzero; mechanical fixes in scope, anything structural reported instead of fixed.
ITEM 3.6 Gitignore hardening (from tonight): ensure memory-note paths and attached_assets pasted-brief patterns are ignored and untracked (may already be done via the earlier one-line commit; verify and complete).

PHASE 3 GATE: typecheck (now including test files per 3.5), boot smokes 2/2, full web suite green, api canonical green except the documented pilot-capture fence (the 3.4 restart-survival test adds to the count; state the new expected total). Report by name.

==================================================
CLOSING
==================================================
After Phase 3's gate: post the final lineage (git log --oneline covering all batch commits), the final suite counts for both projects (these become the baseline the mobile briefs verify against), and an updated CODEBASE-FACTS dated section summarizing the batch. Commit per phase (one commit per phase is acceptable; more granular is fine). No markTaskComplete or apply machinery; this pane commits directly per tonight's established pattern. WAIT after the closing report; the mobile briefs follow from here.