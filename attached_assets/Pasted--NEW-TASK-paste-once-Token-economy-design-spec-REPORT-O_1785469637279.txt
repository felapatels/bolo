→ NEW TASK — paste once

Token economy design spec. REPORT ONLY — no production code changes, no schema migrations, no RevenueCat configuration changes, no new dependencies. Deliverable is a design document at docs/specs/token-economy.md plus findings. Consult docs/CODEBASE-FACTS.md and agent memory. Implementation is explicitly deferred to build 32+ after build 31 is approved; this task prepares everything so implementation can start immediately then.

Approved plan (design within these decisions, do not relitigate them): consumable Token currency ("train/fare tokens"), earned via streaks, zone completions, Express stamps (test-out passes), and Bolo Quiz scores; purchasable via RevenueCat consumables in 3-4 tiers. Spends, ranked: (1) Station Pause — streak freeze, auto-equip up to 2; (2) Express Multiplier — 15-30 min 2x XP window, offered at result card and zone-complete; (3) test-out retry attempts — first test-out per stop stays free forever; (4) Streak Repair within 48h, priced high; (5) cosmetics parked. HARD RULES: no hearts, no limited wrong tries, never monetize failed attempts; paywalls gate content, never attempts. All-Access keeps all content and additionally gets a monthly Token allowance and a permanent small XP bonus. Slice for 32: Token ledger + Station Pause + Express Multiplier. Slice for 33: test-out retries + Streak Repair.

Deliverables:

Ledger design: a server-side Token ledger spec fitted to our actual stack (schema tables with audit rows per earn/spend, idempotency for purchase credits, balance derivation, concurrency/atomicity for spend races) — modeled on how our existing XP and attempts persistence work; cite the real files/patterns being followed. Include the earn-rule table: exact Token amounts per source (propose numbers with reasoning against our current XP magnitudes and streak mechanics — read the real streak implementation and report how Station Pause must hook it).
Economy tuning proposal: earn rates vs spend prices such that a daily free user earns a Station Pause every ~5-7 days organically; Express Multiplier priced to be an occasional treat, not a constant; Streak Repair priced at roughly 3-4x a Station Pause. Show the math. Flag anywhere our current XP/streak values make the proposed numbers awkward.
RevenueCat + App Store groundwork: what configuring consumables requires in our RevenueCat setup (products, entitlements vs non-entitlement consumables, server webhook handling for consumable grants — read our existing REVENUECAT webhook handling and report what changes), proposed tier SKUs and price points, and the App Store review considerations for consumable IAP (what reviewers check, restore-purchases expectations for consumables).
Surface map: where Tokens appear in the product — balance display, earn moments (toasts at streak/zone/Express/quiz), spend moments (streak-break intercept for Station Pause, result-card/zone-complete offer for Multiplier), and the All-Access allowance presentation — mapped to the actual screens/components by file path, with the constraint that no new competing CTA appears on home (the boarding pass remains sole progression CTA; Token UI lives in the stats/streak surfaces and moment-of-need dialogs).
Daily-lesson-limit recommendation: the open question — kill the daily lesson limit when Tokens ship (leaning yes) — argue it concretely against our current limit implementation and its client dead-code debt note; recommend and state the removal's blast radius.
Build-32 task breakdown: the spec sliced into fenced, orderable implementation tasks with file ownership per task, sized, honoring the 32/33 slicing.

Budget discipline: this is reading, design, and writing — no API spend expected; if any experiment seems needed, describe it instead of running it. Report: the spec document plus a summary of findings where reality diverged from the approved plan.