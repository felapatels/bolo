→ NEW TASK — paste once

Five-band scoring display, web AND mobile. Consult docs/CODEBASE-FACTS.md and agent memory (band-derivation, Spec 1 rule 16 nocatch rules) before any code.

Step 0 first, report before building: (1) where band derivation lives today (server or client, file paths) and every consumer of the current three-band values (nailed/close/retry) plus nocatch across api-server, mobile, and web — result cards, review, progress surfaces, analytics events, tests; (2) whether band names/thresholds are centralized or scattered; (3) the exact score field the bands derive from. Then build.

Scope:

Five bands replace three. New ladder top to bottom: Perfect, Great, Good, Almost, Try again. Server-side derivation from the same score field, thresholds defined in ONE config block (provisional, evenly spaced across the current passing range, marked TUNING PENDING in a comment; boundaries will be calibrated in a later pass). Nocatch remains a distinct non-band outcome, unchanged semantics.
Band ladder on the result card, web and mobile. After a scored attempt, the card shows all five band labels as a vertical or horizontal scale with the achieved band clearly highlighted (filled/colored vs muted others), so the user always sees where they landed relative to the top. Requirements: the ladder never shows raw numeric scores (band labels only, consistent with the ex-#874 rule); nocatch never shows the ladder (keeps its current neutral card); the retry-band CTA hierarchy just shipped (Try again primary, Next secondary) is preserved; higher bands keep their existing celebration behaviors mapped sensibly (confetti stays top-band only).
Migration of existing consumers. Map old bands to new everywhere they're consumed: any stored attempt band values, review surfaces, progress/mastery logic (mastered threshold must be explicitly mapped — report what today's >= 80 mastery rule becomes in five-band terms and do NOT change mastery behavior in this task), analytics event values, and the flash-color logic (both practice and review copies; do not extract the shared helper here, that's separate debt). Enumerate every consumer touched.
Copy and color. Band colors from existing brand tokens forming a clear gradient from top to bottom band; retry band keeps its current treatment. No em dashes anywhere.

Constraints: no changes to scoring math, Elo, FSRS, XP amounts, or mastery thresholds — bands are a display layer over the same score. Tests: derivation boundaries (a score at each threshold edge), ladder renders five labels with correct highlight per band on both platforms, nocatch shows no ladder, no raw numeric score in the rendered card, mastery behavior unchanged. Enumerate test counts before and after on all three suites (api-server baseline is the documented 14-failure set). Report per item with file paths and web-verified / jest-verified / deferred-to-device status.