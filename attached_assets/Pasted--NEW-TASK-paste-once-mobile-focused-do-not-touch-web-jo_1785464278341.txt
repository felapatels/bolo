→ NEW TASK — paste once — mobile-focused; do not touch web journey.tsx or home.tsx (other tasks own them tonight)

Build 31 mobile parity batch. Consult docs/CODEBASE-FACTS.md and agent memory (motion engine rules, rn-svg-percentage-height, the ticket sizing contract, chat barge-in implementation, web reference implementations from tasks #899/#901/#902/#907 and the train/station art task). Web is the reference; port behavior and energy, respect native constraints. CRITICAL standing rule: Expo-web verification is NEVER sufficient for native layout or animation — jest what's assertable, and enumerate every item's deferred-to-device status for the build-31 device pass.

Boarding pass energy (from web #899 family): idle breathe, shimmer, train drive-with-settle, arrow motion, glow pulse, press spring, progress-aware CTA copy (Start your journey / Resume at Stop N), and the tear-off on tap before navigating to journey (same never-blocks rule: navigation proceeds regardless of animation failure; reduced-motion instant). Reuse the timing constants' values from web; implement with reanimated/native-appropriate primitives. The ticket sizing contract holds: no percentage-sized SVG in flow, maxHeight belts stay.
Train + station art: port the redrawn colorful train SVG (wheels, steam groups) to the mobile ticket and journey rail marker, and the station signboard card style (top line-color bar, station glyph, warm surface, progress track, quiet locked variant). Verify the stamp/name geometry contract still holds after art changes.
Rail pulse: directional pulse on the active segment toward the next stop, matching the web task's chosen approach; same edge cases (no next stop = no pulse; locked next stop still pulses).
#890 mascot barge-in (chat): the on-screen chat mascot currently stays disabled during playback; make holding Bolo himself interrupt playback and record, same contract as the nav bird (stop playback, orphan the in-flight turn, record; positive hold guard).
#913 practice barge-in parity: port #907's web behavior — a hold at any moment (example audio, feedback audio, post-retry) stops audio and records; never a fake listening state.
#914: review screen renders the romanized "We heard" line per #907's server field, same hiding rules.
Hold-to-talk stability: the circular record button on practice/lessons must not shift under the finger when recording state renders — touch frame identical across idle/recording/evaluating; recording-state UI renders in reserved space or expands away from the anchor. Frame-stability layout assertion across states; check review's control if shared.
#892: bolo-quiz screen-level gate gains the isLoading fail-closed check (deep-link paywall bounce).

Ownership: bolo-mobile only, plus shared components where the art lives; do not touch api-server. If any item requires touching a file another active task owns, stop and report. Enumerate test counts before and after. Report per item with jest-verified vs deferred-to-device, and end with the consolidated device checklist for build 31.