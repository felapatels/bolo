MODE: POWER

TASK: Splash v2 rebuild: layered motion boot with ready-signal hold. One commit to main. Main HEAD at start: b8d32c5. This is a clean rebuild of a design whose original commit was lost; this paste is the sole authority.

HARD RULE: ZERO git commits before the owner's approval message arrives in this pane. ONE report, then STOP. If any commit exists that you did not make after approval, stop and report it. No task cards; bank suggestions as report lines.

SESSION RULES (binding):
1. You are the only writer; work on main directly.
2. Expected scope: the app-layer splash/boot surfaces in artifacts/gujarati-coach, bolo-mobile splash surfaces ONLY as Step 0 shows they exist (Expo splash config changes allowed for item 4), plus docs/CODEBASE-FACTS.md. Do NOT touch the api server or lib/. Anything beyond goes under DEVIATIONS with rationale.
3. No em dashes anywhere. attached_assets never staged. No new test files unless an existing file already asserts a changed surface. Zero generated strays before the report.
4. Routes are truth, comments are hints. Expo-web never proves native; say so wherever a native behavior is asserted but unverifiable here.

STEP 0 (verify, fold into report, misses hard-stop):
A. Locate and quote the current splash implementation(s): web boot splash component and, if present, the mobile in-app splash. Include current mascot sizing (expected: about 78% frame height).
B. Quote the current boot/readiness flow: what the app waits on before showing content (fonts, auth, initial queries) and how the splash is currently dismissed (timer, event, or first render).
C. Quote the proven asset-import pattern used for chachaji-wallet-vignette.png (how art enters the app bundle) and the canonical Bolo mascot asset path.
D. Quote the Expo splash configuration (app.json or equivalent): background color, image, resize mode.
E. Enumerate any test files asserting splash or boot behavior.

DESIGN (build to this):

ITEM 1, asset map: one exported SPLASH_V2_ASSETS map, paths only, five slots: carriage (with empty window), boloWingRaised, boloWingLowered, steamPuffA, steamPuffB. Wire the composition exclusively through this map so the owner can later swap in final art by editing paths only, zero code changes. Populate now with placeholders derived from existing bundled art (canonical mascot plus simple shapes are fine); do not create AI art, do not stage anything from attached_assets.

ITEM 2, layered composition: carriage layer with an empty window; Bolo composited into the window by the app layer (canon: Bolo is never baked into scenes); wing raised and wing lowered crops alternating in a gentle flap loop; two steam puffs rising on offset loops. Calm, boot-appropriate motion. Bolo's size follows the composition, which supersedes the old 78% treatment (banked debt item, resolved by design here). Reduced motion: render the fully composed static frame, no animation, never a blank screen.

ITEM 3, ready-signal hold: the splash holds until the app's real readiness signals settle (whatever Step 0 B found), then releases. No fixed-duration timer as the primary mechanism. Add a max-hold failsafe (release after a hard cap, about 8 seconds) so a stuck signal can never trap the user. The release transition must be clean, no blank flash between splash and first screen.

ITEM 4, boot-gap fix (both platforms; the owner saw a gap and is not sure where): on web, the document/body first-paint background must match the splash background exactly so no white flash precedes the splash; on native, align the Expo splash config (background color, image, resize mode) with the motion splash's first frame so the native-to-app handoff is seamless. Native behavior cannot be proven here; implement per config and state plainly that device verification rides the row 8 parity pass.

GATE (lean during, one full run at the end):
1. Typecheck all touched workspaces; mandatory.
2. Grep-first: run only the Step 0 E test files during development, enumerated.
3. At the end: ONE full web suite run, baseline 413/58; if any bolo-mobile files changed, also the mobile suite, baseline 17/17. Report runner lines verbatim, any delta enumerated by file. Any new failure: report, do not commit.

REPORT (one message, docx or screenshots, then STOP for approval before any commit):
1. Step 0 A through E with quoted blocks.
2. The boot sequence as built, frame by frame, five lines or fewer.
3. Files changed, one line each.
4. DEVIATIONS or "none".
5. User-visible strings added or changed (expected: none; confirm).
6. Ready-signal sources used and the failsafe cap value.
7. Gate results with runner lines.
8. git status and the exact staged list; zero attached_assets, zero strays.
9. Proposed commit message: "Splash v2: layered motion boot with ready-signal hold"
END OF REPORT is the report's final line. After approval only: commit, confirm with git log -1 --decorate and git show --stat HEAD, DEVIATIONS or "none", END OF REPORT sentinel, stop.

END OF TASK. If this line is not the last line you received, the paste truncated; stop and say so.