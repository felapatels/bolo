→ NEW TASK — paste once — do not start until the five-band task has merged (band names must be final first)

Feedback voice latency: speak the band instantly, stream the rest. Web and mobile share the server-side work; client wiring per platform. Consult docs/CODEBASE-FACTS.md and agent memory (tts cache, warm greetings pattern, squawk SFX pattern) before any code.

Step 0 first, report before building: (1) trace today's spoken-feedback path on practice from eval response to first audio: what text is synthesized (feedback sentence? band? both), which endpoint, where the latency comes from (synthesis time vs round trip vs playback setup), and any existing caching; (2) what language the feedback voice speaks (English vs target language) since that determines the band-clip set size; (3) how the warm-greetings pre-warm and the ttsCache dedupe work, as candidate mechanisms; (4) whether the spoken-feedback mute setting gates this path. Then build.

Scope:

Instant band audio. The five band names (Perfect, Great, Good, Almost, Try again) plus the nocatch line get pre-available audio so playback starts effectively instantly when the result lands. Choose mechanism from Step 0 findings: server pre-warmed ttsCache entries per voice/language (warm-greetings pattern) fetched-and-cached client-side on practice mount, or bundled static clips like the squawks if the set is small and stable. Report the choice and why. Nocatch keeps its neutral tone rule: its clip must sound neutral, not disappointed.
Full feedback follows. The detailed feedback sentence synthesizes as today and plays immediately after the band clip (or overlapping its tail if the audio pipeline supports clean sequencing). Net effect: voice starts at result-render time instead of after synthesis latency. If the full feedback fails or is slow beyond a timeout, the band clip alone is acceptable; never block the result card on audio.
Kick synthesis earlier if cheap. Step 0 should report whether the server can begin feedback TTS at eval time (fire-and-forget into ttsCache when the eval response is produced) so the client's fetch hits warm cache; implement if it's a contained change, otherwise report as follow-up.
Respect the existing spoken-feedback mute on both platforms; band clip and feedback both gated.

Constraints: no changes to scoring, bands, or result card layout (five-band task owns that and has merged before this starts). Tests: band clip triggers on result with correct band mapping, nocatch clip neutral-path, mute suppresses all of it, feedback failure still shows results normally. Enumerate test counts before and after, all three suites. Report per item with file paths and platform verification status; audio timing feel is deferred-to-device for mobile, live-browser for web.