→ NEW TASK — paste once

Server fix: free-taste teaser denied on the journey path. api-server only; do not touch web or mobile clients. Consult docs/CODEBASE-FACTS.md. Step 0 trace is complete; findings below are verified, build directly from them.

Verified defect: for a free user in teaser state on a locked language, the journey listing (GET /categories/:id/lesson-groups/:lang) marks the taste station unlocked with teaserStation true, but GET /lesson-groups/:id/phrases applies a blanket denyLockedLanguage with no teaser exception (~learning.ts L2020), so the advertised station 402s into the paywall. The category-phrases route (L442-473) already implements the correct teaser branch, serving exactly the teaser phrase set.

Add a teaser exception to the group-phrases route consistent with the existing teaser contract: when the caller is in teaser state for the group's language AND the requested group is the designated taste group (the one the listing marks teaserStation), serve only the teaser phrase rows (same set the category-phrases teaser branch serves), instead of 402.
CRITICAL constraint from memory: showroom/teaser callers must never reach the unlock-latch machinery — short-circuit before getUnlockedGroupIds/deriveAndLatchUnlock so no lesson_group_progress rows are written for an unowned language. Assert this in a test.
Non-taste groups on a locked language still 402 exactly as today; teaser-exhausted users (3 distinct teaser phrases attempted) follow whatever the existing teaser-exhaustion behavior is on the category route — mirror it, don't invent new behavior.
Tests: teaser caller gets the teaser phrases from the taste group (not 402), teaser caller still 402s on a non-taste group, no progress-latch rows written for the teaser call, entitled user behavior unchanged. Confirm POST /attempts already accepts the teaser phrases from this flow (the trace says the id-aware exception exists; verify with a test rather than assuming).

Enumerate test counts before and after; api-server baseline is the documented 14-failure #782 set, no new failures. No em dashes. Report with file paths.