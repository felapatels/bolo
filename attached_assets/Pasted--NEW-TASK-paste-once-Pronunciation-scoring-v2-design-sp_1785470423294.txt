→ NEW TASK — paste once

Pronunciation scoring v2 design spec: gpt-audio ensemble hybrid. REPORT ONLY — no production code changes, no dependencies, no schema changes; deliverable is docs/specs/pronunciation-scoring-v2.md plus findings. Consult docs/CODEBASE-FACTS.md and agent memory, especially the scoring investigation findings (transcript-only signal, the >=0.90 similarity floor, the gpt-audio sandbox results with 3-run medians, gpt-audio-mini disqualification, logprobs dead end). Implementation is build 32+, gated on build 31 beta approval AND on the real-voice pilot passing.

Decided architecture (design within it, do not relitigate): hybrid. The STT transcript keeps its current jobs — pass/fail gate, wrong-phrase cap, script-mismatch nocatch, the "We heard" raw + romanized display. What changes: for supported languages, the numeric score feeding the band ladder comes from an ensemble of 3 parallel gpt-audio grading calls that receive the actual audio clip plus the target phrase, taking the median. Languages not yet validated keep the current transcript-derived score. The client contract is untouched: five bands, transcript fields, nocatch semantics, scoreBands.ts thresholds.

Spec sections:

Pilot gate design (#928 integration). Define the real-voice pilot that must pass before rollout: recording protocol (same phrases spoken well and deliberately badly by real speakers — Aakesh, family; per-language sets starting hi and gu), sample sizes, and the acceptance criterion (proposed: median ensemble grades must separate good from bad recordings with a stated margin, and run-to-run variance within a stated band; propose concrete numbers with reasoning). Include how pilot clips are stored and how the pilot harness reuses the /tmp sandbox scripts' approach, productionized into a repeatable qa/ script.
Grading call design. The prompt/rubric for the audio grader (grade delivery of the target phrase 0-100 with the rubric aligned to our five-band semantics; JSON prompt-enforced since response_format is rejected for this model), the 3-call parallel ensemble with median selection, timeout and partial-failure rules (2 of 3 returned = median of 2; 1 or 0 = fall back to transcript score for that attempt, never block the result), and where this slots in routes/openai.ts relative to the existing eval — including whether the audio call replaces the gpt-5.4-mini text judge for graded languages (recommended: yes, it also produces the feedback text; spec both the combined and separate variants and recommend one with latency/cost numbers).
Score integration. Exactly how the ensemble grade composes with the existing guards: wrong-phrase cap and script nocatch still fire off the transcript FIRST (an audio grade never rescues wrong words); the similarity floor at lib/pronunciationGuards.ts is bypassed or retained per language flag; band derivation reads the new score unchanged. Quote the real code seams by file and line.
Language rollout table. Per-language flag mechanism (config, defaulting to transcript scoring), initial cohort hi + gu after pilot, criteria for promoting each additional language, and the explicit list of low-resource languages expected to stay transcript-scored indefinitely per the C1 precedent.
Cost, latency, and budget controls. Recalculate per-attempt cost for the recommended variant (ensemble replacing vs stacking on the text judge), monthly projections at 1K/10K/100K attempts, a per-user or per-day server-side grading budget guard (degrade to transcript scoring past it, never error), and the cross-reference to the Chai economy spec's revenue model (both specs must cite each other's numbers).
Failure and abuse posture. Provider outage degrades to transcript scoring silently; grading never produces nocatch on its own; define logging (store ensemble raw grades + median on the attempt row or a side table for later calibration — spec the minimal schema addition as a build-32 migration, listed but not executed); note Azure hi/ta as a future calibration anchor, not a dependency.
Build-32 task breakdown. Ordered, file-owned, sized implementation tasks, with the pilot task (#928) first and everything else gated on its pass.

Findings section: anywhere the codebase diverges from the investigation's description, and any open questions requiring Aakesh's decision, listed explicitly. No em dashes anywhere in the document.