TASK: Coach speaks the English meaning. Product ruling: on web practice, every coach play of the target-language phrase is followed by the English meaning, spoken in an English voice (example: "Namaste", short pause, "means Hello"). An on-page toggle controls it, default ON, persisted per device. Practice surface only; chat is untouched. One gate at the end, one commit.

STEP 0, REPORT FIRST, DO NOT EDIT YET. Report in a numbered list:

Where coach audio is synthesized (endpoint, file, voice selection per language) and whether/how it is cached (per phrase key, storage, TTL).
Where the English translation text for a phrase lives in the data the practice page already has, and confirm it is present for all phrase types (words, sentences). If any phrases lack an English translation field, report the count and how they are identifiable.
The current playCoach flow in practice.tsx post-Chunk 1 (synthesis-failure card, autoplay-rejection tap-to-hear split), and where a second sequential segment would chain (on ended of the first audio element).
Whether the English meaning segment can be synthesized through the existing TTS path with an English voice, and whether it can be cached under its own key so repeated plays and common phrases do not re-synthesize.
Proposed placement for the on-page toggle near the existing speaker/coach controls, matching existing control styling, with your suggested label.
Then STOP and wait for my confirmation before building.

DESIGN RULES, apply after my confirmation:

Two separate audio segments played sequentially, never one mixed-language synthesis: the existing phrase clip untouched and cache-intact, then roughly a 400ms pause, then the English segment. The English segment's spoken text is: "means" followed by the phrase's English translation. If the translation is itself a sentence, drop the word "means" and speak the translation alone.
The English segment is synthesized with an English TTS voice through the existing TTS path and cached under its own key. A synthesis or playback failure of the English segment fails silently to phrase-only behavior: it must never block, delay, or error the phrase play, and never triggers the synthesis-failure card.
The toggle: on-page, near the coach/speaker controls, default ON, persisted per device (localStorage, consistent with the iOS hint key pattern). When OFF, behavior is exactly today's phrase-only play. The toggle state applies immediately to the next play, no reload.
Interaction with Chunk 1 audio work: the English segment chains after the phrase segment's ended event, so it inherits the user-gesture blessing of the play that started it; do not call primeAudioUnlock again for it. If the phrase play lands in the tap-to-hear affordance path, the English segment simply follows whenever the user taps to hear.
Recording and scoring are untouched: the meaning segment must be fully stopped/cleaned up if the user starts a hold-to-record while it is playing, same as any coach audio interruption today, and barge-in behavior follows the existing pattern.
No autoplay of the meaning alone, no captions/text changes, no chat changes, no mobile changes (mobile parity is a later pass, note it in the done report).

GATE, ONE, AT THE END: typecheck clean; full web suite green against current baseline (391/55 plus whatever Stage A added, report the count); targeted tests for the toggle persistence and the fail-silent English segment path; manual browser verification of one phrase playing "phrase, pause, means X", the toggle turning it off immediately, and barge-in during the meaning segment behaving cleanly. Then STOP, report done with the toggle label and placement as built, wait for my confirmation, and commit ONCE with message: "Practice coach speaks English meaning after phrase, on-page toggle". No suggested or draft task cards. No em dashes anywhere.