RELEASE: Task #922, you are next in the queue. Current main includes 4988916 plus #947's baseline retirement.

Step 1, verification before any edit: run the FULL web suite (pnpm --filter @workspace/gujarati-coach run test) and the api-server suite on clean current main, canonical flags, and post the failure list by name. This verifies or refutes #947's claim that 17 web failures pre-exist on the base. Do not fix anything yet; this is the baseline measurement for your slot.

Step 2, reconstruction pattern, not merge: your old branch and checkout are stale (your earlier zero-diff verification was against a ghost commit that never reached main, and your restored 0034_familiar_starhawk files conflict with main's migration fix). Create a fresh branch from current main. Enumerate your legitimate deltas and re-apply by intent, not paste. Your restored 0034 files are DROPPED; main's 0033-snapshot fix is canonical.

Step 3, your scope (consolidated):
a. Shared @workspace/db mock factory: one canonical mock object that all tests consume, so schema additions update one place. Update the language-hint test to consume it.
b. Close the known namedExports gaps: dailyQuizCompletionsTable, dailyQuizzesTable, lessonGroupProgressTable, lessonGroupTestoutsTable, phraseReportsTable, scriptTraceProgressTable, userItemMemoryTable; remove the phantom phraseScheduleTable.
c. Fix Group A (freePhraseReplenish x3): stale mock is layer one; layer two is the FK cleanup gap, after() deletes phrases while user_item_memory rows still reference them. Fix cleanup ordering so it is idempotent against any starting DB state.
d. Your original scope: api-client-mock.ts hook stubs (useGetScenario, useListZoneStamps, QueryKey helpers) and the shared web test helper consolidation. These never reached main; they are live scope.
e. Your approved boilerplate cleanup proposal remains approved, AFTER a through d are green.

Out of scope, hard fences: showroom/teaser tests (Group B, owned by an upcoming task), openai.ts and all production route files, migrations and drizzle meta, the 3 TTS cache tests (now passing, do not touch).

Gate: typecheck clean, both boot smoke tests pass, full canonical suite, report remaining failures BY NAME with classification against your Step 1 baseline. Expected: Group A cleared, Group B still failing (not yours), web failures unchanged or explained. Report and WAIT for confirmation before any apply or markTaskComplete.