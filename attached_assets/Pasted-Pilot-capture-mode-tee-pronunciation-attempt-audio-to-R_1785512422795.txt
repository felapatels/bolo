Pilot capture mode: tee pronunciation attempt audio to R2 for allow-listed users. api-server only, small and temporary-by-design. Consult docs/CODEBASE-FACTS.md, agent memory, docs/specs/voice-data-program.md section 5 (this is a narrow precursor of that design, reusing its decisions).

Purpose: the pronunciation v2 pilot needs clean per-attempt audio from the REAL production capture path. Screen-recording approaches are structurally contaminated (iOS mixes app audio into the recording digitally). Solution: capture server-side.

Env var PILOT_CAPTURE_USER_IDS (comma-separated user ids, empty default = feature fully inert). For attempts by listed users only: after scoring completes in the pronunciation eval route, upload the received audio bytes to R2 at pilot-clips/{languageCode}/{attemptId}.m4a using the provisioned credentials (R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME — present in workspace and deployment secrets), and append a manifest row (JSON lines in R2 alongside, or a simple table if cheaper) recording attemptId, userId, languageCode, phraseId, target text, transcript, raw numeric score, timestamp. Do NOT record or depend on band values anywhere in this feature — the five-band mapping is a display layer with provisional thresholds and must not enter the pilot's data; raw scores only.
Failure posture identical to the voice-program spec: the tee is try/caught and never affects the eval response. Uses @aws-sdk/client-s3 with the R2 endpoint (build the client as a small reusable lib module, e.g. artifacts/api-server/src/lib/r2Client.ts, so build 32 inherits it).
Adapt the pilot harness: a new mode of qa/pilot-pronunciation-v2.mjs that, instead of video segmentation, lists pilot-clips/{lang}/ from R2, pulls clips + manifest, and runs the existing MP3 ensemble + the four acceptance criteria directly — per-attempt files, zero segmentation, ground truth from the manifest (phraseId + target known per clip; attempt type supplied by the fixed-order convention or a session note). All harness outputs report raw scores and criterion math only — no proposed-band column. Keep the video mode intact.
Tests: unlisted user attempts produce zero R2 calls (pin this — the inert default is the safety property), listed user upload path exercised with the S3 client mocked, tee failure does not affect the eval response. Enumerate test counts before/after (gate per the regime task #947 leaves — check CODEBASE-FACTS). No em dashes. Report with file paths, and state the exact env var value format so Aakesh can add his user id.