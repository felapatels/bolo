MINI-BRIEF: Replace synthesized tear SFX with recorded asset (web)

CONTEXT: The Web Audio synthesized burst in src/lib/tearAudio.ts reads as a cymbal hit, not a paper tear. A real recorded paper tear replaces the synthesis. The module's exported interface (preloadTearAudio, playTearSfx), the wiring in home.tsx, the silent-failure contract, the fire-and-forget guarantee, and both test pins are UNCHANGED; only the module internals change. Attached asset: tear-sfx.m4a (0.985s, mono, 44.1kHz, loudness-normalized with -1dB true peak, 13KB). A tear-sfx.mp3 fallback exists if m4a decode proves unreliable; prefer the m4a.

SCOPE:
1. Bundle tear-sfx.m4a in the web app assets.
2. preloadTearAudio(): keep the suspended-context pre-warm; ADD fetch + decodeAudioData of the asset into a module-cached AudioBuffer at mount, so first play has zero fetch or decode lag. All failures silent.
3. playTearSfx(): play the cached buffer via AudioBufferSourceNode through a GainNode. Starting gain 0.55; the asset is normalized louder than the old synthesis output, so tune by ear on dev toward subtle-not-startling and report the final value as a named constant. If the buffer is not ready (preload failed or never ran), fall back to SILENCE, never to a fetch-then-play that would lag the tap.
4. Keep: fire-and-forget, never throws, never delays animation or navigation, warm-context reuse, fresh-context self-close behavior for the no-preload path where applicable.
5. Delete the noise-synthesis code path entirely once the buffer path works; do not leave a dead synthesis fallback.
6. Tests: the existing 2 pins pass unchanged (module is mocked in home-boarding-pass.test.tsx). Add one cheap module-level test only if trivially possible in jsdom (decode is not; skip rather than mock-heavy).

GATE: typecheck clean, both boot smokes pass, web suite green at its then-current baseline, api canonical unchanged. Manual on dev: tap the boarding pass; the sound reads as a real paper tear, onset synced to the tear start, subtle at normal volume. Report the final gain constant and WAIT before commit.