MODE: POWER

TASK: Prod hotfix batch, 6 items. One commit to main.

HARD RULE: ZERO git commits before the owner's approval message arrives in this pane. Working tree stays UNSTAGED until approval. ONE report, then STOP. No task cards; bank suggestions as report lines.

SESSION RULES: scope = artifacts/gujarati-coach plus docs/CODEBASE-FACTS.md only; no api server, no lib/, no bolo-mobile. No em dashes anywhere. attached_assets never staged. No new test files unless a genuinely new surface demands one (justify). If any full-suite run fails, capture failure identity BEFORE any re-run. No browser probes. Divergence goes under DEVIATIONS.

STEP 0 (verify, quote, misses hard-stop):
A. git log -1 --decorate. Acceptable HEAD: c7825a6, or exactly one "Published your App" commit atop c7825a6. Anything else: STOP and report.
B. Root-cause the dead taps: quote how journey.tsx renders the signal and signpost buttons over the scenery layer, including every pointer-events and z-index class in that stack. State in one sentence why taps die in the built app.
C. Quote factForZone's fallback path when no region/line tag matches, showing why every zone can land on the same fact.
D. Quote Signal Lights' round render and any audio control present, and the house pattern other games use to play phrase audio.
E. US-English sweep: grep user-visible strings for non-US spellings (practis, colour, flavour, favourite, organise, recognise, analyse, travelled, cancelled, grey, and kin). Enumerate every user-visible hit; enumerate code/comment hits separately (do not fix those).
F. Splash no-show diagnostics: quote the play-once latch storage mechanism, every bootQualifies condition, and statically verify all five SPLASH_V2_ASSETS URLs plus the two public/splash steam files resolve under the production base path in the built output.

ITEM 1, revive the taps: fix the Step 0 B root cause so signal and signpost buttons are tappable; minimum 44px hit targets.
ITEM 2, signal visibility: active signals become visually unmissable (amber/red active treatment, green cleared, subtle attention pulse suppressed under reduced motion).
ITEM 3, soft stop (owner-ruled): when the learner reaches an unvisited, unwaved signal (the stop before it is complete), the encounter dialog auto-opens, once per signal per session. Wave-through remains one tap and session-remembered. Cleared or waved signals never auto-open. Under-floor auto-wave quip unchanged. Never auto-open over test-out, capture, or an open dialog.
ITEM 4, fact variety: the fallback fact selection incorporates the zone index so distinct zones show distinct facts on the same day. Tagged-preference behavior unchanged.
ITEM 5, Signal Lights audio: play the round phrase's audio via the Step 0 D house pattern, honoring the game's audio toggle; if the existing toggle is dead code, wire it for real. Timing must not shorten the answer window.
ITEM 6, US English (owner-ruled, app-wide): "Keep Practising!" becomes "Keep Practicing!". Fix every user-visible Step 0 E hit if 15 or fewer; if more, fix the game/journey/wallet surfaces and bank the rest enumerated. Record in docs/CODEBASE-FACTS.md: all user-facing copy is US English until localization.
ITEM 7, conditional: ONLY if Step 0 F finds a real defect (bad path, unreachable asset, latch or qualify bug), fix it minimally and label it. If no defect, state "splash: no code defect found" and leave it untouched.

GATE: typecheck; run only test files asserting touched surfaces during development (enumerate); ONE full web suite at the end, baseline 450/62, runner line verbatim, delta by file. Failure = identity first, report, no commit.

REPORT (one message, then STOP for approval): Step 0 A-F quotes; per-item summary (2 lines max each); every user-visible string changed, before/after verbatim; files changed one line each; DEVIATIONS or none; gate results; git status with the exact proposed (unstaged) list; proposed commit message: "Prod hotfix: signal taps, soft stop, fact variety, game audio, US English"
END OF REPORT is the final line. After approval only: stage, commit, confirm with git log -1 --decorate and git show --stat HEAD, DEVIATIONS or none, END OF REPORT, stop.

END OF TASK. If this line is not the last line you received, the paste truncated; stop and say so.