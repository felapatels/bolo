REPASTE: remainder of Chunk 4B, continuing exactly from ITEM 3's cut sentence. Everything from your intact copy through "...the affordance is dormant by construction," stands unchanged; the task below completes it.

ITEM 3 (continued): ...it activates when CROSS_ZONE_GATE_ENABLED flips.) Label: "Test out of this zone". Tapping navigates identically to item 2.
If instead every group is planLocked, do not launch the flow; render the existing upsell affordance/state the postcard already uses for plan-locked content, copy unchanged (the server 402 remains the backstop authority).

GATE (lean tier):
1. Typecheck the web workspace. Mandatory, and the only mandatory check.
2. Grep-first: grep gujarati-coach test files for literals this task touches (the progression-lock dialog's existing strings, "ZonePostcard", "testout", "scope"). If zero test files assert changed behavior or strings, run no tests and say so. If any do, update those assertions and run ONLY those files, enumerated.
3. No tests for navigation feel, animation, or copy rendering; that is the owner's eyeball checklist.

REPORT (one message, then STOP for approval before any commit):
1. Step 0 A-D confirmations: file paths, exact generated-client function names and signatures, quoted query keys.
2. Files changed, enumerated, one line each.
3. DEVIATIONS heading: each divergence with rationale, or the single word "none".
4. Exact user-visible strings added, verbatim.
5. Invalidation implementation: the exact keys/prefixes invalidated on pass.
6. Gate results: typecheck summary; grep findings and any test files run with counts.
7. git status and the exact staged file list including docs/CODEBASE-FACTS.md per the standing end-of-task rule; confirm zero attached_assets and zero files outside artifacts/gujarati-coach plus the facts doc.
8. Proposed commit message: "Chunk 4B: zone test-out web entry + verdict"
After approval only: commit, report the hash, stop. No task cards, no em dashes.

END OF TASK. If this line is not the last line you received, the paste truncated again; stop and say so.