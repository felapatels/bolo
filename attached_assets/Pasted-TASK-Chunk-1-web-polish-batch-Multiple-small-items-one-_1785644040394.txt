TASK: Chunk 1, web polish batch. Multiple small items, one gate at the end, one commit. Scope is web only unless an item explicitly says otherwise. Do not touch scoring, capture mode, the calibration harness, or any api allowlist logic.

PRE-WORK EXISTS, CONSUME IT, DO NOT RE-DERIVE: I have uploaded pre-built files to the workspace: chunk1-error-copy-deck.md (the error and edge-case copy deck, railway voice), an iOS web audio helpers module (contains primeAudioUnlock, isIosSafariWeb, shouldShowIosAudioHint, markIosAudioHintShown, with integration notes in comments), and INTEGRATION-MANIFEST. Read all three first. The manifest is authoritative for what belongs to Chunk 1 and where each piece integrates. Where the manifest and this paste enumerate the same item, follow the manifest's integration detail. If the manifest lists a Chunk 1 item this paste does not mention, include it in your Step 0 report and wait for my ruling before building it.

STEP 0, REPORT FIRST, DO NOT EDIT YET. Report in a numbered list:

Confirm the three pre-built files are present and readable; list their paths.
List every Chunk 1 item per the manifest, each with the target file(s) you located for it.
For the error copy deck: report which user-facing error and edge-case strings currently exist that the deck replaces, with file paths (practice, chat, network failure, mic failure surfaces).
For audio helper 4a (autoplay priming): identify the session entry gesture handler(s) where primeAudioUnlock must be called before navigation, and identify where the first coach phrase autoplay happens, including whether its play() rejection is currently caught.
For audio helper 4c (silent-switch hint): identify where the first coach play attempt of a session is detectable, and propose the trigger wiring per the module's comments.
Mascot long-press: report which component renders the mascot image on web practice and whether any touch-callout or context-menu suppression exists today.
Privacy policy page: report per Section D below.
Then STOP and wait for my confirmation before building anything.

SECTION A, ERROR COPY DECK: Apply the copy deck strings to their surfaces per the manifest mapping. Copy is verbatim from the deck, do not paraphrase or improve it. Where a surface in the deck has no existing string (silent failure today), add the surfaced state per the deck. Rendering only, no behavior changes.

SECTION B, IOS WEB AUDIO (items 4a and 4c): Integrate the uploaded helpers module as-is, do not rewrite it.

4a: call primeAudioUnlock() inside the session entry tap handler(s) identified in Step 0, before navigation. Wire the first coach autoplay so a play() rejection sets a visible tap-to-hear affordance on the existing speaker button instead of failing silently.
4c: show the one-time silent-switch hint per the module's trigger guidance and the hint copy in the copy deck. iOS Safari web only, at most once per device, never in the native app.

SECTION C, MASCOT LONG-PRESS: On iOS web, long-pressing the mascot opens the system image save menu. Suppress it on the mascot image and its animation frames (webkit touch callout and user select none, or the equivalent for how the image is rendered). Do not suppress text selection anywhere else on the page.

SECTION D, PRIVACY POLICY DATA RETENTION UPDATE: Locate the privacy policy page content containing the "Data retention" heading. Report in Step 0: the file path and format, the current full text of the section, whether the page has a last-updated date, whether the section contains any email address or contact form link, and whether the content is web only or shared with mobile. After my confirmation, replace the entire body of the Data retention section with the following, preserving the existing heading level and styling, with "Voice recordings." rendered as bold inline text, reproduced exactly:

We keep your account information and learning progress for as long as your account is active. When you delete your account, we delete your associated personal data and learning progress.

Voice recordings. In normal use, your voice recordings are processed to score your pronunciation and are not retained after the scoring request completes.

Separately, we run an optional voice contribution mode used to improve BOLO's pronunciation scoring. This mode is clearly indicated on screen and is only available to invited contributors. Recordings made in contribution mode are retained, along with the phrase and language they belong to, and are used solely to evaluate and improve our scoring system. If a contributor re-records a phrase, the discarded take is deleted. Contributors can request deletion of their contributed recordings at any time by contacting us, and we delete them when the associated account is deleted.

Rules for Section D: update the last-updated date if one exists, in its existing format. Do not insert the address privacy@bolo-india.app anywhere; if the existing section links to the contact form, keep that link on "contacting us"; if it contains privacy@bolo-india.app as a raw address, flag it in Step 0 and wait for my ruling. Touch no other policy section.

GATE, ONE, AT THE END OF THE WHOLE CHUNK: typecheck clean, full web test suite green (baseline 384 tests, 54 files; report the new count), targeted manual verification in the browser: one error surface showing deck copy, privacy policy section rendered correctly with the bold lead-in, and confirmation of which items are iOS-device-only verifications that Expo-web cannot prove (list them for my device pass, do not claim them verified). Then STOP, report done with the verification summary, wait for my confirmation, and commit ONCE with message: "Chunk 1: web polish batch (error copy deck, iOS audio priming and hint, mascot long-press, privacy policy retention)". No suggested or draft task cards. No em dashes anywhere.