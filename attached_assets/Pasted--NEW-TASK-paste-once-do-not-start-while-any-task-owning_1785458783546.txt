→ NEW TASK — paste once — do not start while any task owning home.tsx or journey.tsx is running (tear, launch moment, review-card polish must be merged first)

Build 31: one-path restructure — Phrasebook door replaces the home topic grid. Web AND mobile. Consult docs/CODEBASE-FACTS.md and agent memory before any code. The Step 0 entry-point trace is complete; key verified facts: train and topics share ONE progress ledger (both derive from the same attempts rows via phrases.lesson_group_id → lesson_groups.category_id), so this is navigation consolidation with NO data migration; topic-card practice already cannot run ahead of the train; zone titles in journeyLines.ts are client-side duplicates of categories.title joined by hardcoded id 1-6.

Hard constraint: do not modify the boarding pass. Its imagery, animation, tear-off, CTA copy, and all associated constants and tests stay exactly as they are. The pass remains the sole progression CTA. Same for the journey map's art and animation.

Topic cards off home, Phrasebook door on. Remove the topic-card grid from home (web src/pages/home.tsx topic section; mobile CategoryCard section in app/(app)/(tabs)/index.tsx). In its place, directly below the boarding pass: a single Phrasebook entry card — book icon, title "Phrasebook", subtitle "Browse and practice any topic", chevron, and a chip row previewing topics: chips show topic title plus mastered/total where progress exists, first 3 topics plus a "+N more" chip. Tapping the card or any chip opens the Phrasebook surface (chip taps may deep-link to that topic). Quiet visual treatment: the pass must remain the loudest element; the door is a standard bordered card.
Phrasebook surface. Web: an index page listing all topics for the active language as cards with per-topic progress, each opening the existing /learn/:id flow (which stays untouched). Mobile: equivalent screen listing topics, each opening the existing /(app)/category/:id flow. Framing is library/reference: header "Phrasebook", no journey or continue language. Free-taste and premium gating behavior on topic entries must remain exactly as the existing topic cards behave today (the entry points move; the gates don't change).
Test-out dialog wiring. The server test-out flow is fully implemented (GET/POST /lesson-groups/:id/test-out, 5 sampled phrases, 0.8 pass ratio, tested_out latch; Express stamp already renders on both clients). Wire the missing UI: the progression lock dialog on the journey (web journey.tsx ~L852-856 dialog, kind "progression", and the mobile mirror) gains a "Test out of this stop" action alongside "Keep practicing". It launches a minimal test-out flow: fetch the 5 phrases, run them through the existing practice attempt UI in a test-out mode, submit to the POST endpoint, show pass (stop unlocks, Express stamp) or fail (encouraging copy, back to practicing) results. Reuse the practice screen with a mode param rather than building a parallel recorder if feasible — Step 0 this decision and report before building the flow. Sentence-gated and language-locked dialogs unchanged.
Zone-title dedup. Kill the drift risk: zone titles come from the categories listing (already fetched by both journey clients) instead of the hardcoded strings in journeyLines.ts / the mobile equivalent. Keep the hardcoded strings as fallback for loading states only. Zone-to-category id joins stay as they are.
Blast-radius cleanup, per the trace's inventory: update or remove tests that pin the topic grid on home; recent plays, review card, chat shortcut, stats banner, and quiz card all stay on home unchanged; the notification resolver and /practice, /learn, /category routes are untouched; analytics — topic-open events fire from the Phrasebook surface now, add a phrasebook-opened event, keep event names for topic opens stable if dashboards reference them (report what exists).

Step 0 first (short, since the big trace is done): confirm current home topic-section boundaries on both platforms, the categories data already available to home for the chip row (no new API calls for the door), and the practice-screen reuse decision for test-out mode. Then build.

Tests: door renders with chips and correct progress, door absent of topic grid (grid removed), Phrasebook surface lists topics and routes correctly, lock dialog shows test-out action, test-out pass latches and unlocks (can stub server), test-out fail path, zone titles render from categories data, boarding-pass suite untouched and green. Enumerate test counts before and after, all three suites (api-server should need no changes — if it does, stop and report why before making them). No em dashes. Report per item with file paths and web-verified / jest-verified / deferred-to-device status.