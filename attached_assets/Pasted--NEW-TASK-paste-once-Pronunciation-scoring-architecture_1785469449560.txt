→ NEW TASK — paste once

Pronunciation scoring architecture investigation. REPORT ONLY — no changes to any production code path, no new dependencies installed into the app, no schema changes. Sandbox experiments are allowed in a scratch directory. Consult docs/CODEBASE-FACTS.md and agent memory (band derivation, eval pipeline, tts config).

Problem statement: scoring does not discriminate pronunciation quality or natural delivery. The pipeline is transcript-centric (Whisper transcribe → compare to target), and Whisper's robustness to bad pronunciation erases the signal we want to grade — if the transcript resolves to the target words, high bands follow regardless of how poorly they were spoken. Five bands currently sit on an effectively binary signal.

Deliverables:

Current-pipeline audit: document exactly what signal reaches band derivation today: transcript similarity only, or anything acoustic (confidence values, logprobs, timing)? Quote the code. Report whether the eval provider already returns per-word confidence or timing data we currently discard.
Options analysis, with evidence not vendor marketing, for adding a pronunciation-quality signal. Evaluate at minimum: (a) dedicated pronunciation-assessment services (Azure Speech pronunciation assessment and comparable offerings — check current support for our Indic languages, per-language coverage table for all 22, pricing per assessment); (b) prompting an audio-capable LLM (gpt-audio class) to grade pronunciation from the clip against the target — run a small sandbox experiment: 5-10 recordings of the same Hindi phrase spoken well vs deliberately badly, report whether its grades actually separate them and how consistently across repeated runs; (c) forced-alignment/phoneme approaches (open-source, self-hosted) — feasibility and per-language coverage honestly assessed; (d) hybrid design: transcript match gates pass/fail, acoustic signal grades the band — what each candidate would look like slotted under the existing five-band ladder without changing the client contract.
Recommendation matrix: per option — Indic language coverage (critical: we support 22, many low-resource), latency added per attempt, cost per attempt at 1K/10K/100K attempts per month, implementation size, and risk. State a ranked recommendation.
Constraint: whatever is recommended must preserve the existing client contract (five bands + transcript + romanized transcript) — the signal swaps under the ladder, the ladder stays.

Budget discipline: cap sandbox API spend at a few dollars; if an experiment needs credentials we don't have (e.g. Azure), design it and report the setup needed rather than blocking. Report format: numbered per deliverable, verified vs inferred, with the sandbox experiment's raw results included.