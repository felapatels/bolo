Ready-to-execute sequence for the build agent — everything verified against the live codebase this session; only the three tester IDs are markers.

Capture-session provisioning: execute once all four dev sign-ins exist
Precondition check — each tester must have signed in on dev web AND loaded the app once (creates the users row). Watch for Apple "Hide My Email": if an email lookup misses, match by name against recent users instead.

# Look up the three missing IDs (CLERK_SECRET_KEY is the dev sk_test key):
for e in pikaboyliam@icloud.com lilrylan@icloud.com kruti818@hotmail.com; do
  echo "== $e"
  curl -s -H "Authorization: Bearer $CLERK_SECRET_KEY" \
    "https://api.clerk.com/v1/users?email_address=$e" | \
    node -e 'let d="";process.stdin.on("data",c=>d+=c).on("end",()=>{const a=JSON.parse(d);console.log(a.length?a[0].id:"NOT FOUND — check for Apple relay account by name via /v1/users?limit=25&order_by=-created_at")})'
done
# Known: aakesh_patel@yahoo.com = user_3HBsumTkU4xtAYmMlH62DKnC5j9

Step 1 — Env append (shared environment; PRESERVE the existing owner ID as the first entry):

PILOT_CAPTURE_USER_IDS=user_3H9XG2UWijYqRUXv6Ls0geMmX4Z,user_3HBsumTkU4xtAYmMlH62DKnC5j9,<ID_PIKABOYLIAM>,<ID_LILRYLAN>,<ID_KRUTI>

Set via the env-var manager (shared env — it lives there today, NOT in .replit, NOT a secret). Comma-separated, no spaces needed (parser trims).

Step 2 — Restart (mandatory: allowlist parses ONCE at module init, artifacts/api-server/src/lib/pilotCapture.ts:31-36; the dev script is build && start with no watcher). Restart the api-server workflow, then confirm up: GET /api/health 200 and a fresh server PID.

Step 3 — Entitle All-Access (tier plus = All-Access; provider left NULL keeps these rows invisible to the Stripe reconcile sweep):

UPDATE users
SET tier = 'plus', subscription_status = 'active',
    current_period_end = NOW() + interval '90 days'
WHERE id IN (
  'user_3HBsumTkU4xtAYmMlH62DKnC5j9',
  '<ID_PIKABOYLIAM>', '<ID_LILRYLAN>', '<ID_KRUTI>'
)
RETURNING id, tier, subscription_status, current_period_end;
-- MUST return exactly 4 rows. If fewer: that tester's users row doesn't
-- exist yet (they haven't loaded the app post-sign-in). Do not INSERT
-- rows by hand; have them open the app, then re-run.

Step 4 — Three-part verification (the #994-closing standard):

(a) Live-process allowlist — read the RESTARTED server process's environment, not a fresh shell:

APIPID=$(ps -e -o pid,cmd | grep "dist/index.mjs" | grep -v grep | awk '{print $1}')
tr '\0' '\n' < /proc/$APIPID/environ | grep PILOT_CAPTURE_USER_IDS
# Must contain all 5 IDs (owner + four testers).

(b) Entitlement readback — SELECT id, tier, subscription_status, current_period_end FROM users WHERE id IN (<the four>); — all four rows plus/active/future date.

(c) Tee fires per ID — drive the real tee with a realistic-size binary buffer (≥50 KB; a tiny probe passes while real clips fail — R2 rejects the SDK's auto-CRC32 on larger bodies unless requestChecksumCalculation: "WHEN_REQUIRED" is set, which r2Client.ts handles; the probe must exercise that path). Throwaway script in /tmp (never in the workspace), run via the pnpm-virtual-store tsx bin (see memory: tsx-scripts.md):

For each of the four IDs: call teeAudioToPilot(Buffer.alloc(60_000, 7), { userId, languageCode: "probe-check", phraseId: null, targetNative: "x", targetRomanized: "x", transcript: "x", score: 0 }).
Expect the [pilot-capture] uploaded clip pilot-clips/probe-check/… success log per call (failures log LOST at ERROR — WARN-swallowed silence is the known trap).
ListObjectsV2 on pilot-clips/probe-check/ → exactly 8 objects (4 × .m4a + .json); GET one sidecar to confirm shape; then DELETE all 8 probe objects.
The script inherits the NEW env only if launched after Step 1; it exercises the identical parse+membership+upload code path the server runs.
Success criteria: (a) shows 5 IDs in the live process, (b) shows 4 Plus-active rows, (c) shows 4 success logs + 8 objects round-tripped and cleaned. Then the freeze is absolute again.

That's everything: sequence is idempotent, ordered, and carries its own failure diagnostics. My side stayed read-only throughout — no files, env, or DB touched. Good luck with the session tonight.