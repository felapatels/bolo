BRIEF A (FULL WEB PARITY): Mobile changes for today's TestFlight build. Mobile app (bolo-mobile) only; the web and api-server are read-only references. Consult docs/CODEBASE-FACTS.md at start; the Task #973, #954/resume-ruling, Task 985, and Task #986 sections document the parity loci and spec inputs. No em dashes anywhere. Deliberate exclusions from this build: mobile tear SFX (the recorded asset shipped on web at 57be2f6; mobile keeps its native PressableScale haptic and gains the sound in a later pass), Android (separate workstream).

STEP 0: PARITY AUDIT (report and STOP for confirmation before implementing)
1. Confirm current main lineage includes: 8c63566 (#967 games hub), 4337839 (resume-ruling pre-brief), the Task 985 scenery/depth commit, ed5f614 (#986 punch-up), 57be2f6 (#984 tear by intent). Confirm the certified web baseline 370.
2. Confirm bolo-mobile builds clean at start.
3. PARITY TABLE: enumerate every user-facing feature or fix landed on web in the current cycle and report mobile's status for each (present / absent / diverged). Cover at minimum, and add anything else found in CODEBASE-FACTS or recent commits: practice prev/next navigation; journey rail comet; games hub color redesign AND the #986 energy model; completed-station resume ruling; station-resume base behavior (e3972fe); journey India scenery + 2.5D depth (Task 985); boarding pass tear animation + haptic (mobile reported native via PressableScale, verify); showroom allTopBand omission (server-side, verify mobile renders locked groups correctly against it); five-band ladder and band labels (no raw numeric scores anywhere); one-path structure surfaces (door present, tour removed).
4. Report the structures you will modify: mobile journey (app/(app)/journey.tsx), mobile practice (app/(app)/practice/[id].tsx), mobile games hub, splash config, and the mobile practice session start-position derivation.
5. Any parity gap found in item 3 that is NOT covered by items 1 through 7 below: list it with a one-line size estimate; do not implement it without confirmation.
6. The standalone Task #968 card, the drafted #990, and the auto-planned mobile scenery task are all superseded by items 3 and 7 below; do not merge or interact with their checkouts.

ITEM 1 (#976 scope): Practice prev/next port
Free navigation chevrons on the mobile practice screen: never attempt-gated, always lands idle with no playback/recording/scoring side effects, prev disabled on first phrase and next on last, both disabled during recording/evaluating, hidden in test-out mode. Zero layout shift for the hold-to-talk circle, and the circle must never shift under the finger during recording state (standing rule). Attempts and bestScore survive any visit order. Accessible names "Go to previous phrase" / "Go to next phrase" (the web CTA-collision trap, documented).

ITEM 2 (#977 scope): Journey comet port
Mobile has its own pulse implementation; port the DESIGN, not the web code: single bright traveling comet, sharp-attack/slow-decay, glow treatment. Source of truth: web RAIL_PULSE export (dotsPerSegment 10, dotRadius 4) and web :root timing (cycle 3.4s, peak 1, glow 5px, keyframe shape 0/4/22), mirrored in mobile conventions. Reduced motion renders nothing.

ITEM 3 (#968 + #986 scope): Games hub, full current web parity
FlatList 2-column grid; per-game color tokens as hex equivalents of the landed web GAME_COLORS including the pressGlow additions (read actual values from the landed config). Entrance: staggered cascade with rise + scale-from-0.9 and springy overshoot (springs.poppy equivalent in Reanimated), five cards settling under 700ms; header Bolo does one whole-image bounce at cascade start, once per mount. Vignette energy model: GamePreview equivalents idle at slow ambient tempo (2.2x duration multiplier), wake to authored full energy on press-in; locked cards hold a paused static frame, washed-out same-hue, and play only while pressed (look-but-locked); off-screen vignettes pause (FlatList viewability callback replaces the web IntersectionObserver). Press: squash-and-spring-release plus per-game hue glow via pressGlow; navigating into a game scales the pressed card up slightly, never delaying navigation. Restraint: nothing beyond vignettes idles; pills, locks, badges static. Reduced motion: everything static, no loops, instant entrance. PressableScale + useAppearSkip + FadeInDown.springify() remain the base vocabulary; scroll-gesture coexistence verified on the grid. All game-preview test IDs preserved.

ITEM 4: Completed-station resume ruling
Implement the ruling per the CODEBASE-FACTS section landed at 4337839 (binding on mobile): a session starts at the first phrase whose bestScore is null or below 80 whenever one exists, regardless of station completion status; phrase 1 only when all phrases are mastered or no per-phrase data exists (tested-out included). No status field consulted in the start derivation. Add the mobile equivalents of the three web pins: T,T,F,T,F starts at index 2; unmastered-at-0 starts at 0; tested-out all-null starts at phrase 1. Entry route must not change the start position. Preserve: full phrase set, teaser sets inert, test-out start unchanged.

ITEM 5: Mic-grant guard (independent mobile audit)
Granting OS microphone permission must NEVER auto-start a recording; an explicit hold is required. Web practice audited clean, but the platforms' permission flows differ, so audit mobile practice independently: if the unobserved-release hole exists (permission-grant callback starting capture without an explicit press), fix it per the chat pattern (grant never starts recording, explicit record action only) and add a pin; if not, report the evidence and change nothing.

ITEM 6: Splash mascot sizing
Reduce the splash screen mascot from 78 percent frame height to a smaller treatment. Propose the value from the canonical mascot rule and the existing splash layout, apply it, and report before/after. Whole-image transforms only; the canonical PNG is never redrawn.

ITEM 7: Journey scenery + 2.5D depth port (Task 985 parity)
Sequenced AFTER item 2 in the same file. Port the design per the Task 985 CODEBASE-FACTS spec inputs, not the web code: the nine-asset scenery set (tuk-tuk, cow, fruit cart, unmanned chai stall, temple silhouette, banyan, marigold toran, cycle rickshaw, river ghat) in the dimensional flat style (3/4 view, one shading step, ground-contact ellipse, shared upper-left light); the zone-themed deterministic placement (urban to market-town to river-temple, 1-3 per zone, alternating sides, anchored to the mobile map's own geometry constants, no per-render randomness); the depth pass using the DEPTH_2_5D layer tokens (scenery < rail < cards < stations < train < postcards < overlays) with the shared shadow language expressed in native styles; rail-bed thickness treatment without altering the path the comet samples; scroll parallax via the mobile map's own scroll mechanism at the documented factor, absent under reduced motion. Bolo PNG untouched beyond the shared ground shadow. Scenery decorative only: no tap targets, no progress semantics, no idle motion (micro-motion candidates remain deferred). If the mobile rendering stack makes any sub-piece disproportionately expensive (e.g. parallax), report the cost and propose deferring THAT sub-piece rather than silently shipping a degraded version.

GATE: typecheck clean across all projects, both boot smoke tests pass, full web suite unchanged at 370, api canonical 682 tests / 681 pass / 1 fail (pilot-capture env fence), mobile test suites green including new tests for items 1, 3, 4, and 7 minimum. Report by name, including the Step 0 parity table and any item 5 evidence. WAIT for confirmation before commit. Device verification is Brief B and runs against the built binary, not Expo web (standing rule: Expo-web is never sufficient for native claims).