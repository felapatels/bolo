Progression completion design spec: checkpoint polish + conversation capstone. REPORT ONLY — no production code changes; deliverable is docs/specs/progression-completion.md plus findings. Consult docs/CODEBASE-FACTS.md, agent memory, docs/specs/pronunciation-scoring-v2.md (polish depends on trustworthy bands), docs/specs/token-economy.md (Chai reward hooks). Build 32 implementation, relaxed timeline, gated on 31 approval; polish additionally gated on scoring v2 shipping in the same build.

Part A, checkpoint polish:

Trigger and surface: at stop completion, when any phrase in the stop sits below the top band(s), the completion moment offers an optional "Polish your phrases" card listing sub-top phrases with their current bands. Skippable with zero friction; never blocks progression, never costs attempts (hard rule: no punitive mechanics). Trace the actual stop-completion flow on web and mobile (files, where the completion moment renders) and specify where the card slots.
The polish run: re-runs only the listed phrases through the normal practice loop; improved bands overwrite, worse attempts never downgrade a stored best (verify how best-score storage works today and specify). XP: the improvement delta earns normally per the existing XP rules (quote them); design a small "all top band at this stop" recognition (visual stamp on the stop, plus a Chai bonus hook reserved for the token build, amount left as config).
Re-entry: polishing must also be reachable later from a completed stop (not only at the completion moment); specify the entry point on the journey map consistent with the existing completed-stop UI.

Part B, conversation capstone:
4. Zone completion unlocks a themed guided conversation with Bolo. Design the scenario system: a per-zone scenario definition (id, title, framing copy, target phrases drawn from the zone's mastered content, Bolo's steering instructions), stored where content lives today (trace how zones/categories map and where scenario definitions best sit — server content vs config). Zone 1 example fully written: Greetings & Manners → greet Bolo at the platform chai stall, use kem cho/namaste/aabhaar-class phrases naturally.
5. Chat integration: trace the current chat system prompt assembly and specify how a scenario mode parameterizes it (scenario framing injected, Bolo steers toward target phrases, gently acknowledges when the learner uses one). Entry point: zone-completion moment on the journey plus a replay entry on the completed zone header. The scenario chat uses the existing chat surface and voice pipeline unchanged; specify the mode param and what renders differently (scenario banner, target-phrase chips).
6. Completion semantics: what counts as finishing a scenario (lightweight: N target phrases used, or Bolo declares the scene complete), what it awards (XP, a zone conversation stamp, Chai hook), and explicitly NO scoring bands inside conversation (chat stays judgment-free; the capstone is performance, not exam).
7. Free-tier posture: does the free user get zone 1's capstone? Recommend (lean yes: it is the best conversion moment in the product — taste the magic, then the paywall for zone 2+ scenarios) and state the gating mechanics consistent with existing entitlements.
8. End-of-line note (33+, design sketch only): the Varanasi arrival moment concept in one page for future work.

Findings: anywhere current code contradicts assumptions, open questions for Aakesh. Build-32 task breakdown, ordered, sized, file-owned, with polish tasks marked dependent on scoring v2 landing. No em dashes anywhere.