SESSION PAUSED FOR FIXES: the freeze is LIFTED for this bounded fix block only. Testers are standing by; speed matters, but report-then-fix discipline holds. Three items, in order:

ITEM 1 (protocol-breaker, sharpened by tester observation): after the 3rd attempt on a phrase, the practice result card changes: "Try again" either moves position or is replaced/demoted by "Next phrase" as the primary CTA. Testers tapping the accustomed Retry position are accidentally advancing to the next phrase, which breaks the 4-attempt capture protocol and is a general mis-tap trap besides. Diagnose first and report in two lines: exactly what changes on the card at attempt >= 3 (button order swap, primary/secondary flip, or Retry removal) and where the attempt-count threshold lives. Then fix: the result card's layout and button positions are IDENTICAL regardless of attempt count: Retry never moves, never disappears, and Next never occupies Retry's position. If the after-3-attempts encouragement to move on is worth keeping as a product touch, express it as copy or a subtle highlight on the Next button, never as layout mutation. Do not change scoring, persistence, auto-advance-on-pass behavior, or the prev/next chevrons.

ITEM 2 (grant-moment junk attempt): on web practice, immediately after the user accepts the browser mic permission prompt, the app produces a failed try with a Retry card: an empty attempt the user never made. This is the practice-surface cousin of the fixed chat grant bug. Diagnose: does the permission-grant continuation submit a recording or an empty buffer without a completed hold? Fix per the chat.tsx pattern: a permission grant never produces an attempt or a result card; the first press after grant starts a fresh, normal recording. Add a pin test if cheap.

ITEM 3 (cosmetic, included because we are here): the Retry button renders blank (invisible label) in dark mode. Fix the styling so the label is visible in both light and dark themes; check the sibling buttons on the same card while there.

GATE for this block: typecheck clean, targeted tests on touched files green, tripwire pre-step, ONE commit covering all three items, post the hash. Client-only changes need no server restart; if ANY api-server file changed, restart it and then MANDATORY re-verification: (a) the live process environ contains all five PILOT_CAPTURE_USER_IDS (boot-time allowlist re-rolls on every restart), and (b) one realistic-size (~50KB) tee probe per the #994 pattern confirms capture still works, probe objects deleted after. Report DONE with the hash and, if restarted, both verification results; the freeze then RE-SEALS absolute and the session restarts.