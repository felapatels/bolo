→ NEW TASK

Build 30 fix batch 3: games package + home cleanup. Consult docs/CODEBASE-FACTS.md and agent memory. Step 0 traces are done; file paths below come from them. Fix exactly these, nothing else. Games and home components are shared surfaces: implement once, verify web and jest, enumerate anything deferred-to-device.

Games:

Target-language audio in every game, spoken by default. Word Match, Phrase Builder, and Speed Round currently have no TTS; Listen and Pick and Bolo Quiz already wire useSynthesizeSpeech + playBase64Audio with a per-card cache. Copy that in-repo pattern: Word Match speaks the Indian card's nativeScript when flipped (never the English match card), Phrase Builder speaks a word tile's nativeScript when tapped into the sentence, Speed Round speaks the prompt word. Reuse the existing cache-key pattern.
Shared mute toggle in all five games. One shared control component, default unmuted, persisted on device (follow the practice/review spoken-feedback mute persistence pattern). Visible in each game's play surface. Muted skips synthesis calls, not just playback.
Exit affordance during active play. Device evidence: mid-play screens (Speed Round confirmed via screenshot) have no visible back control; the traced back buttons live on intro/results screens. Add a persistent, safe-area-respecting exit control on every game's active-play state (all five). Exiting mid-game confirms before discarding a run in timed games.
Listen and Pick wrong-answer review beat. Currently advances on a fixed 800ms feedbackDelay with no user control. On wrong answers: hold the reveal state (red pick, green correct, replayed audio) until the user taps to continue. Correct answers may keep auto-advance.
Phrase Builder reorder. The placed array is ordered and supports it; UI is append/remove only. Allow reordering placed tiles before checking (tap-based is acceptable, e.g. select then move; drag not required). Pin with a test.
Bolo Quiz naming. One name everywhere: "Bolo Quiz." Rename the home card title (currently "Daily Quiz") and any other "Daily Quiz" user-facing strings; home card subtitle keeps the daily hook (e.g. "A fresh quiz every day"). Route names and backend endpoint paths stay as-is; user-facing copy only.
Quiz gate must fail closed. Device evidence: a fresh free account may have briefly shown a start affordance on the home quiz card. Audit the isPlus/subscription-status consumption on the home DailyQuizCard and the hub tile: while subscription status is loading or undefined, render the locked state, never the unlocked one. Pin with a test for the undefined/loading case.

Home:

Delete ContinueCard. Remove components/ContinueCard.tsx, its render on home, and both test files that reference it (home-continue-card directly, home-daily-goal-celebration's mock). The train journey boarding pass is the sole continue mechanism. Web equivalent removed too if present.
Name capture on first home visit. Email sign-up captures no name, so Clerk firstName stays null and the greeting says "friend" forever. Add a one-time, dismissible first-home-visit prompt: "What should Bolo call you?" with a single name field. Submit through the existing PATCH /account/profile dual-write (Clerk + users.display_name) so every consumer updates; call user.reload() after save like settings does. Shows only when firstName is null; dismiss persists and never nags again; settings remains the edit path. Web and mobile.
Pull-to-refresh spinner. Three traced causes on mobile home: no retry policy (bare QueryClient retries 4xx three times), no fetch timeout, and background invalidations from games/review flipping the derived isRefetching with no pull gesture. Fix all three: port the web shouldRetry policy (never retry any 4xx) to the mobile QueryClient, add a reasonable fetch timeout via AbortSignal in custom-fetch, and drive the RefreshControl from gesture-initiated refresh state (local flag set on pull, cleared on settle) instead of raw isRefetching. Pin with tests including the background-invalidation case.

Rules: one owner per file surface. This task owns games/*, home index.tsx + ContinueCard, the QueryClient/_layout retry policy, custom-fetch, and the name-prompt flow. Do NOT touch practice/[id].tsx, journey.tsx, TicketParts/JourneyPassCard (batch 1 owns those) or chat.tsx, tabs _layout.tsx, lib/audio.ts (batch 2 owns those). If the shared custom-fetch timeout risks colliding with batch 2's audio streaming fetches, report the conflict instead of resolving it unilaterally. Enumerate test counts before and after. No em dashes in code or copy. Report per item with file paths and web-verified / jest-verified / deferred-to-device status.