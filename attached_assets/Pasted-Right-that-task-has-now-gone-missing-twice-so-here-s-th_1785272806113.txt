Right, that task has now gone missing twice, so here's the full self-contained prompt: ship order, complete invite email task, polish batch queued behind it. Paste the whole thing:

Ship the auth fix: commit it now. Then run the following two tasks in order, one at a time.

TASK 1: Invite email sender migration. Read docs/CODEBASE-FACTS.md first. Currently invite emails send from onboarding@resend.dev. The Resend domain bolo-india.app is verified (DKIM/MX/SPF all green). Move invite email sending to hello@bolo-india.app. Requirements: (1) The sender address must be env-configurable, not hardcoded; document the variable name and where it must be set for dev and for the production deployment, remembering the build-time VITE_* trap in the facts doc if any client-side surface is involved. (2) Audit for any other places that send email via Resend (contact form, alerts, anything) and report whether they also use onboarding@resend.dev; do not change them in this task, just report. (3) Send a real test invite to aakesh_patel@yahoo.com and report the result, including whether it landed in inbox or spam. (4) Stop and ask if the Resend integration code paths differ from what the facts doc describes. Acceptance: the system shall send invite emails from the configured sender address; the system shall default to hello@bolo-india.app in production configuration; the system shall not hardcode the sender address in code. Scope: email sending code only, no bolo-mobile, no auth surfaces.

TASK 2 (only after Task 1 is committed): Production polish batch, web only, no bolo-mobile. (a) Sign-in card logo: replace the current flag icon with the Bolo bird mascot holding an Indian flag. First check whether a bird-with-flag asset already exists (search assets and mascot animation frames, check the facts doc mascot inventory). If it exists, use it. If it does not exist, STOP and report what mascot assets are available; do not composite, generate, or substitute artwork. (b) Result screen viewport fit: the mascot pushes feedback below the fold; fix so score and feedback are visible without scrolling on a 390x844 viewport. (c) Silent-mode headphone icon clarity fix. (d) daily_goal default from 10 to 50 for NEW users only, do not modify existing users' values; first check the facts doc debt table for whether a cleanup-pass item already changed this and report what you find. Acceptance: the system shall display the configured sign-in logo asset; the system shall present score and feedback within the initial viewport on a 390x844 screen; the system shall default daily_goal to 50 for newly created users and shall leave existing users' values unchanged. Commit when verified.