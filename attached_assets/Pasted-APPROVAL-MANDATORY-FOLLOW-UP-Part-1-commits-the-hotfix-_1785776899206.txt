APPROVAL + MANDATORY FOLLOW-UP. Part 1 commits the hotfix; Part 2 is the splash investigation from the addendum, which the report did not perform. Execute in order.

PART 1, COMMIT NOW: Pre-check git log -1 --decorate; HEAD must be 7d6a7b3. Stage exactly the 13 files enumerated under "Files changed" in your report, nothing else; verify with git diff --cached --name-only (13 paths). Commit message: "Prod hotfix: signal taps, soft stop, fact variety, game audio, US English"
Confirm with git log -1 --decorate and git show --stat HEAD (13 files). Include this confirmation at the top of the Part 2 report.

PART 2, SPLASH ROOT CAUSE (mandatory; static reading already failed to explain prod): Owner-observed on prod at the 16:12 deploy, logged in, fresh tabs: blank WHITE screen with skeleton component frames, no gradient backdrop, no composed scene, app appears around the 8s failsafe mark. Network shows carriage/steam SVGs 200 with the bundle as initiator, which your decode-gate preloader explains without any paint.

Run ONE lean real-browser probe of the PRODUCTION build (build with the required PORT/BASE_PATH env, serve dist, observe boot). Determine which of these is true, in order: (1) the pre-reveal overlay renders transparent instead of its gradient backdrop; (2) the decode gate never resolves and the 1200ms cap fails to force the reveal; (3) the ready-signal release never fires, leaving only the 8s failsafe; (4) a class, CSS var, or keyframe is purged or altered in the production build; (5) the overlay never mounts and the 200s come solely from the preloader. Capture the overlay's computed styles and data/state at boot. Delete probe artifacts.

PART 2 REPORT: Part 1 commit confirmation; root cause in ONE sentence + which suspect; the minimal proposed fix (gujarati-coach + facts doc only) with files listed, UNSTAGED; DEVIATIONS or none; targeted test results if the fix touches tested surfaces. STOP for approval before the second commit. If the fix cannot be small, report the root cause and options instead of building.

END OF REPORT is the final line. All standing rules apply: no task cards, tree unstaged until approval, failure identity before re-runs.

END OF TASK. If this line is not the last line you received, the paste truncated; stop and say so.