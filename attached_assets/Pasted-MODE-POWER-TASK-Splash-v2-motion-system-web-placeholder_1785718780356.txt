MODE: POWER

TASK: Splash v2 motion system (web), placeholder art, branch build. NOT a commit to main.

HARD RULE: ZERO commits to main. Work on branch splash-v2-motion. ONE report, then STOP for approval; the branch merges only after the currently running server task commits, per the owner's merge-order rules. No task cards; bank suggestions as report lines.

SCOPE: artifacts/gujarati-coach only, plus docs/CODEBASE-FACTS.md at the end. Do not touch the api server, bolo-mobile, lib/, or any file another task owns.

STEP 0 (verify, do not explore; fold into report; misses hard-stop): A. Name the file(s) implementing the current web splash/boot screen and the exact signal or moment that currently dismisses it; quote the dismissal line. B. Name the landing surface's primary data query (the one whose resolution should gate dismissal) and its exact query key. C. Confirm the app's reduced-motion detection pattern and theme mechanism. D. Confirm framer-motion or the house animation utility used elsewhere on web; the sequence uses the house pattern, no new animation dependency.

BUILD: implement the SPLASH V2 MOTION SPEC pasted immediately before this task, exactly. Placeholder art: generate simple flat SVG placeholders in-code (teal rounded rectangle carriage with a window cutout; a plain circle with a small triangle wing in two positions for the character frames; two soft gray ellipse steam blobs), each rendered through a single asset map object so the future real PNGs replace placeholders by changing only that map's paths. Sizing per the spec's settle frame: subject cluster ~45% of frame height, centered, generous margins.

GATE (lean): typecheck mandatory. Grep web test files for assertions on the current splash/boot surface; update only genuinely affected assertions and run only those files, enumerated. No new test files; no animation-timing tests; the owner eyeballs motion.

REPORT (one message, then STOP): Step 0 A-D with quotes; files changed enumerated; DEVIATIONS or "none"; the asset-map object's exact shape and placeholder list; how the ready signal + 1.8s floor + 6s ceiling are implemented, one line each; gate results with the full-suite line NOT run (branch build, suite runs at merge time per merge rules); git status on the branch confirming zero main commits and zero attached_assets staged. Proposed eventual commit message: "Splash v2: layered motion boot with ready-signal hold". No em dashes.

END OF TASK. If this line is not the last line you received, the paste truncated; stop and say so.