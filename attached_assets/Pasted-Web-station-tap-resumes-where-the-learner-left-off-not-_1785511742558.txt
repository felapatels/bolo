Web: station tap resumes where the learner left off, not at the group's first phrase. Consult docs/CODEBASE-FACTS.md and agent memory. Device evidence (mobile, build 31): tapping a station on the journey map starts practice at the first phrase of the group even when earlier phrases in that group are already mastered — the learner replays finished words. Web shares the same href shape (/practice/:categoryId?group=:groupId per the entry-point trace), so fix web now; mobile ports in the parity sync later.

Step 0 first, report before building: (1) trace what group-mode practice does on load today — the phrase list order and where the session starts; confirm there is no resume/position logic; (2) inspect the existing skipMastered=true behavior (the notification resolver deep-link at /practice/daily uses it) — what exactly it filters, whether it applies in group mode, and whether "mastered" there means the same best-score >= 80 rule as journey progress; (3) determine what data group-mode practice already loads that identifies per-phrase mastery (the per-phrase stats power the journey counts, so it should be present or cheaply available). Then build.

Fix:

When practice opens in group mode from a station tap, the session starts at the first NOT-yet-mastered phrase in group order. Mastered phrases remain reachable (the learner can navigate back to them; do not remove them from the session) — the change is the starting position, not the phrase set. If skipMastered's existing semantics already provide exactly this, reuse it; if it filters mastered phrases OUT entirely, do not reuse it for this — implement start-position logic instead, and report the distinction.
Edge cases: all phrases mastered (start at the first phrase as today — a deliberate review visit), zero attempts (unchanged behavior), and the teaser/free-taste station (must not regress the teaser flow — teaser serves a fixed 3-phrase set; start-position logic must be inert there).
Journey station links themselves stay unchanged unless Step 0 shows the fix belongs in the link (a param) rather than the practice loader — choose whichever is cleaner for the later mobile port and report the choice, since the parity sync will mirror it.

Constraints: this task owns the web practice loader logic for group mode and, only if needed, journey.tsx's station hrefs — check no active task owns those files before editing; if #948 (progression completion) is touching practice or journey surfaces, STOP and report the collision instead of proceeding. Tests: session starts at first unmastered phrase (seeded mid-group progress), all-mastered starts at first, teaser station unaffected, back-navigation to mastered phrases works. Enumerate test counts before and after (api-server gate per whichever regime #947 has left in place — check CODEBASE-FACTS). No em dashes. Report with file paths and web-verified status.