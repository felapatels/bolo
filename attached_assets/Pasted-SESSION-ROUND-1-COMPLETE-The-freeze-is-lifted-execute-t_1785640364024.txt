SESSION ROUND 1 COMPLETE. The freeze is lifted; execute the post-session queue with these updates:

STANDING CHANGES (supersede earlier "temporary" framing):
A. CAPTURE MODE IS PERMANENT SCAFFOLDING. Do not remove it. It stays dev-only, param-gated, server-checked by allowlist, exactly as built: I will run additional language rounds over time. Update the CODEBASE-FACTS entry to remove the "temporary/remove after corpus" note and mark it a standing pilot tool.
B. ALLOWLIST SHRINKS TO OWNER ONLY: set PILOT_CAPTURE_USER_IDS to exactly two IDs: user_3H9XG2UWijYqRUXv6Ls0geMmX4Z (original dev) and user_3HBsumTkU4xtAYmMlH62DKnC5j9 (yahoo dev). Remove the three tester IDs (pikaboyliam, lilrylan, kruti818). Their entitlements can stay as-is (harmless, 90-day expiry). This takes effect at the STEP 3 restart below; their clips from tonight are already captured and unaffected.

HARVEST AMENDMENTS (apply before running the harvest):
1. Clips with captureMode: true use their sidecar labels directly (authoritative); order-reconstruction applies ONLY to pre-capture-mode clips from earlier tonight.
2. For pre-capture-mode clips: exclude all clips from user_3HBsumTkU4xtAYmMlH62DKnC5j9 with sidecar timestamps before 2026-08-02T02:10:00Z (owner warm-up, including a fumbled namaste); list under discarded with reason owner_warmup.
3. Pre-capture-mode fumbles, discard the affected quads: Kruti (kruti818), Hindi, maaf kijiye; Liam (pikaboyliam), Hindi, dhanyavaad AND maaf kijiye. Quads truncated at 3 attempts by the old result-card mis-tap will appear as 3-member groups: label positions 1-3, position 4 missing.
4. Sidecar-discarded clips (the capture-mode redo button) are excluded automatically; keep them listed under discarded.
5. Recordings never enter the repo: R2 plus gitignored local cache.

QUEUE:
STEP 1: Commit anything still uncommitted from tonight's fix blocks (the capture-mode commit 33c22d0 is in; verify the tree is otherwise clean and commit any stragglers). Tripwire, typecheck, post hashes.
STEP 2: Apply the #998 fix exactly as drafted and approved: verify-time clamp in evaluationToken.ts (exported HONESTY_SCORE_CAP = 92, nocatch passthrough, audioJudged exemption comment), the three pin tests, replace the two literal 92s in openai.ts with the constant, and the data repair UPDATE for attempt 14097 only. Run the api suite; post the hash and the new canonical count.
STEP 3: Update PILOT_CAPTURE_USER_IDS per change B, restart the api-server once, and verify: live environ contains exactly the two owner IDs, and one realistic-size tee probe (with a capture-mode label field) confirms the tee still works. 
STEP 4: Run the harvest with the amendments. Post the manifest summary: labeled count by source (explicit-label vs order-reconstructed), per-tester per-language quad counts, needsReview groups, discarded count, missingClips.
STEP 5: Report and STOP. Calibration harness, scoring v2 build, #997 release, and the token slice each wait for explicit release. The full web+api gate runs at the end of the next implementation block per the cost rule.