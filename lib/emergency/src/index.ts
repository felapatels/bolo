/**
 * The Emergency: an interruption between two stops, and the pressure game
 * behind it.
 *
 * Pure. No React, no DOM, no database. Web, iOS and Android share no
 * components, so the RULES live here and only the rendering differs, which is
 * the same arrangement `lib/story` and `lib/script-trace` already use.
 */
export * from "./drill";
export * from "./stops";
export * from "./films";
