/**
 * Generated by orval v8.20.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import type { ChachaEncounterOffer } from './chachaEncounterOffer';
import type { ChachaEncounterPhrase } from './chachaEncounterPhrase';

export interface ChachaEncounterResult {
  station: number;
  /** Which encounter of the journey this is, 1-based. */
  ordinal: number;
  /** True only when this call paid the Chai. The celebration plays on this and nothing else, so a revisit stays quiet. */
  granted: boolean;
  chaiGranted: number;
  /** Chai balance after the gift. */
  balance: number;
  /** His phone rings at this stop. The server decides, not the client: true at one encounter station per zone (zone 1 fixed at station 3, later zones chosen by a hash of learner, language and zone so a revisit meets it at the same stop). The call is an interruption that replaces the walk into practice; it never delays it. */
  callsNow: boolean;
  phrase?: ChachaEncounterPhrase | null;
  offer?: ChachaEncounterOffer | null;
}
