// Wrong Platform, Part 2. All-Access.
//
// Same file, same rig, different dial: six cards instead of four, eight rounds
// instead of six, the stray drawn from a NEIGHBOURING topic rather than a
// distant one, and no English on the cards. See wrong-platform.tsx for why
// hiding the English is the change that matters: with it showing, the round is
// answerable without reading any of the language.
import { makeWrongPlatformScreen } from './wrong-platform';

export default makeWrongPlatformScreen(2);
