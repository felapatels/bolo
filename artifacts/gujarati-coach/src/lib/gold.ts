/**
 * THE APP'S GOLD, ON WEB. Mobile has it as a theme colour (constants/colors.ts,
 * `gold: '#F59E0B'` in light); web's theme has no gold token, and the first
 * two ports of build 22's screens each typed the hex again. Three copies of a
 * colour is how two golds end up a shade apart, so it lives here once.
 *
 * Gold on a stat icon or a ticket edge is the WORLD's gold (the colour law:
 * gold is the world, purple is touch me, green is done). Gold as STATUS
 * somebody paid for is GoldChip's gradient, deliberately a different thing.
 */
export const GOLD = "#F59E0B";
