# September 12 release backup

Read BOLO-MASTER-HANDOFF-2026-09-12.md first. Receipts are snapshots, not continuously updated state. Older status entries are superseded by later entries.

Scripts document work already performed. Do not blindly rerun mutation scripts: inspect current Apple state first. Never submit reviews; owner retains final submission. The API helper requires ASC_KEY_ID, ASC_ISSUER_ID and ASC_PRIVATE_KEY_PATH environment variables; the private key is not included.

TryBolo site source is backed up separately at https://github.com/felapatels/trybolo. Shared supervisor files here are a dated snapshot; the live working originals remain in ~/bolo-supervisor.
