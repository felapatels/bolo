# BOLO Codebase Facts

Living reference. Paste the relevant sections at the top of every spec so the agent does not re-derive them.

Last updated after: Spec 0 Task 1, Spec 0 Task 2, Spec 1a, Spec 1 v3, the band-derivation follow-ups, Spec D2 Step 0, Task 787 (self-managed Clerk migration), the observability pass (Sentry + PostHog, July 28, 2026; DSNs and keys wired into the development environment the same day), the mobile visual polish pass (July 29, 2026), Task 818 (promotional Plus grants, July 29, 2026 — see the RevenueCat reconcile row in section 8), Task 824 (Free-card teaser copy refresh, July 29, 2026 — see section 5 web monetization surfaces), the build-26-lite release ops (July 29, 2026 — see the RevenueCat rows in section 8), Task 840 (animated game card previews on the web /games hub, July 29, 2026 — see section 5), Task 839 (landing page refresh, July 29, 2026 — see section 5), Task 838 (rigged Bolo in the web bottom nav, July 29, 2026 — see section 5), Task 848 (chat mic-grant hold-confirmation fix, July 30, 2026 — see the section 5 chat.tsx row and the practice.tsx debt row in section 8), and Task 849 (Free daily lesson limit retired additively, July 30, 2026 — `dailyNewLessonLimit("free")` now returns null so every tier is unlimited; the `daily_lesson_limit` 402 stays documented in openapi.yaml but is never emitted; `FREE_DAILY_NEW_LESSON_CAP`, `dailyLessonCapDenial`, `recordLessonGeneration`, `countLessonGenerationsToday`, and the `lesson_generations` table are all intact and still recording; clients unchanged — meters hide themselves via the existing limit-null contract; see the two debt rows in section 8), and the July 30, 2026 home cleanup (the "Start Daily Practice!" CTA card removed from web home.tsx; the daily-goal toast is unaffected — it reads `attemptsToday` from the progress summary; new high-priority unlock-filtering debt row in section 8), and the server-side lesson-group unlock filtering (July 30, 2026, approved D2 spec — shared guard `lib/lessonGroupAccess.ts`, see section 4 and the RESOLVED debt row in section 8; the #782 baseline row grew to 16 the same day from the unrelated `bf12678` sentence-topups data lag), and the home stats-banner failure-state hardening (July 30, 2026 — reported "banner missing" regression was NOT reproducible in five browser permutations; the empty-shell-on-summary-failure presentation introduced by the spinner fix now shows visible retry feedback; see the section 5 home.tsx row), and the #854 unlock-guard hardening tests + the curated-lessons validator fix (July 30, 2026 — Task #853 resolved as a validation-layer bug, not missing content; `seedData.test.ts` and `backfillPremiumPhrases.ts` now validate through `curatedLessonsWithC1`; the #782 baseline is back to 14 — see the section 8 row), and Spec B2 (phrase report button, July 30, 2026 — `POST /phrases/:id/report`, `phrase_reports` table via migration 0031, 20/rolling-hour silent cap; see the section 2 table entry, the section 4 route row, and the new `/contact` in-memory-limiter debt row in section 8), and Spec B1 (language selection onboarding, July 30, 2026 — `users.has_chosen_language` via migration 0032, one-way `hasChosenLanguage` on PATCH /account/preferences, `communityReviewed` on GET /languages, and the web `/choose-language` step; see sections 2, 4, 5, and 10), and Task 857 (needs_client_trust handling in mobile sign-in, July 30, 2026 — see the section 6 Client Trust paragraph and the build-27 release-checklist row in section 8), and the July 30, 2026 build-27 wrap-up (real Client Trust flow verified pre-ship via `qa/client-trust-probe.mjs` — see the section 8 checklist row; iOS buildNumber bumped to 27; B1 skip path browser-verified via `qa/b1-skip-probe.mjs`, which caught and led to the fix of a fresh-account tour/gate navigation race — see the section 5 tour-auto-launcher row), and Task 865 (#859 rebase-merge verification + repair, July 30, 2026 — the rebase DROPPED the 402-fix content (86ae84f) and kept the stale UTC-hardcode choose-language test fixture; both restored by content: react-query no longer auto-retries ANY 4xx (`gujarati-coach/src/lib/queryClient.ts`, network/5xx keep 3 retries, manual refetch untouched), home's stats banner renders a showroom/upgrade overlay (Preview the journey → `/journey` + Unlock → paywall) for a locked-language `upgrade_required` (language_locked / teaser_exhausted) instead of the error-retry shell, and the choose-language test fixture resolves the device timezone via `Intl` so the provider's auto-report never fires in any test-runner TZ; see the section 5 home.tsx row), and Task 860 (web test baseline fixed to zero, July 30, 2026 — all 8 documented pre-existing web failures resolved as stale tests/harness bugs, no product changes beyond a test-only `VoiceCard` export; the full gujarati-coach suite is green, see the two RESOLVED rows in section 8), and Task 895 (chat reply voice matched to greetings, July 30, 2026: `BOLO_MINI_TTS_VOICE` in `artifacts/api-server/src/lib/parrotChat.ts` changed "sage" to "nova" because sage measured 15 to 17 dB quieter at source via ffmpeg volumedetect, producing loud greetings and quiet chat replies on device; both the buffered `boloTTSMini` and streaming `boloTTSMiniStream` paths use the constant; `PHRASE_AUDIO_DEFAULT_VOICE` in `lib/ttsConfig.ts` and `BOLO_MINI_TTS_VOICE` are now exported and a divergence-guard test in `parrotChat.test.ts` pins them equal; `BOLO_GPT_AUDIO_VOICE` "shimmer" is untouched, the two constants must stay separate because gpt-audio and gpt-4o-mini-tts have different voice sets; audited: chat replies never read or write `tts_cache` (only `ttsPrewarm.ts` and `routes/openai.ts` touch it, and the /openai/tts VOICES allowlist never included sage, so no cache entry can pin sage); api-server suite 628 tests, 614 pass, 14 fail, exactly the documented #782 baseline in section 8), and Task 900 (free-taste phrases on the journey taste station, July 31, 2026: server-only fix in `artifacts/api-server/src/routes/learning.ts`; `GET /lesson-groups/:id/phrases` now carries the same M1 teaser exception as the category-phrases route, so a teaser-state caller requesting the designated taste group gets the teaser rows instead of a 402; see the section 4 paragraph under the D1b showroom entry; api-server suite 632 tests, 618 pass, 14 fail, exactly the documented #782 baseline in section 8), and Task 899 (web train size bump + boarding-pass stub tear, July 31, 2026: the redrawn train renders ~16-18% larger through new :root tuning vars in `gujarati-coach/src/index.css` (`--train-ticket-h` 47px / `--train-ticket-h-lg` 65px on the home pass, was h-10/lg:h-14; `--train-marker-w` 37px inside the journey rail-marker pill, whose pill grew w-10 h-7 to w-[46px] h-8); measured before/after via `qa/task899-tear-probe.mjs`: home mobile 61x40 to 72x47, desktop 87x57 to 102x67, marker drawing 32x21 to 37x24, text-block gap unchanged at 12px, no marker overflow, no detail smearing at 2x. Activating the home pass now plays a both-halves tear: `handlePassActivate` in home.tsx preventDefaults, applies `.animate-stub-tear` to the fare-zone stub and `.animate-body-tear` to the ticket body, then `navigate("/journey")` after `--tear-nav-delay` parsed from CSS in ms (JS fallback 500ms). Navigation is never blocked: reduced motion returns early for instant native Link navigation, any animation-path error falls through to an immediate navigate(), and keyboard Enter rides the same click path so it gets the tear too. User-tuned tear look over three feedback rounds (final): both halves pull away from each other along the perforation, each carrying its own accent stock, rounded outer corners, a STATIC jagged clip-path rip edge, and a STATIC drop-shadow filter (box-shadow is useless there: clip-path clips it; the animation itself stays transforms/opacity only); during the tear the Link container goes transparent and drops overflow-hidden/shadow while TicketStripes, shimmer, blur-circle, perforation, and pass-glow are all hidden, so the widening gap shows the ACTUAL page behind the pass and the ticket reads as floating pieces (round 2's translucent-black recess and round 1's page-colored underlay strip were both rejected on the way here). `--tear-nav-delay` is deliberately 500ms, above the original ~250ms plan budget, per two explicit user requests for a longer visible tear. All tear timing/rotation/distance/drop vars live in the :root tuning constants block. The journey header ticket is untouched (tear classes only ever applied in home.tsx). Web suite 252 to 255 tests, all green; 3 new pins in `home-boarding-pass.test.tsx` (tear-plus-navigation, reduced-motion instant nav, getComputedStyle-failure fallback); mid-tear screenshot captured at `qa/shots/task899/mid-tear.png`), and Task 917 (directional pulse on the web journey map's active rail segment, July 31, 2026: `journey.tsx` samples `PULSE_DOTS_PER_SEG` (8) accent dots per segment directly on the rail beziers (same control points as the `segs` `d` strings) along the run from the current stop to the NEXT STATION in path order; a zone-boundary run spans two segments (station to postcard, postcard to station) with one continuous dot order, and no next station (final stop before the terminus, journey complete, showroom with no boardable stop) yields zero dots, so nothing ever pulses toward the terminus and a LOCKED next stop still pulses (test-out invitation). Mechanism is the tie-mark opacity wave, NOT mobile's dash-offset loop (web constraint: transforms/opacity only): each dot carries an inline `--rail-pulse-delay` fraction growing toward the next stop, and the `.rail-pulse-dot` CSS animation (base opacity 0, no fill mode, keyframes `rail-pulse`) phases on it, so the wave direction is correct on every serpentine orientation by construction and the delays are assertable in jsdom. Constants `--rail-pulse-cycle` 3.4s / `--rail-pulse-min` 0 / `--rail-pulse-peak` 0.85 joined the `:root` tuning block; reduced motion renders no dots at all (component gate via `useReducedMotion`) with the CSS reset as backstop. Web suite 310 to 315 tests via new `journey-rail-pulse.test.tsx` (5 pins: active-segment-only count, locked next stop, two-segment zone-boundary run with strictly increasing delays, no-next-station zero dots, journey-complete zero dots, reduced-motion zero dots); live frames at `qa/shots/task917/`; see the RESOLVED web-rail-pulse row in section 8).

**Maintenance rule:** after every completed task, append what changed. Anything in here that turns out to be wrong is worth more than the correction itself, because it means a spec was written against it.

Last updated after: Task 952 (pilot-capture tee, July 31, 2026: `@aws-sdk/client-s3` added as api-server dep; `artifacts/api-server/src/lib/r2Client.ts` exports `getR2Client()` (null when any of R2_ACCOUNT_ID/R2_ACCESS_KEY_ID/R2_SECRET_ACCESS_KEY/R2_BUCKET_NAME is absent); `artifacts/api-server/src/lib/pilotCapture.ts` exports `teeAudioToPilot(rawBuffer, opts)` and `pilotCaptureUserIds` Set (parsed from PILOT_CAPTURE_USER_IDS comma-separated env var at module init, empty → feature inert); `POST /openai/pronunciation` tees audio after res.json at the fast-path exit and the LLM-path exit — never on nocatch/empty/early-error paths; `rawBuffer` hoisted to `let rawBuffer: Buffer | null = null` before the STT try-catch so it is in scope at both exit points; 3 new tests in `openai.pronunciation.pilot-capture.test.ts` (unlisted-user safety, listed-user 2× PutObjectCommand + sidecar shape, R2-throw → resolves); `qa/pilot-pronunciation-v2.mjs` gains `--r2` flag (mutually exclusive with `--video`, requires `--lang`): lists `pilot-clips/{lang}/`, downloads .m4a + .json sidecar, converts to MP3 via ffmpeg, runs audioEnsemble, applies criterion 2 stability check, prints R2-mode report with raw-score column only (no proposed-band); `resendClient.ts` `reply_to` → `replyTo` (pre-existing type error fixed in passing); api-server suite 657 tests, 643 pass, 14 fail — exactly the #782 enumerated baseline).

Last updated after: Task 950 (contact form reply_to test, July 31, 2026: new `artifacts/api-server/src/lib/resendClient.test.ts` pins three contracts on `sendContactNotification` — `reply_to` equals the submitter email, `to` uses `SUPPORT_INBOX_EMAIL` when set, `to` falls back to `LARKsupport@gmail.com` when unset; uses `await mock.module("resend", …)` + dynamic import pattern so the mock Resend class is wired before the lazy singleton is created; no product code changed), and Task 927 (Token Economy design spec, July 31, 2026 — see `docs/specs/token-economy.md`; no code changes; key findings: (1) consumable NON_SUBSCRIPTION_PURCHASE events would accidentally grant Plus via the existing concernedEntitlement null-fallback in revenuecatSync.ts — must add to IGNORED_EVENT_TYPES before any token SKUs go live; (2) daily lesson limit is dead code since July 30 and recommended for removal in Build 32; (3) advisory lock pattern for token spend = pg_advisory_xact_lock consistent with teaser and phraseReports patterns; (4) token balance = SUM(token_ledger.amount) - SUM(token_spend_ledger.cost), never a stored counter; Build 32 slice: ledger tables + earn hooks + Station Pause + Express Multiplier + RevenueCat NON_SUBSCRIPTION_PURCHASE handler + tokenBalance on /progress/summary + remove dead cap code; Build 33 slice: test-out retries + Streak Repair + All-Access monthly grant + web/mobile surfaces), and Task 940 (account deletion purge, July 31, 2026: DELETE /account must explicitly delete every user-keyed table before the `users` row or the FK delete fails; covered tables now: family_seats/plans, chat_turns, friend_invites, attempts, badges, lesson_generations, friendships, xp_ledger, user_ability, user_item_memory, phrase_reports, daily_quiz_completions, game_sessions, lesson_group_progress, lesson_group_testouts, script_trace_progress, contact_submissions; `token_ledger`/`token_spend_ledger` TODO for build-32; rule: audit `lib/db/src/schema/` for `references.*usersTable` whenever adding a schema to keep the handler complete; deletion test seeds all 14 user-keyed tables to catch any future omission)

**Section numbering is stable.** Specs reference section 9 for working rules. Do not renumber.

---

## 1. Repository layout

Monorepo, pnpm workspace, root at `/home/runner/workspace`.

| Path | What it is |
|---|---|
| `artifacts/api-server` | Node/Express API |
| `artifacts/gujarati-coach` | React web app |
| `artifacts/bolo-mobile` | Expo React Native app, expo-router |
| `artifacts/bolo-launch-video` | Marketing asset: programmatic launch video (React/Framer Motion, video-js scaffold). Not product code; nothing in the main apps depends on it |
| `artifacts/bolo-social-clips` | Marketing asset: short social clips, same video-js scaffold. Not product code; no main-app dependencies |
| `artifacts/mockup-sandbox` | Internal design tooling: vite preview server for canvas component mockups. Never shipped; no main-app dependencies |
| `lib/db/src` | Drizzle schema |
| `lib/db/drizzle/` | Migrations and `_journal.json` |
| `lib/api-spec/openapi.yaml` | Schema source of truth |
| `lib/api-client-react/src/generated/api.schemas.ts` | Generated client types |
| `lib/api-zod/src/generated/types/` | Generated Zod types |
| `lib/integrations-openai-ai-react/src/audio/` | Web audio recording hook |

**Stack:** Postgres, Drizzle ORM with drizzle-kit migrate, Clerk auth (self-managed; dev instance `free-bedbug-6.clerk.accounts.dev`, production instance on `clerk.bolo-india.app` with CNAME DNS verified and Apple + Google SSO custom credentials), OpenAI direct key (Whisper STT, chat, TTS), ts-fsrs v5, expo-audio on mobile.

**Clerk key locations (dev and prod never share instances):** dev = workspace secrets `CLERK_PUBLISHABLE_KEY` / `VITE_CLERK_PUBLISHABLE_KEY` / `CLERK_SECRET_KEY` (pk_test/sk_test, free-bedbug-6). Production = Replit production environment vars `CLERK_PUBLISHABLE_KEY` and `VITE_CLERK_PUBLISHABLE_KEY` (both `pk_live_Y2xlcmsuYm9sby1pbmRpYS5hcHAk`, set) plus `CLERK_SECRET_KEY` sk_live as a deployment secret (owner sets it in the Publishing tool's deployment settings, then republishes). Mobile = EAS production env `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` (same pk_live, set). No proxy in production: CNAME custom domain and Clerk proxy are mutually exclusive; `VITE_CLERK_PROXY_URL` stays unset and the stale `EXPO_PUBLIC_CLERK_PROXY_URL` was deleted from EAS. `clerkProxyMiddleware` remains mounted but dormant (nothing routes to `/api/__clerk` when no client sets a proxyUrl). Note `publishableKeyFromHost` returns a dev fallback key when one is present, so the production env must carry live keys or the site would run against the dev instance.

**Never hand-edit** anything under `generated/` or `dist/`. Change `openapi.yaml` and regenerate.

---

## 2. Database schema

### Pre-existing

**users:** `id` text PK, `timezone` text nullable (IANA), `daily_goal` int default 50 (was 10 until migration 0025, July 28, 2026; existing rows untouched), `tz_grace_used_on` date nullable, `has_chosen_language` bool NOT NULL default false (Spec B1, migration `0032_brown_patch.sql`, July 30, 2026: backfill grandfathered rows with a non-null `active_language` to true; see the section 4 "Language choice signal" entry for the write rules)

**attempts:** `id` serial PK, `user_id`, `language_code`, `phrase_id` nullable, `native_script`, `romanized`, `english`, `transcript`, `score` int 0-100, `passed` bool, `feedback`, `created_at`, plus added in Task 1: `latency_ms`, `audio_duration_ms`, `band`, `fsrs_rating`, `theta_delta`, `beta_delta`, `xp_awarded`, `flags` jsonb

**phrases** (`lib/db/src/schema/phrases.ts`): `language_code`, `category_id`, `difficulty` int, `premium` bool, `stage` text (`phrase` or `sentence`), `hint`, `sort_order`, plus Task 1 additions `accepted_answers` jsonb, `elo_difficulty`, `elo_difficulty_rd`, `exposure_count`, plus Spec D2's `register` text nullable (`formal` / `colloquial` / `code_switched`, migration `0021_spec_d2_register.sql`) with index `phrases_language_register_idx` on `(language_code, register)`. All rows currently NULL; no authoring or filtering yet. Plus C1's `source` text nullable (migration `0028_messy_arachne.sql`, July 29, 2026): provenance copied from the seed entry's `origin` field by the seeder (`curated` or `generated_c1`); runtime-inserted rows (lesson generation, replenisher) and rows that predate the column stay NULL. QA passes target `source='generated_c1'` precisely.

**game_sessions:** `id`, `user_id`, `language_code`, `game`, `correct_count`, `total_count`, `xp_awarded`, `context`, `created_at`. Game values: `word-match`, `speed-round`, `listen-and-pick`, `phrase-builder`, `daily-quiz`, `script-trace`

**daily_quiz_completions:** `id`, `user_id`, `language_code`, `quiz_date` date, `score` 0-5, `xp_awarded`, `completed_at`

**script_trace_progress:** `id`, `user_id`, `chapter`, `character_id`, `passed`, `best_score`, `attempt_count`, `updated_at`

**badges:** `id`, `user_id`, `language_code`, `badge_key`, `earned_at`

**languages:** `code` text PK, `name`, `native_name`, `script`, `font_family`, `rtl` bool, `sort_order` int, plus `speech_capability` text default `'supported'` (migration `0022_speech_capability.sql`; see section 4 "Speech capability")

**phrase_reports** (Spec B2, migration `0031_groovy_sebastian_shaw.sql`, July 30, 2026): `id` serial PK, `user_id` FK users, `phrase_id` int FK phrases, `reason` text (`translation_wrong`/`transliteration_wrong`/`audio_wrong`/`other` — text, not a pg enum, so new reasons are a code change), `note` text nullable (≤280 enforced in the route), `language_code` FK languages + `stage` — both DERIVED server-side from the phrase row at write time (never client-supplied), `created_at` timestamptz. Indexes: `(user_id, created_at)` serves the rolling-hour throttle; `(phrase_id)` serves review-time joins (incl. against `phrases.source` provenance). Append-only; duplicate (user, phrase) reports allowed by design

### Added in Task 1 (migration `0020_scoring_core_v2.sql`)

**user_item_memory:** `user_id`, `phrase_id`, `stability`, `difficulty`, `state`, `reps`, `lapses`, `last_reviewed_at`, `due_at`. PK (user_id, phrase_id), index (user_id, due_at)

**user_ability:** `user_id`, `language_code`, `skill`, `theta`, `theta_rd`, `attempt_count`, `updated_at`. PK (user_id, language_code, skill)

**xp_ledger:** `id`, `user_id`, `attempt_id`, `game_session_id`, `source`, **`xp`**, `multiplier_reason`, `language_code`, `created_at`. Unique on `(user_id, source, ref_id)`

> The XP column is named **`xp`**, not `amount`.

---

## 3. Business rules and invariants


### Band thresholds (Spec 0 rule 40; five-band since Task 901, July 31, 2026)

Five bands, top to bottom: `perfect` (score >= 93), `great` (80-92), `good` (68-79), `almost` (55-67), `retry` (< 55). `nocatch` is set separately when audio is under 400 ms or transcription fails. The SINGLE config block is `artifacts/api-server/src/lib/scoreBands.ts` (`BAND_THRESHOLDS`, `BAND_LADDER`, `bandFromScore`, credit-group helpers, `normalizeBand`); clients mirror it in `gujarati-coach/src/components/ui/band-pill.tsx` and `bolo-mobile/lib/ui.ts`, pinned by the three sharedConstants contract suites. The 93 and 68 splits are display-only and marked TUNING PENDING; **80 and 55 are FROZEN** — they are the legacy nailed/close boundaries every behavioral consumer keys on.

**Band is derived from score only.** It was briefly derived from `passed`, which allowed an LLM-returned boolean to inflate it. Fixed. Do not reintroduce.

`passed` is `score >= 80` server-side, so `passed` and "full-credit band" (`perfect`|`great`) coincide, but they are computed independently. Mastery-related rules that read "band nailed / score >= 80" are, in five-band terms, "at least Great" — behavior unchanged.

**Credit groups (behavioral, frozen):** full credit = `perfect`|`great` (== legacy `nailed`): full XP, Elo 1.0, FSRS Good/Easy, test-out counts it. Half credit = `good`|`almost` (== legacy `close`): half XP factor, Elo 0.5, FSRS Hard. Behavioral code must use `isFullCreditBand`/`isHalfCreditBand`/`isPassingBand`, never string-compare individual band names.

**Legacy stored rows:** attempts written before Task 901 still hold `nailed`/`close` in the DB — there was NO data migration. Read paths normalize via `normalizeBand(band, score)` (exact re-derivation: legacy bands came from the same score field with the same frozen edges); `SPEAKING_STREAK_QUALIFYING_BANDS` also accepts the legacy names directly.

### Band treatment (must stay consistent across practice and review)

| Band | Mascot pose | Haptic | Sound cue | XP arc | Shake | Confetti |
|---|---|---|---|---|---|---|
| perfect | `cheer` | Success | `correct` | yes | no | yes (only band) |
| great | `cheer` | Success | `correct` | yes | no | no |
| good | `thumbsup` | Light impact | none | yes | no | no |
| almost | `thumbsup` | Light impact | none | yes | no | no |
| retry | `tryagain` | Warning | `wrong` | no | yes | no |
| nocatch | `thinking` | none | none | no | no | no |

The result card on both platforms renders the five-band **ladder** (`BandLadder` — web `ui/band-ladder.tsx`, mobile `components/BandLadder.tsx`): all five labels (Perfect/Great/Good/Almost/Try again) with the achieved band filled in its brand color (perfect=success, great=accent, good=primary, almost=mutedForeground, retry=destructive) and the rest muted; labels only, never a numeric score. `nocatch` never shows the ladder — it keeps its neutral `BandPill` ("Didn't catch that"), now muted instead of destructive (rule 16 alignment, deliberate Task 901 change; the mobile review flash is likewise neutral gray for nocatch).

`good`/`almost` are partial successes, not failures (worth 0.5 in Elo, Hard in FSRS, 0.6 XP band factor).

`nocatch` is a **system failure to hear**, not a learner error. It gets no negative treatment of any kind. This supersedes the earlier treatment where it shared `retry`'s handling.

Build 30 batch 1 (July 30, 2026) extended nocatch neutrality in both directions: the server skips badge awarding entirely for nocatch attempts (`learning.ts` POST /attempts wraps the award block in `if (!isNocatch)`; the attempt row still persists, `newlyEarnedBadges` is always an array), and the mobile practice full-bleed score flash skips nocatch (no color assigned, no opacity pulse). Nothing negative AND no rewards. Also from that batch: on a **retry-band** result card (mobile) the CTA layout flips, "Try again" becomes the primary chunky button (`try-again-button`) and "Next phrase"/"Finish" a bordered secondary (`next-secondary-button`); all other bands keep the original icon-retry + primary-next layout. The ladder's retry rung (and formerly the band pill) also reads "Try again", so tests must address the button by testID (mobile) or role (web).

### Session end

Session confetti and the `session_complete` cue share one gate: `good * 2 >= total`, where good counts phrases ending at a passing band (`isPassingBand`, legacy nailed|close). A weak session gets no confetti and no celebratory sound. "PERFECT SESSION" = every phrase ended full-credit (unchanged math, legacy all-nailed).

Summary confetti renders language glyphs, capped at 40 on web and 25 on mobile. Per-phrase confetti stays shapes at 70 web / 44 mobile.

### Scoring

- **FSRS:** ts-fsrs v5, default parameters, desired retention 0.90. State materialized in `user_item_memory`.
- **Mastery:** `stability >= 21 days AND last attempt correct`. Reversible. No permanent graduation.
- **Elo:** Rasch logit scale. `K = 0.4 / (1 + 0.05 * n)`. Observed score 1.0 full-credit (perfect|great), 0.5 half-credit (good|almost), 0.0 retry.
- **XP:** `10 * band_factor * difficulty_multiplier * review_multiplier * decay_multiplier`. Band factor 1.0/0.6/0.0. Difficulty `clamp(1 + 0.5*(beta - theta), 0.25, 2.0)`. Review 1.5 when due. Decay 0.1 when stability > 60 days and not due.
- **Game XP:** word-match 15, listen-and-pick 15, phrase-builder 20, speed-round 25, plus 10 when accuracy >= 80%.
- **Daily quiz XP:** 10 per correct, plus 20 for a perfect 5/5. Max 70.
- **Script Trace XP:** flat 30 per chapter.
- **XP total** is always the sum of `xp_ledger.xp`. Never a stored counter.

### Streaks

All streaks use `localDayKey()` and `previousDayKey()` from `progressMetrics.ts`, in the user's IANA timezone. General and quiz streaks share one implementation and cannot drift.

A day with no activity yet falls back to yesterday, so a streak does not appear broken mid-day. This is correct; do not "fix" it.

**Speaking streak (Spec D2):** derived at query time by `computeSpeakingStreakDays()` in `progressMetrics.ts` — filters attempts to passing bands (`SPEAKING_STREAK_QUALIFYING_BANDS`: perfect/great/good/almost plus legacy stored nailed/close) and delegates to `computeStreakDays`, so bucketing and the mid-day fallback can never drift. Surfaced as optional `speakingStreakDays` on `GET /progress/summary`; shown as a Mic stat on web home and mobile progress. No new table or column backs it.

### Architectural conventions

- **Derive, do not store.** Three deliberate exceptions: FSRS state (path dependent), Elo item difficulty (cross-user), and `xp_ledger` (depends on ability and difficulty at time of attempt).
- **One date-bucketing implementation.** `localDayKey()`. Never write a second.
- **One reduced-motion check.** `prefersReducedMotion()` from `motionPrefs.ts`. Never write a second.
- **`evaluationToken` is never decoded on the client.** Signed server authorization material, passed through opaquely. `band` and `xpAwarded` are top-level response fields for display.
- **Generated Zod schemas are non-strict.** Adding response fields is safe for older clients.
- **New API response fields are always optional**, for Expo back-compat with installed builds.
- **No em dashes in any output.**

---

## 4. API server

`artifacts/api-server/src/`

| File | Contains |
|---|---|
| `routes/openai.ts` | `POST /openai/pronunciation`. Line 595 latency guard, 969 LLM `passed` fallback, 993 band derivation |
| `routes/learning.ts` | `POST /attempts`, `GET /review/phrases`, `GET /progress/summary` (1273-1326), `GET /categories`, `POST /game-sessions`, `/attempts/recent` |
| `lib/lessonGroupAccess.ts` | Shared sequential-unlock guard (July 30, 2026): `getUnlockedGroupIds(userId, categoryId, languageCode, {stats?})` = load context + derive + completion latch (idempotent write-through, so phrase routes latch too). Call sites: category phrases GET, category sentences GET (cached path; freshly generated rows are NULL-group = servable), `/lesson-groups/:id/phrases` (locked → 403 `{error:"lesson_group_locked",groupId,status:"locked"}`, never 402), and the journey listing (via `loadGroupUnlockContext` + `deriveAndLatchUnlock`; showroom branch loads context but never derives/latches). Per-phrase rule `isPhraseServable`: unlocked ∨ NULL group ∨ prior attempt (retake exemption — Retake deep-links resolve against the category list). CALLER CONTRACT: entitlement gates run BEFORE the guard. Deliberately UNFILTERED: test-out routes (they exist to sample locked groups — the skip-ahead mechanism), `/review/phrases` (attempted-only by construction), chat seed words, and daily-quiz sampling (approved decision: not practice surfaces). Tests: `routes/learning.unlock-filtering.test.ts` (incl. #854, July 30, 2026: concurrent-derive race → exactly one latch row via the `(user_id, lesson_group_id)` PK upsert, and a non-vacuous teaser/showroom no-latch test that builds a real Greetings teaser group for the test language — reset `__resetTeaserCacheForTests()` when creating throwaway teaser sets) |
| `routes/phraseReports.ts` | `POST /phrases/:id/report` (Spec B2, July 30, 2026): authenticated (mounted behind requireAuth in `routes/index.ts`) phrase-incorrectness reports → `phrase_reports`. Client sends ONLY `reason` + optional `note` (≤280); `language_code`/`stage` derived from the phrase row. Cap: 20 stored reports per user per rolling hour, counted from `phrase_reports` itself (DB-backed like the test-out throttle) — over-limit returns a SILENT `200 {success:true}` and stores nothing (unlike test-out's 429). 400 invalid reason/note/id, 404 unknown phrase. Clients are fire-and-forget with an optimistic "Thanks, we'll check it" toast, failures silent, no practice-flow changes: web `components/phrase-report.tsx` (popover on the practice card, quiet flag next to the play/text row), mobile `components/PhraseReportButton.tsx` (modal sheet next to the "Hear it" button; ships with build 27, no dedicated build). Tests: `routes/phraseReports.test.ts` |
| `routes/games.ts` | Daily quiz, script trace, `computeXp`, `computeQuizStreak` |
| `lib/progressMetrics.ts` | `computeProgressMetrics`, `buildPhraseStats`, `computeStreakDays`, `computeDailyQuizStreak`, `localDayKey`, `previousDayKey` |
| `lib/pronunciationGuards.ts` | `applyScoreGuards`, `normalizeLatin`, `normalizeNative`, `simToScore`. Lines 230/245/274 `passed >= 80`, 266 similarity guard |
| `lib/fsrsScheduler.ts` | `scoreAndBandToRating`, `applyFsrsRating`, `rowToCard` |
| `lib/xpEngine.ts` | XP math, `readLedgerXp` |
| `lib/evaluationToken.ts` | `EvaluationClaims`, `signEvaluation`. Line 116 |
| `lib/feedbackTts.ts` | Eval-time feedback TTS prewarm (Task 903, July 31, 2026): every `/openai/pronunciation` response path fires `prewarmFeedbackTts(feedback, tip)` fire-and-forget — synthesizes the exact spoken-feedback string clients build (`[feedback, tip].filter(Boolean).join(" ")`, pinned by `feedbackSpokenText`) into ttsCache under the default phrase-audio identity (empty language key). An in-flight pending map (`registerPendingFeedbackSynthesis`/`getPendingFeedbackSynthesis`, 60s TTL) closes the race: `/openai/tts` on cache miss JOINS a pending prewarm instead of synthesizing twice (logs `hit: "pending-prewarm"`; falls through to fresh synthesis if the pending rejects). The cache row is written BEFORE the pending resolves. Prewarm is SKIPPED when `TTS_PROVIDER === "elevenlabs"` (per-user voice prefs make the client's cache key unpredictable — see §8). Tests: `lib/feedbackTts.test.ts` (unit + a real-router pending-join integration test) |
| `lib/backfillScoringV2.ts` | Idempotent startup backfill. Line 306 gate throw. `SCORING_V2_GATE_OVERRIDE=1` bypasses |

`index.ts` opens the port FIRST, then runs the startup pipeline (content seed, `runBackfillScoringV2()`, scope triggers, lesson-group backfill) behind `listen` (changed July 29, 2026: the C1 rollout seed blew the publish promote step's ~60 second port-open window when the pipeline ran before listen). Pipeline order is unchanged and load-bearing; a pipeline failure still exits the process. Startup probe path `/api/healthz` (in `artifact.toml`) returns 200 with no auth and no content dependency.

### Language access gating, three-state model (M1 teaser, July 29, 2026)

`lib/gating.ts` resolves locked-language access via `getLanguageAccess(req, lang)` into four states: `allowed` (plan covers the language), `teaser` (locked, but fewer than 3 distinct teaser phrases attempted), `exhausted` (locked, 3 distinct teaser phrases attempted, lifetime), and `locked` (locked language with no teaser set, for example test fixtures; behaves exactly like pre-M1). The teaser set is the first 3 phrase-stage phrases of the first Greetings lesson group per language, resolved and cached in `lib/teaser.ts` (`TEASER_LIMIT`, `getTeaserPhraseIds`, `countTeaserConsumed`; consumption is DERIVED from the attempts table, no new table). `denyLockedLanguage` is now async and takes `opts.teaserPhraseId` for id-aware exceptions on `GET /phrases/:id` and `POST /attempts`. Denials go through `sendLockedLanguageDenial`: `exhausted` sends reason `teaser_exhausted`; `teaser` and `exhausted` denials carry a `teaser: {consumed, limit}` object; `locked` payloads are byte-identical to pre-M1. Successful teaser responses carry the same `teaser` object on each phrase row and on the attempt result (all optional fields in openapi.yaml for mobile back-compat). Attempts consume a slot per DISTINCT phrase regardless of score. The web app shows a post-result UpgradeCard in practice when `consumed >= limit` and reason-keyed UpgradeScreen copy.

### D1b showroom exception on the lesson-groups listing (July 29, 2026)

`GET /categories/:id/lesson-groups/:lang` is a deliberate product exception to M1's 402-on-all-locked rule, decided in the D1b journey-map build (decision 3): for `teaser` and `exhausted` callers, and ONLY those two states, this one read-only route returns the full group structure instead of a 402, so the journey map can render as the paywall's showroom. The showroom response carries zone and station counts and statuses only, zero phrase content: every group is forced `status: "locked"` except, in teaser state only, the single group hosting the teaser set (marked `teaserStation: true`, `status: "unlocked"`). The envelope adds `access: "teaser" | "exhausted"` and `teaser: {consumed, limit}`. No completion-latch rows are written in showroom mode (no `lesson_group_progress` writes for a language the plan does not own). The `locked` state (language with no teaser set) still sends the pre-M1 402 byte-identically, and a test asserts that exact body, so the exception provably applies to teaser and exhausted callers only. This SUPERSEDES the M1 spec's original behavior for this one route; every other locked-language surface keeps the M1 402 contract, so the M1 write-up above and this entry do not contradict each other. Mobile reuses this contract when its map lands (build 26).

Sentence-stage gate on group phrases (July 29, 2026, code-review follow-up): `GET /lesson-groups/:id/phrases` now denies sentence-stage groups for callers without the `sentences` feature with the same 402 `feature_locked` payload as `/categories/:id/sentences/:lang`. The journey UI dialog-gates sentence stations client-side, but that is convenience, not authority: a deep link (`/practice/:id?group=<sentence group>`) hits the route directly, and before this gate a Free caller on an allowed language got a 200 (premium-filtered) instead of an authoritative denial. Boundary pinned in `learning.lesson-groups-showroom.test.ts` (402 for non-Plus with zero sentence text, 200 for Plus).

Taste-station phrases on the group route (Task 900, July 31, 2026): `GET /lesson-groups/:id/phrases` carries the same M1 teaser exception the category-phrases route implements. A teaser-state caller requesting the designated taste group (the group holding the teaser phrase set, the one the journey listing marks `teaserStation: true`) receives exactly the teaser phrase rows, each with the `teaser: {consumed, limit}` object, in the same per-phrase shape as the category-phrases teaser branch, instead of a 402. Before this fix the journey listing advertised the taste station as unlocked but this route applied a blanket `denyLockedLanguage`, so the advertised station 402d into the paywall. Exhausted callers and non-taste groups on a locked language keep the 402 byte-identical via `sendLockedLanguageDenial`, and entitled callers are byte-identical to before. The teaser branch returns BEFORE the sequential-unlock guard (`getUnlockedGroupIds` / `deriveAndLatchUnlock`), so no `lesson_group_progress` latch rows are ever written for a language the caller's plan does not own (the `lib/lessonGroupAccess.ts` CALLER CONTRACT). Boundary pinned in `routes/teaserGating.test.ts`, including a non-vacuous no-latch test that puts the taste group at the exact COMPLETION_RATIO threshold before the denied call, and a verification that `POST /attempts` accepts teaser phrases reached via this flow.

### Pronunciation pipeline (do not modify)

Whisper `speechToText` → retry at high quality if transcript empty or similarity <= 0.40 → fast path if phonetic similarity >= 0.93 using `simToScore` (0.90-1.00 maps to 80-100) → otherwise LLM rubric via `PRONUNCIATION_RUBRIC_PROMPT` → `applyScoreGuards`.

Guards include normalization, a wrong-phrase cap at 40 when a sibling phrase (up to 400 in the same language) beats the target at >= 0.80 similarity, and a Levenshtein floor/cap.

### Speech capability + script-mismatch guard (July 2026)

- `languages.speech_capability` (`supported` | `degraded` | `unsupported`, default `supported`, migration 0022) is server-authoritative, seeded from probe verdicts in `seedData.ts`, exposed as optional `speechCapability` on `GET /languages`. Verdicts: ks + sat degraded, mni + brx unsupported, rest supported.
- **Probe limitation:** verdicts come from `artifacts/api-server/scripts/probeSttLanguages.ts` using TTS-generated audio, so `supported` means "best case with clean synthetic speech". Re-probing with real human recordings is pending and likely infeasible in this environment; if human audio shows a language materially worse, downgrade its seed value.
- `/openai/pronunciation` short-circuits for `unsupported` languages BEFORE any STT call: returns band `nocatch`, xp 0, listen-record-compare copy. Clients never send evaluations for these languages, this is the backstop.
- `applyScoreGuards` guard ladder changed: the old `cross-script-cap` (cap 85) is replaced by universal `script-mismatch-nocatch` — a transcript in the wrong Unicode script (or a Latin transcript with romanized sim < 0.45 against a non-Latin target) proves the recognizer failed, and resolves to band `nocatch` (score 0, no XP, no streak break) in ALL languages. Latin transcripts with sim >= 0.45 stay scoreable; wrong-phrase-cap takes precedence.
- The STT language-code-rejected retry in `lib/integrations-openai-ai-server` is unchanged but now logs `[stt] language_code_rejected_retrying_without_hint` with the language code.
- `POST /attempts` treats band `nocatch` as a system miss end to end: the attempt row is inserted for analytics, but Elo theta is untouched (`thetaDelta` 0, no ability upsert), no FSRS rating/memory write, and no phrase exposure bump. This is the server-authoritative safeguard; clients in unsupported-language compare mode never submit attempts at all.
- Mobile auth offers Sign in with Apple (`components/AppleAuthButton.tsx`, Clerk `oauth_apple` via `useSSO`, same flow shape as `GoogleAuthButton.tsx`) on both `(auth)/sign-in.tsx` and `(auth)/sign-up.tsx`. iOS-only render (`Platform.OS !== 'ios'` returns null) — App Review 4.8 requirement, not needed on Android. Apple may supply a `privaterelay.appleid.com` email and supplies the user's name only on FIRST authorization; all name usages fall back (`user?.firstName ?? 'friend'` etc.), never assume email is the user's real mailbox. Web has no Apple button yet (intentional).
- Clients: web `practice.tsx` (also serves review mode) and mobile `practice/[id].tsx` + `review.tsx` read the active language's `speechCapability`. Degraded shows a one-time dismissible notice (key `bolo.approxNoticeSeen.<code>`, localStorage/AsyncStorage). Unsupported switches to a listen-record-compare stage (play target, hear yourself, no evaluation request, no band/XP).

### Language choice signal + provenance flag (Spec B1, July 30, 2026)

- `users.has_chosen_language` distinguishes an EXPLICIT language pick from the LanguageProvider's first-reconcile seed (which pushes the local default "hi" server-side and must never count as a choice). Exposed as optional `hasChosenLanguage` on the GET /account learning-prefs object. PATCH /account/preferences accepts ONLY `hasChosenLanguage: true`; `false` is a 400 ("a choice can't be unmade") — the flag is one-way. Writers that send true: the web `/choose-language` step, the language picker (both locked and unlocked branches — picker picks now persist server-side, previously local-only), and the account settings language control. A plain `activeLanguage` write without the flag leaves it untouched (that is the seed path).
- `GET /languages` gained optional `communityReviewed: boolean`, derived server-side in `routes/languages.ts` from a module-level Set built by `c1RolloutLanguageCodes()` (`lib/db/src/seedData.ts`, keys of the C1 rollout JSON). **PINNED SEMANTICS (July 30, 2026): `true` means "this language's extended sentence content is C1-LLM-generated and its community review is ONGOING" — it is the transparency-footer-note trigger, NOT a quality seal.** The name reads inverted; clients branching on it must treat `true` as "AI-generated, flaggable, under review" (the server-side Set is literally named `generatedContentLanguages`). `false` means the language did NOT ship C1-generated sentence top-ups, for either reason: hand-curated pilot (gu) or C1 rollout withdrawn (kok, ks, sat, brx, mni — these stay at the curated 8-sentence base). Live values verified against the running server July 30, 2026: exactly 16 of 22 true (as bn doi hi kn mai ml mr ne or pa sa sd ta te ur); false = brx gu kok ks mni sat. Not stored on the languages table — recompute follows the rollout file automatically. If a client ever needs a literal "vetted content" flag, add a NEW field; do not reinterpret this one.
- Tests: `routes/languageChoice.test.ts` (fresh-account default, seed-write leaves false, one-way 400, explicit pick sticks, communityReviewed derivation).

### normalizeNative

Strips ZWJ/ZWNJ, NFC normalizes, removes Devanagari nasal-plus-virama `[\u0919\u091E\u0923\u0928\u092E]\u094D`, drops anusvara and chandrabindu, strips non-letters, lowercases.

Known tradeoff: nasalized and non-nasalized forms collapse, so `हंस` and `हस` normalize identically. Accepted for a fuzzy pronunciation layer.

Devanagari only. Other scripts are #776, gated on native speaker review.

---

## 5. Web app (`artifacts/gujarati-coach`)

| Path | Notes |
|---|---|
| `src/pages/practice.tsx` | Line 40 band color comment, ~153 recorder hook use, 508 token passthrough, 512 optimistic todayXp, 846 mascot pose, 880 XpCounter session, ~780 session summary when `state === "done"`, 1001 mascot idle. Hold-to-speak hit zone (Task 882, July 30, 2026): a plain un-rounded `absolute inset-0` button covering the FULL rendered bird (the container box tracks the in-flow mascot img exactly) — not a belly-only inner box, and no border-radius (radius also clips pointer hit-testing). `qa/practice-mascot-probe.mjs` asserts touchable bounds ⊇ img bounds and that a head press starts recording |
| `src/pages/home.tsx` | `StatCell` in header, general streak with lucide `Flame` icon. Stats banner (July 30, 2026): shell always mounted with height reserved, cells `invisible` until `/progress/summary` resolves; on query ERROR with no (stale) data, a visible "Your stats couldn't load" + Try again overlay renders instead of a silent empty gradient shell. Pinned by `src/test/home-stats-banner.test.tsx` (data/pending/error states) and the live probe `qa/home-stats-banner-probe.mjs`. Cold-load launch moment (Task 902, July 31, 2026): while `loadingCats`, home renders `src/components/home-skeleton.tsx` (ticket-and-card-shaped pulse blocks; the old blocking `Loader2` spinner is gone) and, on a cold page load only, `src/components/brand-splash.tsx` overlays it — a 1.8s beat (canonical `mascot-wave.png` whole-image entrance, text wordmark, CSS-retinted `TrainEngine` slide) portaled to `document.body` (escapes `animate-content-enter`/`PageTransition` containing blocks), `pointer-events-none`, `z-[100]`. Lifecycle: `useBrandSplash(!loadingCats)` — module-level play-once latch + boot-path qualification (only page loads entering at `/`, `/app`, `/sign-in*`, `/sign-up*` may splash; deep links never splash on a later nav to home); data-ready cuts the moment to a 260ms exit fade; warm cache (data ready at first paint) skips entirely; reduced motion never mounts it; decision try/catch + render error boundary fall through to the plain loading home; queries fire exactly as before (overlay only). Timing/scale vars (`--splash-*`) in the `:root` tuning block; keyframes at the bottom of `index.css`. Tests: `src/test/home-brand-splash.test.tsx` (6); probe: `qa/task902-splash-probe.mjs` (slowed-splash, skeleton-after-beat, real cold, warm nav-back, reduced-motion runs) |
| `src/pages/chat.tsx` | ~89 recorder hook use. Mic-grant guard (Task 848, July 30, 2026): positive hold-confirmation — pointer-down records the active pointer id (`beginHold`), grant-resolve continues into recording ONLY if that exact pointer is verifiably still held; window-level pointerup/pointercancel/blur fallbacks end the hold when the permission prompt swallows the button's pointerup. Replaced the negative `isPendingStopRef` released-before-start flag. Tests in `src/test/chat-mic-permission-guard.test.tsx`; real-browser repro `qa/chat-mic-grant-probe.mjs` (deferred getUserMedia shim, clean profile) |
| `src/pages/account.tsx` | Timezone is a searchable grouped dropdown (`src/components/timezone-select.tsx`, Task 870) built from `Intl.supportedValuesOf("timeZone")`; saves through the existing preferences path; note ICU may list legacy ids (Asia/Calcutta) — the component canonicalizes them to modern IANA names for display/save (`canonicalTimezone`, legacy id kept as a search keyword) and replaces cmdk's fuzzy scorer with substring-only ranking (`timezoneMatchScore`: city prefix > city substring > id substring; fuzzy subsequences like "Kolk"→Srednekolymsk score 0). Saved+detected zones are always injected into the list. Tests in `src/test/timezone-select.test.tsx` (NOT account.test.tsx). Lines 868/881 are a pre-existing voice-preview player. Language control sends `{activeLanguage, hasChosenLanguage: true}` (Spec B1) |
| `src/pages/choose-language.tsx` | Spec B1 full-screen language step (July 30, 2026), route `/choose-language` (Guard, NO AppShell). Tile grid: native name prominent (nastaliq-aware via `nativeTextProps`), English secondary, NO lock/Plus badging by design; "Listening practice unavailable" badge ONLY for `speechCapability === "unsupported"` (degraded gets none — deliberate). Locked pick lands on the `/journey` showroom, unlocked on `/app` (a `navigatingRef` guards the redirect-if-already-chosen bounce from yanking the showroom nav). Skip link writes ONLY the sessionStorage marker `bolo.langStepSkipped` (session-scoped, no server write). One-time community-review footer note. Error toast keeps the learner on the step. Tests: `src/test/choose-language.test.tsx` |
| `src/lib/language-step.ts` | Spec B1 helpers: skip-marker read/write + `useExplicitLanguageChoice()` — single PATCH carrying `{activeLanguage, hasChosenLanguage: true}`, merges the response into the account cache via `setQueryData` (no refetch). Used by the step, the language picker (fire-and-forget), and account settings |
| ~~`src/components/language-choice-gate.tsx`~~ | REMOVED July 30, 2026 by product decision: **B1 gate removed; selection screen retained as a route; flag infra retained.** Fresh accounts land directly on `/app` home with the seeded default (Hindi) — no redirect, no gate wrapper in App.tsx. `tour-auto-launcher.tsx` no longer holds on `hasChosenLanguage` (the tour fires on first home load, pre-B1 behavior); it still blocks auto-launch on `/choose-language`, which remains a normal navigable route. The old fresh-account tour/gate navigation race is moot (only one navigator remains). Pins: `src/test/choose-language.test.tsx` (gate file must not exist), `src/test/tour-auto-launcher.test.tsx` (fresh account → tour on home) |
| `src/pages/subscription.tsx` | `DetailRow` at line 655, local, not exported |
| `src/pages/friends.tsx` | Local `ErrorState`/`SectionLoader` helpers near the bottom; uses the SHARED `EmptyState` for the empty leaderboard and no-friends states (an earlier note calling that an "inline EmptyState" was wrong) |
| `src/App.tsx` | Route-level code splitting + idle prefetch since July 30, 2026: only Landing and Home are eager imports; every other page uses the local `lazyRoute()` wrapper (NOT bare `React.lazy`) behind a `Suspense` (`RouteLoading` spinner, `aria-label="Loading page"`). Cut the logged-in home dev-preview load from 190 to 115 requests (src modules 120→55) — measured with `qa/home-load-timing.mjs`. `IdleRoutePrefetch` then warms every route chunk sequentially starting 3s after mount (`PREFETCH_ORDER`, most-likely-next first; skipped under `navigator.connection.saveData`), so post-idle navigation shows no spinner. `lazyRoute` exists because wouter location updates are sync external-store updates React can't transition — even a resolved `lazy()` promise flashes the fallback for a frame; the wrapper renders the component directly once its module resolved and exposes `.preload()`. Keep new routes on `lazyRoute` + add them to `PREFETCH_ORDER`; only add an eager import if the page is an entry surface |
| `src/components/mascot.tsx` | Canonical-PNG Bolo (reverted July 30, 2026, owner decision — the Task 836 rig is retired): renders the five canonical pose PNGs from `public/mascot/` as WHOLE images. Pose changes crossfade via `AnimatePresence mode="popLayout"` keyed `motion.img` (0.22s, instant under reduced motion) — the CURRENT pose img is IN-FLOW (`h-full w-full object-contain`; only the exiting img gets popped absolute by framer). Never absolutely position the current img: practice's `fill` chain has no definite ancestor height, so `height:100%` resolves to 0 and the bird collapses to ~10px (Task 882 regression, July 30, 2026). Pinned by `qa/practice-mascot-probe.mjs` (authed real-browser bbox assertions across idle/recording/evaluating/result at mobile+desktop). Whole-image motion only: entrance pop (`mascotEntrance`), idle bob (`floatIdle`) or `"calm"` breathe-scale for nav chrome, `whileHover`/`whileTap` tilt, and `activity` "talking"=scale pulse / "listening"=lean (no beak sync — `talkAudioRef` is accepted but unused, kept for API compat). Funny idle variants removed. Perf guards: `useAnimationVisible` (IntersectionObserver + visibilitychange) pauses all ambient animation off-screen/tab-hidden; `useReducedMotion` disables idle animation entirely. Public API unchanged (`pose/size/idle/ambient/fill/className/activity/talkAudioRef`) |
| `src/components/bolo-rig.tsx` | **RETIRED July 30, 2026 — non-canonical hand-drawn approximation, do not render it in the app.** Kept on disk unreferenced (nothing imports it) for reference only; header banner in the file says the same. Canonical-art rule (owner decision): **No new Bolo artwork may be created by any means (drawing, SVG, AI generation, tracing). The only permitted mascot pixels are the five canonical PNGs, animated as whole images.** Historical detail: artwork redrawn July 30, 2026 to match the original PNG mascot sheet (fuller 4-lobe crest, r=10 navy irises, thinner brows, shorter rounded beak hook, 4-finger scalloped wing fan, chunky pad+3-toe feet, inset blush, `bolo-sheen` radial highlight on head/body). Geometry-only change: all pivots, `POSES`, and transform templates untouched; compare against `public/mascot/*.png` via `qa/rig-shot.mjs` (4x element shot of the home hero rig). Layered SVG rig: per-part pose targets in `POSES`, transforms written to the SVG `transform` **attribute** via a `TG` helper subscribing to MotionValue templates (framer-motion intercepts the `transform` prop on `motion.g`, so exact-pivot `rotate(deg cx cy)` must be set as an attribute). Wing convention: positive rotate = raise outward, negative = sweep inward across the chest (thinking's wing-to-chin). Ambient blink/breath/head-tilt loops, clamped pupil cursor-tracking (`(pointer: fine)` only), amplitude-driven beak sync (WebAudio `captureStream` analyser on the chat playback element, analysis-only; synthetic sine fallback), flinch beat entering tryagain, poke reaction on pointerdown (650ms cooldown). Reduced motion collapses everything to still pose frames (`mv.jump`) |
| `src/components/ui/confetti.tsx` | Hand-rolled framer-motion. 70 shape pieces, 40 in glyph mode |
| `src/components/ui/badge.tsx` | `Badge`, cva |
| `src/components/ui/band-pill.tsx` | `BandPill`, five-band types/helpers (`BAND_LADDER`, `bandLabel`, `bandFromScore`, credit-group helpers, `bandTextClass`) — client mirror of server `scoreBands.ts` (Task 2; five-band Task 901) |
| `src/components/ui/band-ladder.tsx` | `BandLadder` five-band result ladder (Task 901) |
| `src/components/ui/empty-state.tsx` | `EmptyState` (Task 2) |
| `src/components/ui/count-up.tsx` | `CountUp` (Spec 1 v3) |
| `src/components/XpCounter.tsx` | (Spec 1a) |
| `src/components/XpArc.tsx` | (Spec 1 v3) |
| `src/components/layout/bottom-nav.tsx` | Line 25 XpCounter chrome. Below `lg`. Since Task 823 mounted ONCE by `AppShell` (outside the `PageTransition` subtree — see the P1 v2 paragraph); game subpages (outside AppShell) keep their per-page mounts. NOT on practice/chat. Since Task 838 the centre chat button renders `<Mascot pose="wave" size={56} ambient="calm" />` instead of the static wave PNG |
| `src/components/layout/desktop-nav.tsx` | Line 77 XpCounter chrome. `lg` and above, via `AppShell`. CORRECTION (Task 823): an earlier note said it "IS on practice" — wrong; practice is Guard-only (not wrapped in AppShell), so neither nav renders there |
| `src/lib/motion.tsx` | `SoundWavePulse`, a time-based 6-bar loop (audio-driven only when given an `amplitude` MotionValue). `funnyIdleVariants(reduceMotion)`. `PageTransition` (Task 812): 200ms fade + 10px rise for routed page content, fade-only under reduced motion |
| `src/lib/sound.ts` | `playCue` (Spec 1 v3). Cues under `public/sounds/cues/` |
| `src/lib/band-audio.ts` | Instant band clips (Task 903): `BAND_CLIP_FILES` (Band → `public/sounds/bands/*.mp3`), `preloadBandClips()` (once per page load, called on practice pointer-down), `playBandClip(band)` → `{finished, stop}` handle that never throws (null on failure). practice.tsx result effect: band clip plays the moment a result lands (gated on the spoken-feedback pref), feedback synthesis races an 8s timeout (`FEEDBACK_AUDIO_TIMEOUT_MS`), then feedback audio plays after `clip.finished`; failure/timeout = band clip only, result card never blocked. Tests: `src/test/practice-band-audio.test.tsx`; live probe `qa/task903-band-audio-probe.mjs` |
| `src/lib/scriptGlyphs.ts` | `glyphsForLanguage`, sourced from Script Trace chapter data (Spec 1 v3) |
| `src/lib/soundPref.ts` | (Spec 1a) |
| `src/lib/motionPrefs.ts` | `prefersReducedMotion()` (Spec 1 v3) |

**Fonts (self-hosted since the fonts pass, July 28, 2026):** `src/fonts/fonts.css` declares all `@font-face` rules (Inter + 13 Noto script families incl. Ol Chiki and Meetei Mayek) from local woff2 subsets in `src/fonts/` (~2.0 MB total, `font-display: swap`, OFL.txt alongside). `index.css` imports it; there are **no** runtime Google Fonts CDN requests (index.html preconnect/link tags removed too). Regenerate via the css2-API fetch approach if families change.

**Web visual polish pass (Task 812, July 29, 2026):** `AppShell` wraps its children in `PageTransition` keyed by wouter location — the shell persists across /app, /progress, /friends, /games because Switch reconciles it in place, so the location key is what re-triggers the entrance; DesktopNav sits outside it and never re-animates. `index.css` gained two utilities: `.card-lift` (hover lift + press compress; applied to game cards, category-detail phrase/sentence cards, leaderboard rows, friend rows) and `.animate-content-enter` (0.25s skeleton→content fade; applied to home and progress roots, category-detail `<main>`, and friends' leaderboard/requests/friends sections). Both are pure CSS, so the global reduced-motion rule collapses them automatically (spinners exempt via `:not(.animate-spin)`). `EmptyState` copy now staggers in under the mascot's existing pop-in entrance, gated on `useReducedMotion()`.

**Journey map (Spec D1b v2, July 29, 2026):** `/journey` (route in App.tsx, Guard + AppShell) renders the full Bolo Rail map for the active language: sticky boarding-pass header (line name, done/total stations, teaser consumed/limit chip in showroom mode), serpentine railway track (see the July 29 serpentine paragraph below) with six fare-zone postcards (`ZonePostcard`, grayscale when a showroom zone is fully locked), station rows ("Stop N of M", express stamp for `tested_out`, rotated-diamond marker + Plus chip for sentence stage, "Free taste" chip on the teaser station, mascot at the current station). Stations sort phrase-stage before sentence-stage, then by position. Three lock-dialog kinds: progression, sentence (Plus), and language (teaser vs exhausted). Plain-locked 402 renders the UpgradeScreen. Zone/category mismatch is a hard-stop error screen, no remap. Sentence stations are ALWAYS dialog-gated for non-Plus users client-side, and the server is separately authoritative (see the section 4 sentence-stage gate). Line/zone naming data lives in `src/lib/journeyLines.ts` (`JOURNEY_ZONES`, `JOURNEY_LINES` for all 22 language codes, `getJourneyLine()` falls back to a generic "Bolo Line"). `practice.tsx` gained a `?group=` mode (`useListLessonGroupPhrases` with the enabled+queryKey pair, back target `/journey`, post-attempt invalidation of the group phrases and category lesson-group lists). `home.tsx` fronts the topic grid with a "Ride the {line}" card (rebuilt into the boarding-pass hero by Task 823 — see the P1 v2 paragraph). Reachability (resolved July 29, 2026): the language picker routes locked-language taps into the showroom (sets the locked language active, navigates to `/journey`), and `language-context.tsx` no longer auto-reverts plan-locked active languages; see the resolved section 8 row.

**Journey serpentine + full-ticket pass (Task 3 of the asset/visual batch, July 29, 2026; web only, presentation only):** User-picked mockup winners ("pronounced" serpentine curve + "full ticket") applied to production. (1) `journey.tsx` map: the straight rail was replaced by a serpentine railway — stations alternate left/right (`stationX`: even global index → x=92, odd → mapWidth−94), cubic-bezier twin-rail segments with sleeper ties (`RailSegment`: 3 SVG strokes — dashed ties at 0.3 opacity, 8.5px rail, 4px `hsl(var(--background))` center split; locked segments GRAY + 0.5 opacity + dashed). Fixed vertical rhythm: station rows 100px, postcards 122px, terminus 92px. The map column is `max-w-[390px]` centered (mobile-first on desktop too); geometry adapts to narrower phones via a ResizeObserver width hook (`useMapWidth`). The interchange diamond is pinned where the track meets each postcard's top border. `StationRow` became `StationCard` (card is the Link/button tap target with the same aria-labels; the marker is a separate non-interactive absolute element). All behaviors unchanged: lock dialogs, showroom graying (GRAY zoneColor), sentence gating, teaser chip, `?group=` hrefs. (2) `journey.tsx` header + `home.tsx` hero: full-ticket treatment via shared `src/components/ticket.tsx` (`TicketStripes` diagonal CSS-gradient stock, `ZoneStamp` rotated dashed fare-zone ring, `TicketPerforationV` with bg-background notch cutouts). Header pass gains a w-[76px] tear-off stub (stamp of the current station's zone, hidden when there is no current stop); hero gains a w-16 stub (stamp from `useJourneyProgress().current.zoneIndex`, vertical-writing-mode line name) and drops the title truncate for 2-line wrap (`text-lg lg:text-2xl`). Stripes/stamps use `line.accent`; no artwork generated. Verified via `qa/demo-polish-shots.mjs` (all checks pass, mobile + desktop) with Nix chromium; web suite at documented 8-failure baseline; typecheck clean. **Cutout-ruling port (July 30, 2026, post build-28):** the mobile cutout ruling now applies on web too — cutout circles may ONLY straddle card edges as bites; the hero's floating `PunchHole` (component deleted from `ticket.tsx`) and the journey header's floating notch dot + 🎫 emoji (tofu without an emoji font) were removed. Measured light+dark via `qa/task879-shots.mjs` (Clerk ticket sign-in; probes every bg-colored circle in both cards → all STRADDLES-EDGE, none FULLY-INSIDE, no emoji).

**Journey scenery/color pass (July 29, 2026; web only, presentation only):** `src/components/journey-scenery.tsx` adds hand-coded inline-SVG scenery (brand palette + line accent, no raster/generated art). (1) `ZoneVista`: each fare-zone postcard gained a picture side — an accent-tinted sky band with a landmark scene keyed by ZONE INDEX (fixed across all languages): gateway arch (Z1 Greetings), family homes (Z2), clock tower (Z3 Numbers), chai stall (Z4 Food), bazaar + pennants (Z5), festival palace + fireworks (Z6 finale). The vista svg must use `preserveAspectRatio="xMidYMax meet"` — `slice` crops the top of tall landmarks because the band is wider than the 240-unit scene. `ZonePostcard` was restructured (vista on top, address row below, stamp+postmark now side-by-side; rotated italic caption removed) and `PC_H` grew 122→152; harness-checked texts ("Fare zone N ·", stamp "Zone"/number) unchanged. Showroom graying is free: the vista sits inside the postcard's `grayscale` wrapper. (2) `TracksideDoodad`: small scenes (tree, cloud+birds, railway signal, bushes+milestone, hut, telegraph pole) cycle by global station index in the free strip opposite each stop card, rendered inside the map SVG; grayed via CSS `filter: grayscale(1)` when the zone is showroom-locked. (3) `Bunting`: festival flag string above the terminus. Also: the current stop's status line renders in the line accent. Verified via demo-polish harness (all pass) + per-postcard element screenshots; suite still at the 8-failure baseline; typecheck clean.

**Boarding pass + journey CTA animation (July 30, 2026; web only, presentation only):** High-energy idle-motion and CTA-copy pass on the home hero pass and journey map; the pass is the app's single continue mechanism and reads like a game's PLAY button. index.css gains a TUNING-CONSTANTS `:root` block (all amplitudes/cycle times as CSS vars, one place to adjust after seeing it live) plus seven keyframe pairs after the reduced-motion block, all whole-element transform/opacity, short rests, identity start/end: `.animate-ticket-breathe` (3.2s heartbeat, scale via `--pass-breathe-scale`), `.animate-ticket-shimmer` (skewed light band sweeping the ticket face once per heartbeat), `.animate-pass-glow` (opacity-pulsing accent shadow overlay lifting the pass off the page), `.animate-cta-arrow-nudge` (2.4s double-pump slide, `--arrow-slide`), `.animate-train-drive` (home train drives forward then settles with a small overshoot), `.animate-stop-glow-pulse` (2.6s strong border+glow on the Now-boarding card), `.animate-train-bob` (2.2s double station bounce). `home.tsx`: press feedback is a dedicated framer `motion.div` (whileTap scale 0.94 + low-damping spring = overshoot on release; `PASS_PRESS_*` constants at top of file); the breathe class lives on a wrapper div because a CSS transform animation on a framer-driven element would fight its inline transform; shimmer overlay sits inside the Link (overflow-hidden clips it), the glow overlay sits OUTSIDE the Link (so its outward shadow is not clipped) but inside the press div. CTA copy is progress-aware via `useJourneyProgress`: "Start your journey" (no progress), "Resume at Stop N · X phrase(s) to go" (current stop, remaining = phraseCount - masteredCount), "Continue your journey" (progress but no boardable stop). `journey.tsx`: current-stop `TrainEngine` bounces; current `StationCard` glow uses solid ring + 16px bloom; the current-station `Mascot` gets `idle="cheer"` (whole-image hop on the canonical PNG, reduced-motion safe inside the component). Every CSS class is gated on `useReducedMotion()` in JS (the global CSS reduce block also covers the keyframes). Pinned by `src/test/home-boarding-pass.test.tsx` (5 tests: three CTA copy states incl. singular, reduced-motion static render asserting all five home classes absent, hero analytics press). Live-verified via `qa/boarding-pass-cta-shots.mjs` (asserts all seven classes in the authed DOM + captures home/journey shots). Web suite: 35 files / 252 tests. Same-day follow-up (train + station card visual upgrade, web only): `train-svg.tsx` redrawn in brand tokens (indigo body, teal trim, slate chassis, `currentColor` headlamp only) with animatable parts as SVG groups: `.train-wheel` spoked groups (need `transform-box: fill-box`) and `.train-steam-1/2/3` puffs resting at opacity 0. Wheel spin + steam puff keyframes fire ONLY via descendant selectors under `.animate-train-drive`/`.animate-train-bob`, so they inherit the parents' reduced-motion JS gating; svg root is `overflow: visible` so puffs rise above the viewBox. NOTE: `station.masteredCount/phraseCount` are optional on the journey `Station` type — template literals hid this; arithmetic needs `?? 0`. Journey `StationCard` is now a station signboard: active card gets a full-width line-color top bar (`rounded-t-lg`, NOT overflow-hidden — that would clip the glow overlay's bloom), warm `--station-surface` background (dark-mode override exists), `StationSignGlyph`, and a filled progress track replacing the text-only mastered fraction (label kept); non-current cards get a short quiet top tick (line color when accessible, border gray when locked) and keep hover. `--steam-rise` and `--station-surface` joined the :root constants block. Probe extended: wheel/steam group counts (4/3) asserted on both surfaces.

**Chat voice iOS/WebKit audio unlock (July 29, 2026; web `chat.tsx`):** every iPhone browser (Safari, Chrome, Firefox — all WebKit) only allows `.play()` on an `<audio>` element previously started inside a real user gesture; Bolo's reply audio starts seconds after the tap, so the old per-turn `new Audio(...)` was silently blocked (captions showed, no voice — rejection swallowed to `setPhase("idle")`). Fix: two pooled elements (`voicePoolRef` voice/greeting, `sfxPoolRef` squawk) "blessed" with a 50 ms silent WAV data URI by `unlockAudioPlayback()` called synchronously in `handleMicPointerDown`, `sendTextTurn`, and `handleRetry`; all buffered/greeting/SFX playback goes through `acquireAudio(pool, src)` (pause + clear handlers + swap src). The MSE streaming path keeps its own fresh element (WebKit never takes it; desktop doesn't need blessing). The word-reveal duration `probe` only calls `.load()`, never plays — left as `new Audio`. Play-rejection catches now log `[audio] play blocked path=…` with the error name. Test impact: chat test files must stub `Audio` with a constructible class (like the practice tests' `FakeAudio`) because the pointer-down handler now constructs synchronously — `vi.fn(() => ({...}))` stubs throw "not a constructor" (fixed in chat-bilingual-hint, chat-mic-permission-guard, chat-error-banner). Suite back at the 8-failure baseline; typecheck clean. Real-iPhone confirmation pending (needs republish — the report came from the production app).

**Web monetization surfaces (July 29, 2026):** `/upgrade` sells exactly two plans: All-Access (internal id `plus`, $12.99/mo or $89.99/yr, 7-day trial) and Family ($19.99/mo or $139.99/yr). One Language is retired on web; legacy `?plan=one_language` deep links land on the All-Access card, and server internals for mobile One Language holders (requiredPlan values, gating.ts, chatLimits.ts, dev-override endpoint) are unchanged. Customer-facing name is "All-Access" on every web sale surface (the "Save 42%" badges match both annual prices); `subscription.tsx`/`account.tsx` keep "One Language" only as a display label for held mobile tiers. `upgradeHref` takes `{plan, reason}` (the lang param is gone). `beginFamilyCheckout(interval, queryClient)` sends the billing interval; the server's `getFamilyPriceId(interval)` reads `STRIPE_FAMILY_MONTHLY_PRICE_ID`/`STRIPE_FAMILY_ANNUAL_PRICE_ID`. All four Stripe live price IDs live in `.replit` `[userenv.shared]`; the old $9.99/$71.99 Plus prices are archived in Stripe so no new checkout can use them. The in-place Plus-to-Family upgrade on `subscription.tsx` uses the monthly Family price. The Free context card on `/upgrade` reads two lines since Task 824 (July 29, 2026): "Hindi + a free taste of all 22 languages" plus a muted "3 lessons a day" line that keeps the server-daily-cap mirror comment; a full web sweep found no other user-facing "Hindi only" framing (account.tsx's "Hindi + {chosenLangName}" is the accurate legacy One-Language label, left alone). NO REAL SUBSCRIBERS exist as of July 29, 2026: the only paid row ever was the owner's $9.99 test subscription, canceled in Stripe on July 29, 2026; the prod users row reads tier=free, status=canceled with the canceled subscription id and Stripe customer id retained as the audit trail. No grandfathering machinery is needed for the price change.

**Games hub preview vignettes (Task 840, July 29, 2026; web only, presentation only):** the `/games` hub cards' static lucide icon tiles were replaced with miniature looping preview vignettes that pantomime each game's mechanic (Word Match pair slide+light-up, Listen & Pick wave+chip pick, Phrase Builder tile shuffle, Speed Round timer ring + answer flashes, Bolo Quiz choice cycle + sparkle). Implementation: `src/pages/games/game-previews.tsx` + colocated `game-previews.css` (kept out of index.css deliberately), pure CSS keyframes on design tokens, no framer-motion. Two load-bearing conventions: (1) each vignette's BASE styles are authored as its settled frame and keyframes carry all motion states, so the global prefers-reduced-motion rule in index.css collapses every loop to a meaningful static frame for free; (2) no `animation-fill-mode` anywhere (the forwards/both containing-block trap — see the P1 v2 bottom-nav paragraph). Loop phases stagger via a `--gv-delay` custom property (negative delays, set per-card in `index.tsx`), each cycle holds a long settled pause so the grid stays calm. `GamePreview` falls back to the game's lucide icon for unknown ids; locked cards keep the dimmed tile + lock treatment but still play their preview. Card layout, links, pills, and difficulty badges unchanged (`src/test/games-hub.test.tsx` pins titles, hrefs for free/Plus, and vignette presence). Verified via `qa/games-preview-shots.mjs` (see the QA harness row); suite at the 8-failure baseline; typecheck clean.

**Landing page refresh (Task 839, July 29, 2026; `src/pages/landing.tsx`, presentation only):** the signed-out landing now reflects the full product. Header logo swapped from the static `mascot-wave.png` to the rigged `<Mascot pose="wave" size={36} idle="none" />` (hero already used the rig). Sections: hero + SpeakingDemo (unchanged centerpiece, expanded copy), language chips, "The loop that makes it stick" (4 cards: Hear it → Say it out loud → Get coached → It comes back — spaced review is part of the pitch), "Way more than flashcards" (Chat with Bolo, journey map, games arcade w/ Script Trace call-out, retention loop) plus anchored feature chips, updated comparison block, and an honest no-hard-sell Free vs Plus/Family note. All sections reuse the local `Reveal` + `glass-card` language and a shared `FeatureCard`; hero secondary CTA and language chips moved from `bg-white` to `bg-card` for dark mode. `src/test/landing.test.tsx` pins the new headings (chip assertions use anchored regexes because labels recur in card body copy).

**Bolo in the bottom nav (Task 838 layout kept; mascot rendering reverted to canonical art July 30, 2026):** the centre nav button keeps its enlarged 80px pop-out circle but renders the canonical `mascot-wave.png` again via `Mascot` `ambient="calm"` (56px framing): plain fade entrance, subtle whole-image breathe-scale, no drop shadow. The nav Link's `aria-label="Chat with Bolo"` carries the accessible name (the Mascot wrapper is `aria-hidden`); `bottom-nav.test.tsx` pins img-present with `mascot-wave.png` src inside the chat link.

**QA visual verification harness (maintained tooling, July 29, 2026):** `qa/home-overflow-probe.mjs` (horizontal-overflow detector: measures scrollWidth vs viewport at 390px, lists unclipped offenders, and hide-bisects down to the culprit element; `E2E_PATHS` picks routes — used to find the home boarding-pass overflow, where the grid track honored the ticket's nowrap min-content until `min-w-0` was added to both grid children), `qa/upgrade-paywall-shots.mjs` (paywall pricing, layout, and card-overlap checks at mobile and desktop widths, monthly and annual) and `qa/journey-map-e2e.mjs` (journey map scenarios: `teaser`, `plus`, `free`, and `picker`; `picker` is the real locked-picker-to-showroom navigation plus home/chat blast-radius checks). Both drive real chromium: `CHROME_BIN=$(which chromium) E2E_USER_ID=<clerk user id> E2E_SCENARIO=<name> NODE_PATH=/tmp/pw/node_modules node qa/<script>`. `qa/games-preview-shots.mjs` (Task 840) joins the set: /games hub card vignettes — running animations, staggered `--gv-delay` phases, reduced-motion settle check, light/dark + mobile/desktop shots (when `which chromium` is empty, the playwright-browsers Nix store chromium binary works as `CHROME_BIN`). `qa/nav-bolo-shots.mjs` (Task 838): bottom-nav rigged Bolo — svg-not-img in the chat button, ambient transforms actually changing over time, light/dark + /games shots, reduced-motion still-frame visibility (note /chat renders outside AppShell so it has no bottom nav — that skip is expected). Prereqs: chromium via system deps (not committed to replit.nix), playwright installed at `/tmp/pw` (a workspace restart wipes `/tmp`; reinstall with `npm i playwright`), a throwaway Clerk QA user signed in via the sign_in_tokens API, and `users.has_completed_tour=true` on that user or the first-run tour hijacks navigation. Screenshots land in `qa/shots/` (gitignored). Upcoming visual work should rerun these rather than building new harnesses.

**Demo-day polish pass (P1 v2, Task 823, July 29, 2026; web only, presentation only):** Six items. (1) Bottom nav: root cause of the nav scrolling away was `.animate-content-enter`'s `animation-fill-mode: both` leaving a persistent identity transform on page wrappers, which made them containing blocks so the nav's `position: fixed` became container-relative; fixed by switching the fill mode to `backwards` (visually identical end state). `BottomNav` now mounts once in `AppShell` OUTSIDE the `PageTransition` subtree (per-page mounts removed from home/progress/friends/games hub; the six game subpages keep theirs); `.pb-safe` was an undefined utility computing 0px — now a real `env(safe-area-inset-bottom)` utility in index.css, and index.html gained `viewport-fit=cover`. `/journey` main got `pb-28 lg:pb-14` to clear the nav. (2) Home hero: `home.tsx` boarding-pass hero in the line accent (station name, "Stop N of M", per-stop mastered bar, Begin/Continue action verb, streak + daily-goal chips on the ticket stub); live state comes from `src/lib/useJourneyProgress.ts`, which reuses the journey page's six `useListCategoryLessonGroups` queries (shared react-query cache) and degrades to `current: null` on loading/402/error. Topic grid header is "Browse by topic". New allowlisted analytics event `journey_entered_via_hero` (web `analyticsEvents.ts` only, payload = language code). Tests mocking `@workspace/api-client-react` as a module factory and rendering Home MUST now stub `useListCategoryLessonGroups` (entitlements-gating.test.tsx does). (3) Chat meter: presentation-only rework into a labeled free allowance ("2 free chat minutes each week", time-left counter, amber low state under 25% with an inline "Go unlimited" link, and a zero state that replaces the bar with an inline Go All-Access prompt). Expiry BEHAVIOR untouched: `disabled={capExhausted}` on both mic surfaces, the `startRecording` upgrade redirect, and `weekly_cap_exceeded` handling are byte-identical. The meter still only renders once the server reports `secondsRemaining` (unchanged condition). (4) `src/components/train-svg.tsx`: interim `currentColor` side-profile engine SVG, faces right; used in the journey current-station marker (replacing the 🚂 emoji) and the home hero. The bespoke artwork-batch train replaces it later. (5) `ZonePostcard` furniture: zone-number stamp corner, circular postmark, divided-back vertical rule, italic slightly-rotated geo-name caption (bundled fonts only); all tinted via the existing `color` variable so the D1b grayscale locked treatment inherits unchanged; "Fare zone N" copy preserved for the harness. (6) Hold-to-speak affordance: practice idle copy is now "Hold Bolo to speak" (aria-labels unchanged: belly zone still `Hold to speak`/`Release to submit`), idle pulsing ring on practice and chat mascots (omitted under reduced motion), pressed state via a plain CSS `scale` transform on an inner div — deliberately NOT a stacked framer `motion.div`, which would fight the amplitude MotionValue on the practice mascot. One-time first-practice hint bubble via localStorage `bolo.practice.hint.v1` (hook lives with the unconditional hooks ABOVE practice.tsx's loading/error early returns — placing hooks below them crashes with "Rendered more hooks"). Practice tests asserting the visible idle copy were updated to the new string. Recording logic untouched. New harness `qa/demo-polish-shots.mjs` (same env/pattern as the others; Clerk sign-in tokens are single-use, so it mints one per browser context) captures the matrix: home hero, journey top+mid-scroll, chat, practice idle+pressed at 430x900 and 1280x900. Web suite baseline unchanged (account.test.tsx x6 + chat-error-banner x2).

**Bottom-nav Bolo hero button (Task 837, July 29, 2026; web only, presentation only):** The centre Bolo circle in `bottom-nav.tsx` grew 56→80px (`h-20 w-20`, parrot img 36→56px) and its offset moved `-top-5`→`-top-11`. Geometry invariant: the circle's BOTTOM edge stays at row top + 36px — all extra size protrudes UPWARD, past the XP strip and ~13px above the nav's top border — so the row height, label position, and pb-safe interplay are untouched. The active (`/chat`) tint is now an opaque treatment (`bg-card` always on, tint via a `from-primary/15 to-primary/5` gradient overlay) so the full-width XP bar the circle overlaps never shows through; note `/chat` renders without a BottomNav (own layout), so the active state is only reachable if that changes. The "Bolo" label gained `leading-none` — without it the 15px line box clipped ~3px below the viewport when `pb-safe` is 0. Pages that mount the nav bumped mobile bottom padding pb-24→pb-28 (progress, friends, games hub + six game subpages; home/journey were already pb-28) to clear the taller protrusion. Verified with a throwaway chromium harness (same sign-in-token pattern as `qa/demo-polish-shots.mjs`): sizes, protrusion, label, light/dark/hover shots at 430x900. Web suite baseline unchanged (account.test.tsx x6 + chat-error-banner x2); typecheck clean.

**Web polish batch (Task 870, July 30, 2026):** (1) Progress page shows NO numeric scores anywhere — history uses `BandPill` (`band ?? bandFromScore(score)`; `bandFromScore`/`bandLabel` exported from `ui/band-pill.tsx` — five-band since Task 901), the "Best Score" stat card became a "Best Attempt" band label, and advanced-analytics per-topic "avg N" became a band label. (2) Friends page has an absolute-positioned back button to `/account` (aria-label "Back to account") matching Account/Subscription. (3) Language-picker dialog redesigned for 360px: English names full-width and never truncated, lock state is a single corner Crown glyph (`data-testid="picker-locked-<code>"`, no per-tile ALL-ACCESS pill or Lock icon), one shared header note "Locked languages need All-Access". (4) Choose-language + picker tiles softened (rounded-3xl, 1px border-card-border/70, shadow-sm). (5) Customer-facing copy: "Plus" → "All-Access" across the web app, and sell copy no longer promises "unlimited lessons"/daily-limit framing (pitches languages + features instead); internal identifiers (isPlus, PlusInterval, cancelPlus, plan codes, lucide Plus/UserPlus) intentionally untouched. Screenshot harness: `qa/task870-shots.mjs`. Mobile half of the copy sweep is a separate task.

**Build 30 batch 3, games + home cleanup, web half (July 30, 2026).** (1) All five games (Word Match, Phrase Builder, Speed Round, Listen and Pick, Bolo Quiz) speak target-language audio via the existing `useSynthesizeSpeech` + per-card base64 cache pattern: Word Match speaks the Indian card's nativeScript on flip (never the English card), Phrase Builder speaks a tile's nativeScript when placed (reorder taps stay silent), Speed Round speaks the prompt word; Bolo Quiz gained a per-question `audioCacheRef`. (2) One shared mute control: `src/lib/gameAudioPref.ts` (localStorage key `bolo.gameAudio`, `off` = muted, default unmuted) + `src/components/game-mute-button.tsx`, wired into all five play surfaces; muted skips synthesis calls entirely, not just playback. (3) Every game's active-play state has a persistent exit control; timed games (Speed Round, Bolo Quiz) confirm via `window.confirm` before discarding a run. (4) Listen and Pick wrong answers hold the reveal (red pick, green correct, replay) until a tap-to-continue; correct answers keep auto-advance. (5) Phrase Builder supports tap-based reorder of placed tiles (select, then tap another placed tile to swap; tap the selected tile again to return it to the tray). (6) Games hub Plus tiles fail closed: `plusReady = isPlus === true && !isLoading`, pinned in `games-hub.test.tsx` (loading and undefined cases). Note: the web home has NO Daily Quiz card and never had a ContinueCard equivalent (confirmed by search), so the quiz-rename and ContinueCard-deletion items were mobile-only. (7) One-time name prompt on home: `src/components/name-prompt-card.tsx`, shown only when Clerk `firstName` is null, saves via the PATCH /account/profile dual-write then `user.reload()`, dismissal persisted in localStorage (`bolo.namePromptDismissed`); web tests that render home and do not care about it stub it to null (needs a react-query provider + profile hooks otherwise). (8) Shared `lib/api-client-react/src/custom-fetch.ts` now has a 30s default AbortSignal timeout with a per-call `timeoutMs` override and a `RequestTimeoutError` (both re-exported from the package index). Chat SSE streaming bypasses customFetch entirely, so the timeout cannot collide with batch 2's audio streaming.

**Phrasebook door + tour removal (Task 906, July 31, 2026; web half).** (1) Home's topic grid is gone; in its place a Phrasebook door: `<section aria-label="Phrasebook">` in `src/pages/home.tsx` with a stretched Link to the new `/phrasebook` route, subtitle "Browse and practice any topic", a chip row of the first 3 categories deep-linking `/learn/:id` (mastered/total shown only when masteredCount > 0), and a dashed "+N more" chip when more than 3 exist. (2) New `src/pages/phrasebook.tsx`: the full topic library (grid moved from home, mastered/total line per card, back link to /app), fires the new allowlisted `phrasebook_opened` analytics event exactly once on mount with `{ language }` (added to `analyticsEvents.ts` on BOTH platforms; home does NOT fire it). Route registered lazily in App.tsx behind Guard+AppShell. (3) The guided tour is REMOVED from the web app entirely: deleted `src/lib/tour-context.tsx`, `src/components/guided-tour-overlay.tsx`, `src/components/tour-auto-launcher.tsx`, `src/test/tour-auto-launcher.test.tsx`; App.tsx/account.tsx/bottom-nav/desktop-nav stripped. The server-side `users.has_completed_tour` column and account-preferences `hasCompletedTour` API field remain (api-server was out of scope; see section 8). (4) Journey zone-title dedup: `src/pages/journey.tsx` resolves zone display titles from the `/categories` listing by id, with `JOURNEY_ZONES` hardcoded titles as a loading-state fallback only; the title-mismatch hard-stop guard is removed (a server-side rename now just renders). (5) Analytics: alongside `phrasebook_opened`, a new `TOPIC_OPENED` event fires from the door chips (source `home_chip`) and from phrasebook cards (source `phrasebook`); no pre-existing topic-open events existed. Door/page carry stable testids (`phrasebook-door`, `link-phrasebook-door`, `phrasebook-chip-*`, `phrasebook-topic-*`). (6) Express test-out shipped on web (closes the deferred task-819 gap): the progression lock dialog offers "Test out of this stop" (`link-test-out`) linking to `/practice/:zoneId?group=<id>&mode=testout`; practice.tsx in testout mode loads GET `/lesson-groups/:id/test-out` (envelope: phrases + sampleSize + requiredCorrect), saves NO per-phrase attempts and awards no XP (collects `evaluationToken`s in a ref), hides all retry controls (one take per phrase, banner `testout-banner`), and submits one POST batch at the end; verdict screen (`testout-summary`) shows pending/pass (Express stamp + confetti, invalidates the group listings)/fail (encouraging copy)/error (resubmit unless 429). Fail path returns to `/journey` (no in-place session reset, deliberate). (7) Progression dialog defect fixes: the manual duplicate X close button was removed (shadcn `DialogContent` renders its own) and the copy reads "finish the stop before this one to board here". Tests: `phrasebook-door.test.tsx` (home door, main variant), `home-phrasebook-door.test.tsx` (door testids + chip analytics), `phrasebook-page.test.tsx` (library page + analytics), `journey-zone-titles.test.tsx` (dedup), `journey-testout-dialog.test.tsx`, `practice-testout.test.tsx`. NOTE: any new hook import in practice/journey must be added to every full-replacement `vi.mock("@workspace/api-client-react")` factory in tests importing those pages (8 files patched for the testout hooks).

**Home upsell card reflow + badge-card return pin (Task 919, July 31, 2026; web only).** (1) `UpgradeCard` in `src/components/plus.tsx`: the title/badge row is now `flex flex-wrap items-center gap-x-2 gap-y-1` and the h3 gained `min-w-0`. Rationale: `PlusPill` is deliberately `shrink-0 whitespace-nowrap` (the 360px language picker relies on that never-wrap rule), so at narrow widths the pill must yield to the next line instead of squeezing the title into one-word-per-line wrapping or clipping the card edge; PlusPill itself is unchanged at all call sites. (2) Badge-card navigation: the home latest-badge card is a wouter push to `/progress`, which renders under Guard+AppShell, so the always-mounted nav (BottomNav below `lg`, DesktopNav at `lg`+) is the return path by convention (Progress is a primary tab; back buttons are reserved for non-tab pages like Friends). No code change; pinned instead. (3) Home card audit at 360/768/1280: page never overflows; `innerOverflow` flagged on the stats banner, boarding pass, and badge card is their absolutely-positioned decorative blobs clipped by each card's own `overflow-hidden` (by design, not the defect class); phrasebook door, chat shortcut, recent plays clean. Tests: `src/test/home-badge-card.test.tsx` (4: badge card lands on /progress + BottomNav Home returns, no-badge renders no card, UpgradeCard wrap structure + pill contract + href, PlusPill global shape). Live probe: `qa/task919-shots.mjs` (three-width measurement pass incl. pill-within-card / pill-overlaps-title rect checks, plus an authed badge-card click-through to /progress and back; seed a `badges` row with a REAL badge key like `first_phrase` for the QA account if it has none, then delete it). Web suite 42 files / 295 tests.

**Unresolved:** one inventory reported `src/pages/review.tsx` exists but does not call the eval endpoint. A later one reported the web app has no review screen. Resolve before writing any spec touching web review.

---

## 6. Mobile app (`artifacts/bolo-mobile`)

Expo, expo-router. `app/(app)/_layout.tsx` sets `headerShown: false` for Stack screens. Practice and review are Stack screens **outside** the `(tabs)` group, so both are full-screen with no tab bar and no system header.

| Path | Notes |
|---|---|
| `app/(app)/(tabs)/_layout.tsx` | Five slots: Home, Games, Bolo (center bubble), Progress, Language switcher (opens picker; Profile is NOT a tab — reachable from Home's top-right gear). Floating pill bar since the pre-28 polish pass |
| `app/(app)/(tabs)/index.tsx` | Home. Line 306 XpCounter chrome, 543/556 score badges |
| `app/(app)/(tabs)/progress.tsx` | `Stat` component, general streak with Feather `zap` labeled "Day streak" |
| `app/(app)/(tabs)/chat.tsx` | ~487/529 recorder start/stop |
| `app/(app)/practice/[id].tsx` | Local five-band `bandColor`/label copies near the top (NOTE comments; kept in place per the section 8 flash-duplication row), record button pulse (time-based), metering consumption, recorder start/stop, token passthrough, optimistic todayXp, session summary when `phase === "done"`, mascot pose, result card with `BandLadder` (~1853), `PracticeHeader`, XpCounter session. Line numbers drifted in Task 901 — search by symbol |
| `app/(app)/review.tsx` | Line 244 `ReviewHeader`, 257 XpCounter session, ~731/751 recorder start/stop, 764 token passthrough, 769 optimistic, 784 setResult, 842/852 header render, 924 session summary, 1060 title ternary |
| `app/(app)/account/index.tsx` | `NavRow` 651, `StepperRow` 679, both local. Timezone row with modal |
| `components/Mascot.tsx` | Five PNGs, RN `Image` in Reanimated `Animated.View`. All idle and funny effects early-return on `useReducedMotion()` |
| `components/TalkingMascot.tsx` | Chat wrapper, listening/talking/thinking overlays |
| `components/Confetti.tsx` | Hand-rolled Reanimated. 44 shape pieces, 25 in glyph mode |
| `components/BandPill.tsx`, `EmptyState.tsx`, `XpCounter.tsx` | (Task 2, Spec 1a). BandPill now only renders on nocatch result cards; scored bands use the ladder |
| `components/BandLadder.tsx` | Five-band result ladder (Task 901); band types/helpers live in `lib/ui.ts` |
| `components/XpArc.tsx`, `CountUpText.tsx` | (Spec 1 v3) |
| `components/TipCard.tsx` | Rotates India fun facts during processing. No props |
| `components/BadgeUnlock.tsx`, `BadgesGallery` | Achievement badges. Also fire confetti |
| `lib/ui.ts` | `scoreColor` lines 34-35 |
| `lib/audio.ts` | `RECORDING_PRESET` line 26 with `isMeteringEnabled: true`. `stopAndReadRecording`. Silence auto-stop ~35-53 |
| `lib/settings.ts` | `bolo.silentMode` in AsyncStorage |
| `lib/sound.ts` | `playCue`, `CUE_SOURCES` static require registry, currently empty (Spec 1 v3) |
| `lib/band-audio.ts` | Instant band clips (Task 903): static `require()` map of `assets/sounds/bands/*.mp3`, `playBandClip(band)` → `{finished, stop}` never-throws handle (guards a stop racing ahead of async start). Playback goes through `lib/audio.ts` `playAssetAudio(source, onDone)` — mirrors `playBase64Audio`'s iOS earpiece mode-flip/keepAudioSessionActive/pause-before-remove logic for bundled assets. `practice/[id].tsx` result effect mirrors web: clip at result render (spoken-feedback pref gated), feedback races `FEEDBACK_AUDIO_TIMEOUT_MS` (8s), plays after `clip.finished`, token-guarded. Tests: `__tests__/practice-band-audio.test.tsx` (band-audio mocked at the module seam — jest can't transform mp3 requires) |
| `lib/scriptGlyphs.ts`, `lib/soundPref.ts`, `lib/motionPrefs.ts` | (Spec 1 v3, 1a) |

**Native-script fonts** (`constants/fonts.ts`): all 13 Noto script families are bundled via `@expo-google-fonts/*` packages — including Ol Chiki and Meetei Mayek (both bundled since the app's creation; an earlier audit note claiming they were missing was wrong). `SCRIPT_FONTS` maps backend `fontFamily` → loaded font. Meetei Mayek is mapped on **Android only** (Platform-gated since the fonts pass, July 28, 2026): on iOS the Expo-loaded font bypasses the OpenType shaper and mis-renders combining vowel marks, while iOS ships its own Noto Sans Meetei Mayek system fallback (since iOS 10) with correct shaping. Do not map it on iOS.

**Mascot poses (both platforms, same five PNGs):** `wave`, `cheer`, `thumbsup`, `thinking`, `tryagain`.

**Visual polish pass (July 29, 2026, mobile).** Loading: both auth gates (`app/index.tsx`, `app/(app)/_layout.tsx`) show a floating Mascot instead of a raw `ActivityIndicator`; word-match and listen-and-pick topic pickers show 6 `SkeletonCard` rows (64px, radius 16) and their phrase loads use `FunFactLoader`. Speed Round / Phrase Builder / Daily Quiz still show plain "Loading…" text (follow-up proposed). Empty states: shared `components/EmptyState.tsx` gained an optional `mascotPose` prop and a mount-only entrance (ZoomIn mascot pop + staggered FadeInDown text) guarded by `useAppearSkip()`; review passes `thumbsup`; friends' inline `EmptyFriends`/`EmptyLeaderboard` use the same inline pattern (test-asserted strings unchanged). Press feedback: `GlobeButton`, all three tappable `PlusUpsell` cards, and the chat skip button now use `PressableScale` (which supplies the light haptic — manual `hapticLight()` calls were removed to avoid doubles). Haptics: `hapticLight()` added to Google/Apple SSO button presses, the three account toggles, the theme `Segmented` control (only on actual change), and both reminder switches. All entrances use the `lib/entrance.ts` appear guards (Expo Go + reduced-motion safe).

**Silent mode** (`bolo.silentMode`) skips the coach's auto-played voice for each new phrase. Device-only, not synced. Does not mute feedback and does not affect recording. The web practice header (July 29, 2026) shows it and the spoken-feedback toggle as two matching labeled pills — PHRASE (Volume2/VolumeX) and FEEDBACK (Headphones/HeadphoneOff) — with ONE shared convention: teal filled = that audio plays, gray = off (they previously had opposite active states). Note the visual is inverted from the stored flag: PHRASE is teal when `silentMode` is **false** (`aria-pressed={!silentMode}`). Labels stay visible at all widths per user preference (progress bar has room).

**First-time language choice (B1 mobile parity, July 30, 2026).** `lib/language-step.ts`: in-memory module skip flag (session-scoped, NEVER AsyncStorage; `useLanguageStepSkipped()` is a `useSyncExternalStore` read so bootstrappers react to a skip; `resetLanguageStepSkipForTests()` simulates restart) + `useExplicitLanguageChoice()` — one PATCH `{activeLanguage, hasChosenLanguage: true}` merged into the account cache (web-helper mirror). `app/(app)/choose-language.tsx`: full-screen step, all languages, NO lock badging/dimming/paywall routing (aspirational pick), listening badge only for `speechCapability === 'unsupported'`, community footnote, Skip sets the session flag only. GATE REMOVED July 30, 2026 by product decision (same day, both platforms): `LanguageChoiceBootstrapper` deleted from `app/(app)/_layout.tsx` and `TourBootstrapper` no longer holds on the language step — fresh accounts land directly on home with the seeded default (Hindi) and the tour fires on first home load. The `choose-language` screen stays registered as a normal navigable route; the flag + one-PATCH helper infra is retained. The picker modal (`language.tsx`) now persists picks via the shared helper (`adoptLanguageLocally` on LanguageContext updates only the local mirror — going through `setActiveLang` too would double-PATCH; `setActiveLang` remains for the provider's internal seed/correction writes). Tests: `__tests__/language-choice-gate.test.tsx`, `__tests__/language-choice-step.test.tsx`. Build number unchanged (bump deferred to the map port).

**Mobile auth (July 28, 2026 fix).** Factor set: **password, email code, and SSO (Apple/Google)**. Factor selection is driven by Clerk's sign-in response — after `signIn.password()`, `signIn.status` and `signIn.supportedFirstFactors` decide the next step (a `needs_first_factor` response offering `email_code` auto-falls-over to the emailed-code step; web sign-ups are passwordless, so this is their only mobile path). `app/(auth)/sign-in.tsx` also has an explicit "Email me a sign-in code instead" entry (`signIn.emailCode.sendCode`/`verifyCode`). Shared error policy lives in `lib/authErrors.ts`: every auth operation ends in navigation, a user-visible error, or a code step; expected user-input errors (wrong password/code) show but do not go to Sentry; everything else is Sentry-captured with `authContext` tag; a flow that stops at a non-complete status surfaces **the status and offered factor strategies in the user-visible copy and the Sentry event** (never a generic error). Never pass emails/passwords/codes/factor objects (they carry `safeIdentifier`) into these helpers — strategy strings only.

**Journey map port (Spec D1b-M, July 30, 2026, headlines build 28).** Mobile port of the web `/journey` map — translation job, web is source of truth, zero api-server/web changes. `app/(app)/journey.tsx` (Stack screen outside `(tabs)`, registered in `app/(app)/_layout.tsx`): 6 per-zone `useListCategoryLessonGroups(zoneId, lang)` queries + `useListCategories`; serpentine track rendered as per-zone absolutely-positioned `react-native-svg` slices (viewBox `0 startY mapW sliceH`, 3-layer rail segments) inside a ScrollView — NOT one tall Svg (Android texture limits); stop markers/postcards/station cards are absolutely positioned RN views over the slices (a11y labels: "Stop N of M — StatusCopy"). Line/zone geo theming in `lib/journeyLines.ts` (22 lines, e.g. gu = Gujarat Express; fallback for unknown codes). Scenery (`components/journey/Scenery.tsx`) grayscales via palette-swap (COLORS/GRAYS), not filters (no SVG filters in rn-svg). Boarding-pass fittings in `components/journey/TicketParts.tsx` + `TrainEngine.tsx`; home hero pass `components/journey/JourneyPassCard.tsx` (driven by `lib/useJourneyProgress.ts`) sits above DailyQuizCard on home — ContinueCard intentionally kept below. Locked-stop dialog (RN Modal) has 3 kinds: progression, sentence (→ paywall), language showroom (teaser/exhausted → paywall with `{lang, reason}`); NO test-out on mobile (approved ruling). Showroom entry: locked rows in `language.tsx` adopt the language locally and open the map read-only (plain 402 summary → UpgradeRequiredScreen). To make that adoption survive, `contexts/LanguageContext.tsx` no longer auto-reverts a supported-but-locked active language to the first allowed one (mirrors the web D1b ruling that removed the same guard); only the unsupported-code fallback remains. Consequence: a free account can persist a locked activeLanguage (showroom, or post-downgrade) — journey/practice handle their own 402/403 states; since the pre-28 polish pass, home detects the summary 402 (`asUpgradeRequired`) and renders a showroom banner inside the stats gradient (Preview the journey / Unlock) instead of degraded zero stats, mirroring web home. Practice `?group=` scope: `practice/[id].tsx` accepts a `group` param, fetches via `useListLessonGroupPhrases` (must pass matching `queryKey` when using `enabled`), invalidates only that group key on completion, and renders a locked-stop card (not the error screen) on 403 `lesson_group_locked`. `react-native-svg` moved devDeps→deps. Tests: `__tests__/journey-map.test.tsx`, `journey-showroom.test.tsx`, `practice-group-locked.test.tsx`. NOTE: the new hooks forced `getListLessonGroupPhrasesQueryKey`/`useListLessonGroupPhrases`/`useListCategoryLessonGroups` stubs into ~38 existing test files' `@workspace/api-client-react` mock factories (inserted at factory top so file-specific keys still win), and the home test's rn-svg mock became a full passthrough set. **Build-28 full-screen ticket regression (fixed July 30, 2026, ships in build 29):** on a real iPhone (native only — Expo web looked perfect) both ticket surfaces grew to fill the viewport. Root cause: `TicketPerforationV` rendered `<Svg width={2} height="100%">` as a NORMAL-FLOW child of an indefinite-height stretch row — web resolves that percentage to auto, native Yoga measures the Svg and inflates strip → row → card unbounded. `TicketParts.tsx` now carries a sizing contract (header comment): no percentage-sized Svg in flow, ever; stripes + perforation Svgs render only after an onLayout measure, numerically sized, inside absolute wrappers. Belt caps: `JourneyPassCard` `maxHeight:240`, journey `headerTicket` `maxHeight:140` (both `overflow:'hidden'`; testIDs `journey-pass-card`/`journey-header-ticket`). Pinned by `__tests__/ticket-sizing.test.tsx` + a journey-map regression test (real Yoga layout is device-only; jest pins the bug's ingredients stay gone). `Scenery.tsx`'s percentage Svg is safe — absoluteFill inside the fixed 56px vista.

**Pre-28 mobile polish pass (July 30, 2026).** (1) Tab bar is a floating rounded pill: absolute-positioned, 14px side margins, `bottom: max(insets.bottom, 14)`, radius 32, height 74, 1px border, soft shadow (elevation 16 Android — parent elevation + the overflow-visible Bolo bubble verified on web only, not on a device). Active Home/Games/Progress icons sit in a brand-filled `TabIcon` pill (46×28, `primary` bg / `primaryForeground` icon). `TAB_BAR_CLEARANCE` in `components/Screen.tsx` raised to 124 web / 132 native. (2) The 5th slot (route name still `profile`) is `LanguageTabButton`: globe + uppercase active-lang code, pushes `/(app)/language`; the friend-request badge moved to Home's top-right gear button (`useListIncomingFriendRequests` now imported by home, and by every test mocking api-client that renders home). (3) `language.tsx` picker redesigned to match web: 2-col FlatList of rounded tiles, native script prominent (Nastaliq keeps lineHeight 52 + taller tile), full English name never truncated, single gold `MaterialCommunityIcons crown` corner glyph for locked (check-circle for active), header sub-line "Locked languages need All-Access…" when any lang is locked; locked-tile a11y label contract: "`{name}` — locked, preview its journey". (4) Customer-facing "Plus" copy swept to "All-Access" across mobile (paywall, PlusUpsell, subscription, family, voice, analytics, progress, chat, practice, games pill, billing-history `billingPlanLabel`); internal identifiers (`isPlus`, `PlusPill`, `plusOnly`, tier ids) intentionally unchanged. (5) Locked-adoption regression pinned in `__tests__/language-locked-adoption.test.tsx` (supported-but-locked survives; unsupported code still falls back). Screenshot harness: `qa/pre28-shots.mjs`. (6) Boarding-pass fixes (home hero + journey header): the eyebrow brand is per-language native-script "Bolo Rail" via `getRailBrand(code)` in `lib/journeyLines.ts` (20 script entries; sat/mni deliberately fall back to Latin "BOLO RAIL" — untrusted transliteration beats wrong glyphs), rendered in a nested `<Text>` with `nativeTextStyle` (the Latin UI font renders tofu for Indic scripts; `isTallCascadingScript` adds lineHeight room for Nastaliq — journey test fonts mocks must stub it). Stub layout made deterministic: the rotated ZoneStamp sits in a fixed centered slot (56×56 home / 52×52 journey header) so it can't straddle the perforation; the vertical line name is an ABSOLUTE rotated Text inside a measured slot with explicit `maxWidth` — react-native-web's Text base style has `maxWidth:'100%'` which otherwise clamps an explicit width to the 14px slot (one-glyph truncation), and a bare rotated Text only reserves its unrotated box (the original stamp/name collision). Verified via `qa/pass-shots.mjs` + `qa/pass-probe.mjs` (Devanagari + Gujarati headers, stamp/name rects). Cutout ruling (approved): punch/notch circles may ONLY straddle card edges as bites (perforation ends + edge midpoints) — never float inside the card. The web hero's floating `PunchHole` was deleted from mobile `TicketParts.tsx`, and the journey header's floating notch dot + 🎫 emoji (tofu without an emoji font) were dropped from the port; `qa/notch-probe.mjs` measures every background-colored circle in both cards and flags FULLY-INSIDE vs STRADDLES-EDGE.

**Build 30 batch 1: journey layout fixes (July 30, 2026).** (1) Journey header safe-area: `journey.tsx`'s main render keeps `padTop={false}` but the header now pads itself with `paddingTop: 10 + headerTopInset` where `headerTopInset = Platform.OS === 'web' ? 67 : insets.top` (matches Screen's own web chrome offset); loading/error branches use a plain `<Screen>` and need nothing. testID `journey-header`. (2) Stamp geometry: `TicketParts.tsx` exports `zoneStampExtent(size)` (ceil of size x (cos12 + sin12), the rotated bounding box: 48→57, 44→53) and `stampNameFontSize(name, size)` (longest WORD fits the 0.84-size chord budget at 0.7em/char, clamped [3,7]). ZoneStamp's name Text lost `numberOfLines` (wraps on spaces, can never ellipsize; testID `zone-stamp-name`), letterSpacing only at fontSize 7. Slots sized from the extent: `JourneyPassCard` stampSlot (`home-stamp-slot`) and the journey header stub slot (`header-stamp-slot`); the old fixed 56/52px slots clipped the rotated corners. Longest real geoName: "Thiruvananthapuram Central". NOTE: the WEB `ticket.tsx` still has the old fixed-slot + `truncate` treatment (see section 8). ChunkyButton gained an optional `testID` prop. Tests: `practice-flash-neutral.test.tsx` (new), plus updated `ticket-sizing`, `journey-map`, `journey-showroom` (safe-area mock now required by anything rendering JourneyScreen), `practice-retry`, `score-trail`, `practice-score-trail` (result-card wait is now the band-agnostic "Hear yourself" text since retry cards lost `retry-button`).

**Build 30 batch 2: chat record trigger + reply audio (July 30, 2026).** Three device-reported build 29 chat defects fixed, owned files `(tabs)/_layout.tsx`, `chat.tsx`, `lib/audio.ts`. (1) Dead nav-bird hold-to-talk: `BoloTabButton` read only `accessibilityState?.selected`, but the installed `@react-navigation/bottom-tabs` v7 passes the selected flag to custom `tabBarButton` renderers as `aria-selected` on native and passes NO `accessibilityState`, so `focused` was always false on device — pressIn never fired the haptic or the registered start handler (press-scale styling is unconditional, hence "visual feedback but nothing happens"). Jest missed it because both tab suites mocked the renderer with `accessibilityState`; `nav-bar-hold-to-talk.test.tsx` now mocks the real v7 shape. The button accepts both shapes (`ariaSelected ?? accessibilityState?.selected ?? false`). (2) Barge-in orphaning: the `wasPlaying` branch of `handleStartRecording` now bumps `activeTurnRef` (previously only `wasProcessing`) — with progressive voice streaming the SSE turn is still open while phase is `playing`, and a late `reply` payload from the interrupted turn could flip the phase back to `playing` mid-recording. (3) Reply loudness/quality mismatch: all chat players (`playSquawk` in chat.tsx, `playBase64Audio`/`playStreamingAudio` in lib/audio.ts) now pass `keepAudioSessionActive: true` to `createAudioPlayer`. Without it, expo-audio schedules an AVAudioSession `setActive(false)` 0.1 s after ANY player finishes or pauses unless another registered player is strictly `.playing` — and a buffering AVPlayer (streamed reply, first bytes 300+ ms out) does NOT count as playing, so the squawk chirp finishing mid-buffer deactivated the session the playback-mode flip had just configured. The greeting is the only clip never preceded by another player's completion, which matches "greeting loud and clear, later replies quiet and degraded" exactly. TTS content was ruled out empirically (probe: buffered vs `stream_format:"audio"` synthesis of identical text, both 24 kHz/128k mono mp3, streamed ~3 dB LOUDER). Exact audible outcomes are device-verify-only. Tests: `__tests__/lib-audio-playback-mode.test.ts` (mode-flip ordering, playbackModeToken races, keepAudioSessionActive pins, against the real lib/audio) and nav-bar suite section 6 (barge-in through the registered wrapper, turn orphaning, squawk option pin). Intentional asymmetry left in place and documented: the on-screen mascot Pressable stays `disabled` during `playing` (no barge-in from the mascot; nav bird only).

**Build 30 batch 3, games + home cleanup, mobile half (July 30, 2026).** (1) Game TTS parity with web: Word Match speaks the native card's nativeScript on flip (the card grid renders only after `onLayout` measures it, so jest tests must fire a manual layout event on `word-match-grid`; cards carry `testID="word-match-card-<id>"`), Phrase Builder speaks a tile's nativeScript on placement, Speed Round speaks the prompt; all use `useSynthesizeSpeech` + `playBase64Audio` with the per-card `id:voice` cache-key pattern from Listen and Pick / Bolo Quiz, and `useGetAccount` supplies the ttsVoice so a voice change busts the cache. Any jest suite that renders a game PlayingScreen must now mock `useSynthesizeSpeech`, `useGetAccount`, and `@/lib/audio` (expo-audio cannot load in jest; setup-screen-only suites still need the `@/lib/audio` mock because the import is top-level). (2) Shared mute: `lib/gameAudioPref.ts` (AsyncStorage key `bolo.gameAudio`, `off` = muted) + `components/GameMuteButton.tsx` (exports `useGameAudio()`; `testID="game-mute-btn"`), in all five play surfaces; muted skips synthesis calls. (3) Exit affordance: every active-play state has a persistent safe-area-respecting exit control; timed games confirm via `lib/gameExit.ts` `confirmDiscardRun` (Alert on native, `window.confirm` on Expo web), and Listen and Pick (mid-round) plus Bolo Quiz (mid-quiz) also confirm before discarding a started run — exit buttons carry `testID="game-exit-btn"`, confirm/cancel behavior pinned in `__tests__/game-exit-confirm.test.tsx`. (4) Listen and Pick wrong-answer reveal holds until tap-to-continue (`testID="listen-and-pick-continue"`); correct answers keep auto-advance. (5) Phrase Builder tap-based reorder (`handlePlacedTap`: select, tap another placed tile to swap, tap again to return to tray), pinned in `__tests__/phrase-builder-reorder.test.tsx`. (6) "Bolo Quiz" is the sole user-facing name (home card and hub; routes/endpoints unchanged); home DailyQuizCard and the hub tile fail closed via `plusReady = isPlus === true && !entitlementsLoading`, pinned in `__tests__/home-quiz-gate.test.tsx` + `__tests__/games-hub-gate.test.tsx`. (7) ContinueCard deleted (component, home render, `home-continue-card.test.tsx`; the daily-goal test's mock updated) — the train journey boarding pass is the sole continue mechanism. (8) One-time name prompt: `components/NamePromptCard.tsx` on home, only when Clerk firstName is null; saves via PATCH /account/profile dual-write then `user.reload()`; dismissal persisted (AsyncStorage `bolo.namePromptDismissed`). (9) Pull-to-refresh: root `_layout.tsx` QueryClient ports the web shouldRetry policy (never retry ApiError 4xx), home drives RefreshControl from a gesture-initiated `pullRefreshing` local flag (set on pull, cleared when `Promise.allSettled` of the three refetches settles) instead of raw `isRefetching`, so background invalidations from games/review no longer flip the spinner; pinned in `__tests__/home-pull-refresh.test.tsx` including the background-invalidation case.

**Build 31 one-path restructure, mobile half (Task 906, July 31, 2026).** Mirrors the web half: (1) home (`(tabs)/index.tsx`) topic grid removed, Phrasebook door card between the boarding pass and the quiz card (same testIDs as web), chips deep-link to `/(app)/category/:id`, door opens the new `/(app)/phrasebook` screen (registered in `_layout.tsx`); tour scroll fallback adjusted (y 500 → 300). (2) Journey zone titles from the categories listing with `journeyLines.ts` fallback. (3) Express test-out: journey progression dialog gained the `link-test-out` Pressable routing to `/(app)/practice/[id]` with params `{ group, mode: 'testout' }` (LockInfo carries zoneId/groupId); `practice/[id].tsx` testout mode mirrors web exactly (sampled phrases, no attempts/XP, no retry controls, banner, one POST batch, verdict screen with testIDs `testout-passed-title`/`testout-failed-title`/`testout-checking-title`/`testout-resubmit-button`); the 402/locked handling applies unchanged because the testout query feeds the same `phrases.error` seam. Dialog copy fixed to "finish the stop before this one" (mobile had a single close control already; the duplicate X was web-only). NOTE: the same jest mock-factory rule as web — new api-client hooks used by the practice screen must be added to every `jest.mock('@workspace/api-client-react')` factory in tests importing it (18 files patched); RNTL hides `aria-hidden` elements from queries unless `includeHiddenElements: true`. Tests: `home-phrasebook-door`, `phrasebook-screen`, `practice-testout`, plus journey-map dialog updates.

**Client Trust second factor (Task 857, July 30, 2026, ships in build 27).** `needs_client_trust` (Clerk Client Trust: password sign-in from a new device) is now routed instead of falling into the unexpected-state branch (which is what build 26 did — visible error + Sentry). When `signIn.supportedSecondFactors` offers `email_code`, the screen auto-sends via **`signIn.mfa.sendEmailCode()`** and reuses the existing code-entry step (a `verifyPath: 'firstFactor' | 'clientTrust'` state picks which API the verify/resend buttons call — the second factor uses `signIn.mfa.verifyEmailCode({ code })`, NOT the first-factor `signIn.emailCode.*`). Verify at `complete` finalizes via the shared `finalizeSession`; any other status falls back into `handleNonCompleteState`. `needs_client_trust` without an `email_code` second factor surfaces the status + second-factor strategy strings on screen and in Sentry per the standard diagnostic pattern. Tests: `__tests__/sign-in-client-trust.test.tsx`. Clerk SDK stays pinned (@clerk/expo 3.7.4 / @clerk/shared 4.25.2 — `mfa.*` verified present there).

**Journey line accent swap, Hindi ↔ Assamese (Task 912, July 31, 2026).** In both `artifacts/gujarati-coach/src/lib/journeyLines.ts` and `artifacts/bolo-mobile/lib/journeyLines.ts` (kept identical): Hindi (Ganga Line) accent is now `#65a30d` (lime) and Assamese (Kamrupa Express) is `#f43f5e` (rose-500). History: the task originally brightened Hindi's `#e11d48` to `#f43f5e`, then the user directed a straight swap with Assamese's lime instead. These accents drive the home boarding pass, journey rail/postcards, and pass glow on both platforms; no other code references the hex values.

---

**Build 31 mobile parity batch (July 31, 2026; bolo-mobile only, plus shared journey components).** Eight web-parity items in one pass. (1) `components/journey/TrainEngine.tsx` rewritten as the multicolor rn-svg port of web `train-svg.tsx`: chassis `foreground`, body `primary`, trim `secondary`, steam `cardBorder`; the old `color` prop is now `tint` and colors ONLY the headlamp. `motion` prop (`'drive' | 'bob' | 'none'`) drives whole-wrapper transform keyframes; wheels spin via `useAnimatedProps` rotation, steam puffs are phase-shifted AnimatedCircles. viewBox `0 -14 64 56` gives steam headroom above the layout box; Svg is absolute + numerically sized inside a width×height wrapper (TicketParts sizing contract), testID `train-engine`. Shared loop timing lives in new `lib/useLoopProgress.ts` (withRepeat 0→1, rests at 0 when disabled/reduced-motion). (2) `JourneyPassCard.tsx`: idle motion (breathe scale, accent glow `pass-glow`, measured shimmer band `pass-shimmer`, arrow double-pump) + progress-aware 3-state CTA (`Start your journey` / `Resume at Stop N · X phrase(s) to go` / `Continue your journey`) + stub tear-off on activate: 600ms tear with a 16% grip waypoint and jagged `TornEdge` zigzags, `onPress` fires at 500ms, card resets at 1200ms; reduced motion activates instantly. Belt caps/testIDs from build 28 unchanged. (3) Journey map dressing (`journey.tsx`): current station card becomes a signboard (roof bar `signboard-bar`, glyph `station-sign-glyph`, pulsing ring `stop-glow`, light/dark station surface); non-current stops carry a zone-color tick `stop-tick-<id>`; attempted stops trade the "x/y mastered" status suffix for an 80px progress track (`stop-progress-<id>`) + "x/y mastered" label; the rail segment from the current stop to the next carries a directional dash pulse (`rail-pulse`, AnimatedPath strokeDashoffset), skipped under reduced motion. (4) Practice + review barge-in parity (#913): `blocked = evaluating` only — the record button stays enabled during coach playback (hold stops the audio and records on the same gesture); "Listen first" hint/dim removed; fixed slots pinned (waveform-slot h22, record-hint-slot h40, 88×88 button). (5) Chat mascot barge-in (#890): the on-screen mascot's `onPressIn` now fires during `phase === 'playing'` too (nav-bird parity); disabled only by `capExhausted`. (6) Review "We heard" romanized line (#914): same show/hide contract as practice. (7) Bolo Quiz gate fail-closed (#892): `entitlementsLoading` early-return before the `!isPlus → paywall` replace. Tests: `train-engine.test.tsx`, `journey-pass-motion.test.tsx`, `quiz-plus-gate.test.tsx`, `review-we-heard-romanized.test.tsx`, `record-barge-in-stability.test.tsx`, +5 in `journey-map.test.tsx`, +1 mascot barge-in in `nav-bar-hold-to-talk.test.tsx`. Device-verify list: tear feel, glow/shadow on Android (invisible on transparent views; elevation on tear halves), steam clipping, signboard in dark mode, pulse direction, barge-in gestures.

**Phrasebook door + tour removal (Task 906, July 31, 2026; mobile half).** Mirrors the web change. (1) Home (`app/(app)/(tabs)/index.tsx`): Topics section, CategoryCard, and ErrorNote removed; a Phrasebook door card (accessibility label "Open the Phrasebook") renders directly below `JourneyPassCard`, pushing `/(app)/phrasebook`; chip row = first 3 categories pushing `/(app)/category/:id` (label "Open the {title} topic", mastered/total badge only when started) plus a "+N more" chip. (2) New `app/(app)/phrasebook.tsx` screen (registered in `_layout.tsx`): CategoryCard and its styles moved verbatim from home, cards push the existing `/(app)/category/[id]` so gating is unchanged, fires `phrasebook_opened` once with `{ language }`. (3) Guided tour REMOVED: deleted `contexts/TourContext.tsx`, `components/GuidedTour.tsx`, and 3 tour test files; `_layout.tsx` no longer mounts TourProvider/TourBootstrapper; account's Replay Tour row removed but the `hasCompletedTour` prefs-merge lines in `account/index.tsx` are KEPT deliberately (API contract merge helper). (4) Journey zone-title dedup identical to web (`app/(app)/journey.tsx` + `lib/journeyLines.ts` comment). Tests: `__tests__/phrasebook-door.test.tsx` (7: 4 home door + 3 phrasebook screen); `language-choice-gate.test.tsx` rewritten without tour mocks (4 tests to 3).

---

## 7. Assets, libraries, and audio

- **Animation:** framer-motion on web, react-native-reanimated on mobile. No Rive, no Lottie, no canvas-confetti.
- **Mascot:** every surface (web, mobile app, video artifacts, legal-page header, hosted email images) renders the five canonical pose PNGs; on web `Mascot` animates them as whole images (crossfade/bob/tilt — see the mascot.tsx row). The Task 836 SVG rig was retired July 30, 2026 (non-canonical art) and `bolo-rig.tsx` must stay unreferenced. No Rive state machine exists.
- **Confetti:** hand-rolled on both platforms, not a library.
- **Sound cues:** a `playCue` layer exists on both platforms (Spec 1 v3) but **no cue audio files exist**. Mobile `CUE_SOURCES` is empty; web 404s silently. Cues are silent no-ops until assets ship.
- **Coach TTS playback already existed** and is separate from the cue layer.
- **Band result clips (Task 903, July 31, 2026):** six bundled mp3s per platform — `perfect, great, good, almost, retry, nocatch` — in web `public/sounds/bands/` and mobile `assets/sounds/bands/` (0.8–1.7s each). Generated once by `artifacts/api-server/scripts/genBandClips.ts` (gpt-4o-mini-tts, voice nova = the feedback voice, per-clip tone instructions; nocatch is deliberately NEUTRAL per the system-miss rule), loudness-normalized with ffmpeg loudnorm I=-16 (24kHz mono 48k). They speak English band names, so the set is voice-static, not per-language — regenerate with the script if the default TTS voice ever changes. Playback wiring: see the `band-audio.ts` rows in §5/§6 and `lib/feedbackTts.ts` in §4. Live-verified July 31, 2026 via `qa/task903-band-audio-probe.mjs`: band clip starts ~20ms after the pronunciation response (no synthesis wait), feedback sentence follows after the clip, server prewarm join observed as `hit: "pending-prewarm"`.

### Audio recording

**Web:** custom `useVoiceRecorder` hook at `lib/integrations-openai-ai-react/src/audio/useVoiceRecorder.ts`. Uses `MediaRecorder` and holds the `getUserMedia` `MediaStream` in `streamRef` with its own `AudioContext` and `AnalyserNode` computing RMS levels. Since Spec D2 the analyser is **always** created in `startRecording` (silence detection reuses it via `readRms()`), and the hook exposes `getAmplitude()` — a pull-based 0..1 value (RMS x5, clamped). Callers sample it themselves (rAF loop); it never triggers React renders.

**Mobile:** `expo-audio`. `recorder.record()` and `stopAndReadRecording(recorder)`. Metering is enabled via `isMeteringEnabled: true` in `RECORDING_PRESET`. `useAudioRecorderState(recorder, 60)` (60ms in practice and review since Spec D2; was 250ms) exposes a **dBFS** `metering` value, also used for adaptive silence auto-stop — auto-stop timing is wall-clock based, so the faster poll does not change its behaviour.

> Metering gotchas: the value is dBFS (logarithmic, mostly negative), so map roughly -50..0 onto 0..1 with clamping — use `meteringToAmplitude()` in `lib/audio.ts`, never a second inline mapping. A linear read looks dead until someone shouts.

**Visualisation (Spec D2):** web `SoundWavePulse` (`src/lib/motion.tsx`) accepts an optional `amplitude?: MotionValue<number>`; with the prop it renders audio-driven bars, without it it keeps the old time-based loop. Mobile has `components/Waveform.tsx` (7 Reanimated bars off a `SharedValue`, reduced-motion fallback = 5-dot static level meter). Both practice/review screens scale the mascot 1.0-1.08 with live amplitude (MotionValue on web, SharedValue on mobile — never React state) and show a "We can't hear you" hint after >1.5s of near-silence. Reduced motion disables the mascot scale and swaps the waveform for the static meter (`prefersReducedMotion()` at the sampling sites).

---

## 7a2. Email sending via Resend (July 28, 2026)

Two distinct Resend mechanisms coexist:

1. **Replit Resend connector** (`@replit/connectors-sdk`, no API key in app secrets) — used by friend invites (`artifacts/api-server/src/lib/inviteEmail.ts`) and family-plan invites (`artifacts/api-server/src/lib/familyInviteEmail.ts`). Both call `connectors.proxy("resend", "/emails", ...)` — the proxy must be addressed by **connector name** (`"resend"`), not the `conn_…` connection id (the id form stopped resolving and returns 404 "no connection found"). The connector's key is send-only (GET `/emails` returns 401 `restricted_api_key`), so delivery status cannot be queried through it.
2. **Raw `resend` npm SDK with `RESEND_API_KEY`** — used by ElevenLabs quota alerts (`quotaAlertEmail.ts`; sender `ELEVENLABS_ALERT_FROM` ?? `Bolo! <onboarding@resend.dev>`) and the contact form (`routes/contact.ts` → `resendClient.ts`; sender `RESEND_FROM` ?? `noreply@boloapp.in`, which is NOT a Resend-verified domain and 403s — see debt table).

**Invite CTA + template (July 28, 2026):** CTA destination is `INVITE_CTA_URL` (dev+prod env = `https://bolo-india.app`; code default is the same web URL — flip the env var to the App Store link at iOS launch, no code change). Both invite templates carry a properly hidden preheader div and a hosted mascot header image (`https://bolo-india.app/mascot/mascot-wave.png` — hosted, not base64, because Gmail strips data URIs). Family invites' CTA remains the personal join link.

**Contact-form sender:** `RESEND_FROM` (dev+prod env = `hello@bolo-india.app`), no hardcoded fallback — `fromAddress()` throws if unset, caught by the best-effort send path (logs + `email_sent=false`).

**Invite sender address:** `INVITE_FROM_EMAIL` (server-side env var, both dev and production; no `VITE_*` involvement). There is deliberately **no hardcoded fallback** — a missing value throws at send time. Dev and production Replit env vars are set to `hello@bolo-india.app` (the bolo-india.app domain is Resend-verified: DKIM/MX/SPF green). Because it is a server-side env var (not a deployment secret and not a build-time `VITE_*` value), the production env var is read at runtime by the deployed api-server; the workspace-secrets-baked-into-Vite trap in §8 does not apply.

## 7b. Observability (Sentry + PostHog, July 28, 2026)

**Sentry (error reporting, all three apps).** Initialized ONLY when the DSN env var is present; without it every call is a no-op. Each init scrubs PII in `beforeSend`/`beforeBreadcrumb`: emails masked by regex, and keys like `transcript`, `audioBase64`, `nativeScript`, `romanized`, `english`, `email*` redacted. `tracesSampleRate: 0` (errors only), `sendDefaultPii: false`, Sentry user = id only.

| App | Init file | DSN env var | Notes |
|---|---|---|---|
| api-server | `src/lib/sentry.ts` (imported FIRST in `index.ts`) | `SENTRY_DSN` | `Sentry.setupExpressErrorHandler(app)` + a new global express error handler in `app.ts` (500 JSON, logs via pino). `@sentry/node` is in build.mjs `external`. Dev-only test route `GET /api/__sentry-test` (throws; absent when NODE_ENV=production) |
| gujarati-coach | `src/lib/sentry.ts`, called from `main.tsx` | `VITE_SENTRY_DSN` | Dev-only verification: open with `?sentry_test=1`. `setSentryUser` synced from Clerk in `App.tsx` `AnalyticsIdentitySync` |
| bolo-mobile | `lib/sentry.ts`, called at module load in `app/_layout.tsx`; root export is `Sentry.wrap(RootLayout)` | `EXPO_PUBLIC_SENTRY_DSN` | app.json plugin is `["@sentry/react-native/expo", { organization: "lark-enterprises-llc", project: "bolo-mobile" }]`; source map upload at EAS build still needs `SENTRY_AUTH_TOKEN` as an EAS secret (see debt). Dev-only verification: `EXPO_PUBLIC_SENTRY_TEST=1` |

**PostHog (product analytics, both clients).** Event names live in ONE constants file per app — `src/lib/analyticsEvents.ts` (web) and `lib/analyticsEvents.ts` (mobile) — the deliberate, complete set: `sign_up_completed`, `language_selected`, `first_practice_session_started`, `first_phrase_attempted`, `session_completed`, `paywall_viewed`, `purchase_completed`. Wrappers in `src/lib/analytics.ts` / `lib/analytics.ts` (`track`, `trackOnce` — once per browser/install via localStorage/AsyncStorage `bolo.analytics.once.*`, `identifyUser` — Clerk user id only). Autocapture, pageviews, and session recording OFF. Init only when `VITE_POSTHOG_KEY` / `EXPO_PUBLIC_POSTHOG_KEY` present (`VITE_POSTHOG_HOST`/`EXPO_PUBLIC_POSTHOG_HOST` optional, default US cloud). **Payload rule: no phrase content, transcripts, audio, or email — user id and language code only** (plus numeric counts).

Hook sites: web `App.tsx` (sign-up via `user.createdAt` < 2 min, also identity sync), `account.tsx` `handleChangeLanguage`, `practice.tsx` (session start effect, evaluate call, summary transition), `upgrade.tsx` (`Paywall` mount, `?checkout=success`). Mobile `app/_layout.tsx` (`AnalyticsIdentitySync`, same createdAt rule — covers email/Google/Apple without touching auth screens), `language.tsx` `choose`, `practice/[id].tsx` (list-loaded effect, evaluate call, done transition), `paywall.tsx` (mount, `runPurchase` success).

**Correction to section 5:** web practice's summary state literal is `"summary"` (line ~791 `setState("summary")`), not `"done"` as previously written; `"done"` is mobile-only (`phase === 'done'`).

---

## 7c. D1a Slice 1 — lesson grouping data layer (July 28, 2026)

- NEW table `lesson_groups` (lib/db/src/schema/lessonGroups.ts): the journey-map "station" unit — ordered partitions of a (language, category)'s phrases into ~10-phrase chunks. Unique (language_code, category_id, position) — NOT the spec's (category_id, position), because categories are shared across languages. `title` null pending fare-zone/station naming.
- NEW nullable columns `phrases.lesson_group_id` (FK) and `phrases.lesson_group_position` (1-based within group).
- Migration: `0023_lesson_groups` (drizzle generated it as a duplicate `0022_` prefix — artifact of the healed journal; file + journal tag renamed to 0023 by hand, DDL untouched).
- Grouping backfill: `artifacts/api-server/src/scripts/backfillLessonGroups.ts`, run at startup after scoring v2 (advisory lock 727_003). Idempotent: skips any (language, category) that already has groups. Ordering tiebreak inside a stage block: `(sort_order, id)`. Stage blocks (phrase, then sentence) are never interleaved within a group; positions number phrase-stage groups first. Tail chunks of ≤4 merge into the previous group (sizes mostly 8-13; a stage block of ≤7 with no previous group stays small — observed mins 5-7 in gu/mni/brx).
- Result on migration day: 620 groups over 132 (language, category) pairs, 22 languages, median size 10, ZERO unassigned phrases; concatenation property (group order reproduces (sort_order, id) order per stage) verified for gu/hi/ta.
- NEW endpoints (both additive; openapi.yaml + regenerated clients): `GET /categories/{id}/lesson-groups/{lang}` → `{ lessonGroups: [{id, position, title, phraseCount, attemptedCount, masteredCount}], unassignedCount }` (progress derived at read time from attempts; no stored counters); `GET /lesson-groups/{id}/phrases` → same per-phrase shape as the category listing (Phrase schema), ordered by lesson_group_position, premium-filtered identically. Existing endpoints untouched.
- The existing `lessons` table is a per-(language, category) AI-content-cache record — a MISNOMER kept as-is (renaming is non-additive; deferred cleanup, see debt). The journey-map grouping is `lesson_groups`.
- Tests: `learning.lesson-groups.test.ts` (endpoints + partitionIds unit).
- Post-review hardening: migration `0024_phrase_group_position_unique` adds a UNIQUE index on `phrases (lesson_group_id, lesson_group_position)` (NULLs never conflict), guarding the concatenation invariant against future writers. Since July 29, 2026 the backfill runs AFTER the server listens (listen-first startup), so a replenisher write can in principle race the first-boot backfill window; both sides hold advisory locks and the unique index plus retry-then-fallback bounds the damage to a NULL-group insert that self-heals on a later backfill; a cross-scope composite FK (group and phrase agreeing on language/category) and a race regression test are deferred to Slice 2 (see debt).

### D1a Slice 2, lesson group unlock model (July 28, 2026)

- NEW tables (migration `0026_slice2_unlock_model`, generated as duplicate `0025_` prefix and renamed by hand pre-application; DDL reordered so the composite FK follows its target unique constraint): `lesson_group_progress` (composite PK user_id + lesson_group_id; only `tested_out` is persisted today, all other statuses are derived at read time) and `lesson_group_testouts` (append-only submission log: user, group, passed, created_at; keeps history for future rate limiting).
- Composite-FK scope hardening: `lesson_groups` gains UNIQUE (id, language_code, category_id); `phrases` gains composite FK (lesson_group_id, language_code, category_id) referencing it, MATCH SIMPLE so NULL-group rows stay legal. A phrase can no longer point at a group in a different language or category. This closes the Slice 1 "scope not DB-enforced" debt row.
- Unlock derivation (`artifacts/api-server/src/lib/lessonGroupUnlock.ts`, pure): statuses are locked / unlocked / in_progress / completed / tested_out. First group always unlocked; a group unlocks when the previous one is completed or tested_out. Completed = at least 80% of the group's phrases at bestScore >= 80 (the attempts-based mastery signal; FSRS remains review-scheduler only). Completed takes precedence over tested_out. Monotonicity is enforced by a LATCH (post-review fix): replenishment grows a group's denominator with fresh phrases, which would dilute a completed ratio below the threshold, so the lesson-groups GET persists a `completed` progress row the first time completion is observed and the derivation honors persisted completed/tested_out rows regardless of the live ratio. Nothing ever re-locks.
- Entitlement precedence: all deny* gates run BEFORE unlock computation; a gated caller gets the 402 payload and never sees unlock state.
- Test-out endpoints (additive, in openapi.yaml + regenerated clients): `GET /lesson-groups/{id}/test-out` returns a fresh random sample (5 phrases, or all accessible if fewer) plus sampleSize and requiredCorrect = ceil(0.8 x n); `POST` validates server-signed evaluationTokens (user match, phrase-in-group, distinct, exact sample count), passes on requiredCorrect attempts at a full-credit band (`isFullCreditBand`, legacy `nailed`), records every submission, and upserts `tested_out` progress on pass. Retryable with a new sample. If every phrase in a group is premium-filtered away, the assessment itself 402s (feature_locked).
- `GET /categories/{id}/lesson-groups/{lang}` now returns an optional additive `status` per group.
- Replenisher insert-time assignment (strategy A, cap 14): new phrase-stage rows append to the LAST phrase-stage group while it is under 14 phrases; overflow creates a new phrase group at (last phrase position + 1) and shifts sentence-stage groups up by one, all in a single transaction (two-phase sign-flip position update to dodge the unique index). Retries once on unique violation, then falls back to the legacy NULL-group insert (surfaces via unassignedCount rather than failing the request). Progress keys on group id, never position, so shifts cannot orphan progress.
- Tests: `learning.lesson-groups-unlock.test.ts` (derivation units, endpoint statuses, test-out pass/fail/forgery/402 precedence, replenisher append + overflow shift + concurrent-writer race). Replenish test cleanups now delete `lesson_groups` (and the new suite deletes `lesson_generations`) because the replenisher creates groups at insert time.

## 8. Known debt and open items

| Item | Notes |
|---|---|
| Prod reconcile-on-read against RevenueCat is broken (July 29, 2026, Task 818) | The Replit RevenueCat connector token is v2-scoped ONLY: every `/v1/...` call returns 401 "Invalid API Key" (code 7225), and v2 customer writes 403 (missing `customer_information:customers:read_write`). `fetchSubscriber` in `revenuecatClient.ts` uses v1 `GET /v1/subscribers/{id}` through the connector, so the reconcile-on-read safety net silently no-ops in production (401 → non-OK → null → stored state untouched). Webhooks are the ONLY working sync path and did fire correctly for the Task 818 promotional grants. Fix options: route reconcile through a stored V1 secret key (one now exists as `REVENUECAT_SECRET_API_KEY`, owner may revoke), or get the connector v1-capable. Also learned: v1 `POST .../entitlements/{id}/promotional` no longer auto-creates the subscriber (404 code 7259); `GET /v1/subscribers/{id}` does (201) — prime with a GET first. Grant tooling: `scripts/src/grantPromotionalPlus.ts` |
| Mobile build 26 shipped against the new RevenueCat project (July 29, 2026) | EAS production env now carries `EXPO_PUBLIC_REVENUECAT_IOS_API_KEY` / `_TEST_API_KEY` for project `proja487649a` plus `EXPO_PUBLIC_REVENUECAT_ENTITLEMENT_ID=plus` (no ONE_LANGUAGE vars ever existed in EAS — nothing was removed). Purchase client needed zero code changes: the new project has no one_language offering, so the paywall's tier toggle never renders and it degrades to a single All-Access surface; all live prices come from store metadata. iOS buildNumber 26 (EAS build `04843e1b`) built and reached TestFlight even though all three `eas submit` attempts reported ERRORED with a null error — a false negative; check TestFlight before resubmitting. ASC "Submit for Review" remains a manual owner step. Known leftover: the workspace shared env still holds the OLD project's public SDK keys (dev-only mismatch; proposed task was cancelled) |
| Canonical mascot for icon art (July 29, 2026) | The canonical Bolo mascot is the teal/blue bird in `artifacts/bolo-mobile/assets/images/mascot/` (five pose PNGs; web copies in `artifacts/gujarati-coach/public/mascot/`). `assets/branding/icon.svg`, `assets/branding/adaptive-icon.svg`, and the pre-July-29 `assets/images/icon.png` depict a NON-canonical green parrot and must never be reused as icon sources. `assets/images/icon.png` + `adaptive-icon.png` are now composited from `mascot-wave.png` (existing sprite pixels only, no generated art). Strengthened July 30, 2026 (owner decision, applies everywhere, not just icons): **No new Bolo artwork may be created by any means (drawing, SVG, AI generation, tracing). The only permitted mascot pixels are the five canonical PNGs, animated as whole images.** The retired `bolo-rig.tsx` SVG is a non-canonical approximation and must not be rendered in the app |
| ~~Mobile bolo-quiz screen-level gate lacks a loading check~~ | **Resolved (July 31, 2026, build 31 batch, #892).** `bolo-quiz.tsx`'s gate effect now early-returns while `entitlementsLoading` is true, then redirects only a LOADED Free user. Pinned in `__tests__/quiz-plus-gate.test.tsx` (loading = no redirect, loaded Free = paywall, loaded Plus = stays) |
| Splash reuses icon.png at 78% mascot height (July 29, 2026) | app.json `splash.image` points at the new icon.png, so the splash shows the bird large on cream. May want a smaller mascot treatment; revisit post-launch |
| Mobile jest flake: stray haptic timer fires after teardown (noted July 30, 2026, Task 882 validation) | `practice-phrase-id-in-request.test.tsx` intermittently fails in FULL-suite runs under load (`evaluate` call-count assertion, ~30s suite time) yet passes 6/6 in isolation — a `hapticHeavy` `setTimeout` in `app/(app)/practice/[id].tsx` (~line 1102) fires after the jest environment tears down ("import after teardown" ReferenceError; even an isolated PASS exits 1 from it). Pre-existing, not tied to any web change. Fix someday: clear the timer on unmount or fake timers in that test |
| Mobile practice: the bird is not the record button (noted July 30, 2026, Task 882) | Web practice makes Bolo himself the hold-to-speak target (full rendered bird, head to feet). Mobile practice (`app/(app)/practice/[id].tsx`) instead uses a separate 88px circular mic Pressable (`record-button`) below the phrase card; the 104px mascot in `mascotRow` is not touchable at all. No head-vs-belly under-coverage exists there (there is no bird hit target to under-cover), but the interaction pattern diverges from web. Deliberately left for the next mobile pass — build 29 was already compiling and a hit-target polish does not warrant a respin |
| Practice/review flash logic duplicated on mobile (noted July 30, 2026, batch 1 addendum #891; both copies migrated to five-band IN PLACE by Task 901 — the duplication itself remains) | `app/(app)/practice/[id].tsx` and `app/(app)/review.tsx` each carry their own copy of the score-flash/shake logic (`flashOpacity`/`flashColor` + the retry-only-never-nocatch gate). Review's copy already implements the nocatch gate correctly (verified by inspection — it predates batch 1 and its comments cite the Spec 1 rule), but NO review-screen test pins it: `practice-flash-neutral.test.tsx` covers the practice copy only. Extract a shared helper and add review-screen flash coverage in a future polish pass (helper extraction was explicitly deferred out of the addendum). Web side: the addendum shipped the CTA flip in `gujarati-coach` practice.tsx (band `retry` → "Try again" primary, "Next phrase" secondary; hard-error "Record again" unchanged); web's standard layout still labels the secondary "Retry" for non-retry bands by design |
| Contact-form sender likely 403s (July 28, 2026) | `resendClient.ts` defaults `RESEND_FROM` to `noreply@boloapp.in`, which is NOT verified with Resend (an in-code comment in quotaAlertEmail.ts records the 403). The contact form swallows the failure (`email_sent=false`) so it fails silently. Reported during the invite-sender migration; deliberately NOT changed (out of scope). Fix: set `RESEND_FROM` to a bolo-india.app address or migrate to the connector |
| `/contact` rate limiter is in-memory (noted July 30, 2026, low priority) | `routes/contact.ts` throttles via `createRateLimit` (in-memory sliding window, 3 per 10 min) — resets on restart and is per-replica, unlike every other throttle (test-out, phrase reports), which counts DB rows. Migrate to the DB-backed pattern someday; deliberately untouched in Spec B2 |
| ~~Clerk production keys stale in EAS~~ | **Resolved (July 28, 2026).** Production Clerk instance `clerk.bolo-india.app` is live (DNS verified, Apple + Google SSO). EAS production env carries the correct `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` pk_live and the stale `EXPO_PUBLIC_CLERK_PROXY_URL` was removed. Remaining owner step: set `CLERK_SECRET_KEY` sk_live in the deployment settings of the Publishing tool and republish. See "Clerk key locations" in section 1 |
| Observability keys | Development AND production env vars now set (`SENTRY_DSN`, `VITE_SENTRY_DSN`, `EXPO_PUBLIC_SENTRY_DSN` (dev), `VITE_POSTHOG_KEY`, `EXPO_PUBLIC_POSTHOG_KEY` (dev); Sentry org o4511813816352768 / lark-enterprises-llc, PostHog US cloud). Web also carries committed production fallbacks (public write-only DSN/phc values) gated on `import.meta.env.PROD` because the deployment build does not reliably see production env vars (see trap below). Still manual: EAS production env `EXPO_PUBLIC_SENTRY_DSN` + `EXPO_PUBLIC_POSTHOG_KEY`, and `SENTRY_AUTH_TOKEN` as an EAS secret for source map upload. Dashboard-side arrival of the dev test events is unverified (no auth token available to the agent); confirm in Sentry/PostHog UIs |
| Stale dev-Clerk cookies poisoned prod auth (July 28, 2026) | Browsers that visited bolo-india.app during the dev-keyed window carry dev-instance JS cookies on the apex; they ride to clerk.bolo-india.app and break OAuth (err_code=authorization_invalid) and email sign-up silently. Server, DNS, Google creds, and instance config were all healthy. Remedied by a detect-then-purge cleanup in gujarati-coach src/lib/clerkCookieCleanup.ts (called from main.tsx before Clerk init). v2 (July 28, 2026): purges ONLY when a dev-era distinguisher cookie is visible (__client or __clerk_db_jwt* on the apex; prod's __client is HttpOnly on clerk.<domain> so invisible); marker is a first-party cookie (bolo_clerk_dev_cleanup=v2, Domain=.bolo-india.app, Secure), written and read back BEFORE any delete as a write-probe — the v1 localStorage marker silently failed under privacy extensions and looped users out after every sign-in. Temporary: remove after the affected window ages out. RESOLVED July 28, 2026: user-verified in an extension-laden browser; one pre-fix poisoned browser needed a single manual cookie clear (state unreachable post-fix); no real-user impact remains |
| TRAP: deployment build bakes workspace secrets into Vite `VITE_*` values | Observed July 28, 2026: production env vars held pk_live but the published bundle carried the workspace pk_test secret, so the Replit deployment build reads workspace secrets, not production env vars, at build time. Clerk's `publishableKeyFromHost` returns a DEV fallback unconditionally, so bolo-india.app served the dev Clerk instance. Fixed client-side: App.tsx derives pk_live from the hostname at runtime on bolo-india.app / www; sentry.ts and analytics.ts carry committed PROD fallbacks. Any future build-time `VITE_*` value must NOT rely on production env vars alone |
| Web journey stamp still truncates long names (noted July 30, 2026, Build 30 batch 1) | `artifacts/gujarati-coach/src/components/ticket.tsx` keeps the pre-fix ZoneStamp treatment mobile just outgrew: fixed-size slot that clips the -12deg rotated corners and a `max-w-[80%] truncate` name that ellipsizes most real geoNames ("Thiruvananthapuram Central"). Port the mobile fix (rotated-extent slot sizing + wrap-not-truncate with per-word font fit, `TicketParts.tsx` `zoneStampExtent`/`stampNameFontSize`) to web. Deliberately not done in batch 1 ("fix exactly these, nothing else") |
| `playwright-core` restored as a root devDep (July 30, 2026) | Every `qa/*.mjs` browser harness imports it, but it was missing from the tree (an earlier merge dropped it), so all QA probes failed with ERR_MODULE_NOT_FOUND. Re-added with `pnpm add -w -D playwright-core`. Run harnesses with `CHROME_BIN=$(which chromium)` |
| No cue audio files | `playCue` exists on both platforms with nothing to play. Source real tabla or dholak samples; do not synthesize |
| Orphaned tour persistence (Task 906, July 31, 2026) | The guided tour was removed from both clients, but api-server was out of scope, so `users.has_completed_tour` and the account-preferences `hasCompletedTour` PATCH field remain live-but-unread (mobile `account/index.tsx` still merges the field to honor the API contract). Remove column + field + openapi entry in a dedicated api-server task |
| `latencyMs` unenforced | Neither client sends it. Spec 0 rule 47 is a no-op. #777 has nothing to measure |
| `todayXp` in-memory filter | `learning.ts` pulls the full ledger and filters in application code. Needs a SQL date-range filter |
| api-server suite baseline (Task #947, July 31, 2026) | **All-green, no exceptions.** Any api-server test failure is NEW breakage — no cross-checking against a pre-existing list is required or appropriate. The former #782 baseline of 14 failures is fully retired: warmGreetings x2 (stale ElevenLabs voice-ID assertions → pinned to `phraseAudioIdentity().voice`); review ordering x3 (route migrated to FSRS `userItemMemoryTable` → tests now seed it); entitlementsGating suite x1 (after() FK violation: xp_ledger deleted before users now); analytics x2 + progress/summary x1 (route reads XP from `xpLedgerTable` → tests seed it); TTS cache x3 (route uses `phraseTtsCacheKey` → tests updated from `ttsCacheKey`); parrot system-prompt x2 (stale string assertions removed to match current prompt). Pre-existing unattributed: freePhraseReplenish (3, mock.module isolation) + teaser 4-state model (3) — owned by separate tasks |
| C1 Gujarati sentence pilot shipped, other languages pending (July 29, 2026) | Gujarati sentence stage grown 8 to 51 per category via committed `lib/db/src/data/curatedSentencesC1.json` (43 generated entries per category, `origin:"generated_c1"`), generated offline by `artifacts/api-server/scripts/generateSentenceTopupsC1.ts` (`pnpm --filter @workspace/api-server run generate-sentences-c1`). `sentenceCount(slug, languageCode)` is language-aware (gu = 8 + C1 file length, others 8); all Gujarati seed/backfill consumers MUST use `gujaratiLessonsWithC1()`, never `GUJARATI_LESSONS` directly, or exact-count sentence validation fails. Seed inserts are chunked (50/batch); chunking alone did NOT keep the first rollout boot inside the promote port-open window, which is why startup is now listen-first (see section 8 notes on `index.ts`). Two-check QA (blind back-translation + native-grammaticality judge, `artifacts/api-server/scripts/qaSentencesC1.ts`, frozen report `qa-c1-report.json`) ran July 29, 2026: initial 185 ok / 23 minor / 50 major; all 50 majors deleted and regenerated with the dative/experiencer grammar constraint now baked into `generateSentences` (generic rule for all languages, Gujarati few-shot examples injected only for Gujarati). Replacement pass: 38 ok / 4 minor / 7 major of 49 judged; residual majors are mostly gloss-nuance judge strictness, one dative slip. Judge CAVEAT: the grammaticality prompt names dative constructions, which produces false positives on correct nominative sentences — adjudicate flags, do not auto-trust. Minor romanization items live in the #809 backlog. Extended to 16 more languages July 29, 2026 (see the C1 21-language rollout row) |
| C1 sentence rollout: 16 of 21 languages shipped, 5 withdrawn (July 29, 2026) | Sentence stage grown 8 to 51 per category for as, bn, doi, hi, kn, mai, ml, mr, ne, or, pa, sa, sd, ta, te, ur via committed `lib/db/src/data/curatedSentencesC1Rollout.json` (keyed language code then category slug, 43 entries each, `origin:"generated_c1"`, 4,128 rows total). Scripts: `generateSentenceTopupsRollout.ts` / `qaSentencesRollout.ts` / `pruneRolloutMajors.ts` (pnpm scripts `generate-sentences-rollout`, `qa-sentences-rollout`, `prune-rollout-majors`; the first two take `--langs`, plus `--model/--out/--categories` and `--model/--in/--out` for experiments). `sentenceCount(slug, lang)` now derives from the rollout file for non-gu languages; ALL curated-file seed/validation consumers MUST go through `curatedLessonsWithC1()` (mirrors `gujaratiLessonsWithC1`), and `generateSentences` accepts an optional `model` override. Two-check QA on all 5,418 generated rows: 12 languages landed at pilot-level major rates (8.9-20.2 percent), 4 elevated (as 30.6, doi 36.4, sa 37.6, mai 41.1) converged after one prune-and-regenerate round; final residual major rates 1.2-18.6 percent (report `qa-c1-rollout-report.json`, describes shipped rows only). WITHDRAWN: kok, ks, sat, brx, mni stay at the 8-sentence curated base; both gpt-5.4-mini and a full-size gpt-5.4 generation+judging experiment (`experiment-c1-full.json`, `qa-c1-experiment-report.json`, one category each) failed at 67-100 percent major rates, dominated by sentence-gloss meaning mismatches. These 5 overlap the degraded/unsupported STT tier; their path is Bhashini probe plus native-speaker validation, not more LLM generation. Dev-DB note: gu/greetings carries 8 pre-pilot runtime-replenished sentences (source NULL), 4 of which text-collide with pilot JSON entries, so gu shows 55 there and 254 total `generated_c1` instead of 258; append-only seeding, prod unaffected |
| Lesson-group sentence top-up pass (July 29, 2026) | `backfillLessonGroups.ts` gained a second, append-only pass: unassigned sentence-stage rows in already-grouped (language, category) pairs get NEW groups positioned after the pair's current max position; existing groups and membership are never touched (completed is latched). Runtime never inserts sentences, so every unassigned sentence row is seeder content. A batch at or below the merge threshold (4) waits for more content rather than forming a tiny group |
| Curated provenance NULL in pre-existing environments | Rows seeded before the `source` column exists keep NULL (the seeder only stamps rows it inserts). Only `generated_c1` is reliable as a filter; `curated` appears only on fresh-DB seeds. Do not treat `source IS NULL` as runtime-generated |
| Pronunciation/TTS routes skip the language gate | `POST /openai/pronunciation` and the TTS route in `openai.ts` never call `denyLockedLanguage`; only the chat route gates by language. A locked-language client that already holds phrase content can synthesize audio and get evaluations for it. Pre-existing, observed during the M1 teaser work (July 29, 2026), deliberately NOT changed then. If closed later, the gate must honor the teaser exception for teaser phrase ids |
| Feedback TTS prewarm skips the ElevenLabs provider (Task 903, July 31, 2026) | `prewarmFeedbackTts` no-ops when `TTS_PROVIDER === "elevenlabs"` because per-user voice preferences make the client's eventual cache key unpredictable at eval time. With ElevenLabs active (flag currently false), feedback synthesis pays the full round trip again — the band clip still plays instantly, only the follow-on sentence is slower. If the flag flips, extend the prewarm to resolve the caller's voice pref at eval time |
| Truncated test-workflow logs | The `test` workflow's captured log keeps only the tail (~90 lines), which can cut most of the node:test failure summary and make a full-suite run look like it had only a handful of failures. Count failures only from a complete captured log (`pnpm run test > file 2>&1`), never from the truncated workflow preview |
| `phrases.register` unpopulated | Spec D2 added the nullable column + `(language_code, register)` index; no authoring or filtering yet — all rows are NULL |
| ~~Stale drizzle meta snapshots~~ | **Resolved with 0021.** Task 1's ad-hoc DDL left `meta/` lagging the committed migrations, so `generate` re-emitted applied DDL. The 0021 repair rewrote the terminal snapshot to the full current schema; `generate` now emits "No schema changes", and `check-drift` runs a trial generate on every pass so regression cannot land silently |
| Web pre-existing test failures | RESOLVED (Task 860, July 30, 2026): baseline is now ZERO — the full web suite runs green (33 files / 239 tests). The 6 `account.test.tsx` failures were stale tests asserting the voice-picker section that account.tsx deliberately unmounts with `{false && …}` while the TTS provider is evaluated; rewritten to assert the picker is hidden for both tiers, and the 4 VoiceCard preview/cache tests now render the (test-exported) `VoiceCard` directly so the cache-isolation coverage survives until the picker returns. The 2 `chat-error-banner.test.tsx` failures were harness bugs, see the row below. Any web test failure is now NEW breakage |
| TRAP: publish diff reads the committed migration chain (July 29, 2026) | The publish schema sync appears to compute its delta from the committed migration chain, not only the live dev database, so a committed-but-unapplied migration enters the publish delta. Never stage a migration by leaving it unapplied. Observed during the 0026 composite-FK ordering failure: with the re-add migration (0027) committed but unapplied, publish kept emitting `phrases_lesson_group_scope_fk` before its target unique constraint and failed; with 0027 removed from the tree the publish reported success. Support ticket filed for the FK-before-unique ordering bug. UPDATE July 29, 2026: trigger fallback executed (see the trigger-fallback row) — the FK is dropped from the committed schema (0029), so PUBLISHING IS UNBLOCKED pending the owner's generated-migrations panel pre-flight: the delta must contain NO `ADD CONSTRAINT phrases_lesson_group_scope_fk` line (expected: two lesson_group_* CREATEs, the unique constraint ADD, phrases.source ADD COLUMN). If an FK ADD CONSTRAINT still appears, STOP and rethink |
| Lesson-group scope enforced by TRIGGERS, not FK (July 29, 2026) | `phrases_lesson_group_scope_fk` dropped (migration 0029) as the fallback for the publish FK-before-unique engine bug; the same invariant (phrase's lesson_group must match its language_code and category_id; NULL lesson_group_id unconstrained) is enforced by `phrases_lesson_group_scope_trg` (BEFORE INSERT OR UPDATE OF the three scope columns) plus parent-side `lesson_groups_referenced_guard_trg` (BEFORE DELETE OR UPDATE of key columns while referenced). Both raise SQLSTATE 23503 so FK-shaped error handling still matches. DDL lives in migration 0030 AND an idempotent api-server startup guard (`ensureLessonGroupScopeTriggers`, runs BEFORE the lesson-group backfill, logs "created" vs "already present (no-op)" distinctly — the first prod boot's log line is part of post-publish verification). KNOWN GAP: triggers lack the FK's cross-row locks, so a concurrent insert-phrase/delete-group race can slip through under READ COMMITTED; no group-delete path exists today, and any future one must take explicit locking. Restore procedure for the declarative FK: docs/trigger-fallback-lesson-group-scope.md section 7 (re-add FK and drop triggers in ONE migration, remove the startup guard) |
| ~~`daily_goal` default of 10~~ | **Resolved (July 28, 2026).** Migration `0025_daily_goal_default_50` set the column default to 50 for NEW users only; the 4 existing rows at 10 were deliberately left unchanged. Server-side missing-row fallback in learning.ts updated to 50; client display fallbacks (`?? 10` in home.tsx/XpCounter) are loading placeholders and were left as-is. No prior cleanup pass had changed this |
| FSRS is mobile-only on the client | Review queue and `/review/phrases` have no web surface |
| #776 | Conjunct-nasal normalization for scripts other than Devanagari. Gated on native speaker review |
| ~~Web `EmptyState` may be unused~~ | **Wrong (verified July 29, 2026):** `friends.tsx` imports and uses the shared `EmptyState` (empty leaderboard + no-friends states); the earlier "inline EmptyState" note referred to its local `ErrorState`. Task 812 added the staggered text entrance to the shared component |
| `CountUpText` type cast | Needs `as unknown as Partial<TextInputProps>`; the prop is valid natively but absent from public TS types |
| Duplicate `useReducedMotion` imports | In both mobile practice screens. Harmless, typecheck-clean |
| Web review screen | See the contradiction noted in section 5 |
| `lesson_groups.title` all NULL | Pending fare-zone/station naming (content work, per journey-map decision record `docs/BOLO-Journey-Map-Design-Decision.md`) |
| ~~Replenished phrases stay ungrouped~~ | **Resolved (July 28, 2026, Slice 2).** phraseReplenisher assigns groups at insert time (append to last phrase group under cap 14, else new group + sentence shift). Residual: the retry-then-fallback path can still insert NULL-group rows after two consecutive unique violations; observable via `unassignedCount`, self-heals on a later backfill |
| ~~Lesson-group scope not DB-enforced~~ | **Resolved (July 28, 2026, Slice 2).** Composite FK (lesson_group_id, language_code, category_id) on phrases references UNIQUE (id, language_code, category_id) on lesson_groups; race regression test added in `learning.lesson-groups-unlock.test.ts` |
| ~~Test-out rate limiting~~ | **Resolved (July 30, 2026).** `POST /lesson-groups/:id/test-out` throttles per user per group: max 3 submissions per rolling hour, counted from the `lesson_group_testouts` log (DB-backed, same pattern as the friends invite cooldown; runs before token verification). The 4th submission returns 429 with a `Retry-After` header + `retryAfterSeconds` body — seconds until the oldest in-window submission ages out. Throttled submissions are not logged. Tests (4th-attempt trigger + 429 shape) in `learning.lesson-groups-unlock.test.ts` |
| Skeleton home (post-launch polish) | RESOLVED (Task 902, July 31, 2026): `src/components/home-skeleton.tsx` (ticket-and-card-shaped blocks) replaced the single blocking spinner; a cold-load brand splash (`src/components/brand-splash.tsx`) overlays it. See section 5 home.tsx row |
| ~~practice.tsx mic-grant guard variant (July 30, 2026)~~ | **Resolved for WEB (Task 907, July 31, 2026).** The unobserved-release hole WAS present in practice (hit-zone `onPointerLeave` doubled as a release; no window-level fallbacks). Web practice now uses the same positive hold-confirmation as chat (`beginHold` installs window `pointerup`/`pointercancel`/`blur` listeners, pointerId-matched; unmount cleanup runs silently) while keeping practice's own release-means-done-speaking semantic: a release that lands before the recorder resolves finishes/sends (chat discards). MOBILE still carries its playback gate — see the mobile barge-in parity row below |
| chat-error-banner web tests failing | RESOLVED (Task 860, July 30, 2026): three harness bugs, no product bug — the banner always rendered, just with the wrong copy or not reached. (1) The fetch mock lacked `text()`, so `res.text()` threw a TypeError that chat.tsx maps to the "mango lassi" network copy instead of the expected "ran into a snag" copy; (2) the recorder mock lacked `getLastDurationSeconds()` (same TypeError path); (3) the 5-byte mock blob tripped the ≥2048-byte size floor, ending the turn silently before fetch. Mocks fixed; both tests pass |
| `lessons` table misnomer | It is a per-(language, category) content-cache record, not a lesson. Rename deferred as a future NON-additive cleanup |
| TRAP: dev-DB `drizzle-kit migrate` recorded 0023 without executing its DDL | July 28, 2026: migrate reported success and inserted the hash row, but `lesson_groups` did not exist; DDL applied by hand via psql afterwards. Fresh-DB full-chain (`db-migrations`) applies 0023 cleanly, so the anomaly is dev-DB-local. If a table is "missing" despite migrate success, check `to_regclass` before trusting the hash table |
| TRAP: Clerk expo one-shot `signIn.password()` is NOT complete-or-throw | `signIn.password()` (Clerk "future" hooks API; there is no separate `attemptFirstFactor`) can return without error at a non-complete status (`needs_first_factor` for passwordless accounts, `needs_second_factor`, etc.). Code that assumes error-or-complete swallows those states silently — this was the July 2026 production sign-in failure. Always branch on `signIn.status` + `supportedFirstFactors`, and surface unexpected statuses in the UI and Sentry (see §6 mobile auth) |
| TRAP: migration ghost-apply (general pattern) | drizzle migrate against the shared dev DB can record a migration's hash row in `__drizzle_migrations` WITHOUT executing its DDL. Detection: `SELECT to_regclass('public.<table>')` (or check the specific column/index) for each object the migration creates, compared against the hash rows present. Fix: apply the committed migration SQL by hand via psql; never hand-edit applied migration files |
| ~~Journey showroom unreachable via web navigation~~ | **Resolved (July 29, 2026).** `language-picker.tsx` now routes locked-language taps into the `/journey` showroom (sets the locked language active first), and `language-context.tsx` permits plan-locked active languages (the allowedLanguages auto-revert branch and its entitlements query were removed; only the unsupported-code fallback remains). Server PATCH /account/preferences accepts any supported code (existence check only), so cross-device sync of a locked active language works. Verified live in a real browser (`qa/journey-map-e2e.mjs` scenario `picker`): Free user taps locked Tamil, lands on the Nilgiri Mountain Railway showroom (52 stations, free-taste chip, grayed zones), and home and chat stay upright with the locked language active, no console errors. Mobile is unaffected: it wires its own navigation when the map lands at build 26 |
| **Build-27 release checklist: re-enable Clerk Client Trust** (Task 857, July 30, 2026) | Client Trust is currently DISABLED in the Clerk dashboard because build 26's sign-in errored on `needs_client_trust`. Build 27 handles it (see the section 6 Client Trust paragraph). Manual post-ship step: once build 27 is live in the stores, re-enable Client Trust in the Clerk dashboard. Do not enable it earlier — installed build-26 clients would hit the old error again. **Real flow VERIFIED PRE-SHIP against the dev instance (July 30, 2026, dev Client Trust toggle ON):** `qa/client-trust-probe.mjs` drove the live FAPI as a fresh client (= new device) with a throwaway `+clerk_test` user — password `attempt_first_factor` returned `status: needs_client_trust` with `supported_second_factors: ["email_code"]`, `prepare_second_factor(email_code)` (the `signIn.mfa.sendEmailCode()` equivalent) returned 200, and `attempt_second_factor` with test code 424242 returned `status: complete` + a `created_session_id`. The Clerk Client Trust toggle itself is dashboard-only (verified July 30, 2026: Backend API `PATCH /v1/instance` has no trust setting, candidate endpoints 404; the only API surface is per-user `bypass_client_trust`); dev and prod toggle independently — dev is ON as of July 30, 2026, prod remains OFF until build 27 is live. Remaining device-matrix nicety (not a blocker): one on-device pass to see the email arrive and the code-entry UI complete on a real simulator/handset |
| Daily-limit client dead code + spec fields strippable in build-27+ cleanup (July 30, 2026, Task 849) | The Free daily lesson cap was retired additively: `dailyNewLessonLimit` always returns null, so the web home meter, mobile `DailyCapNote`, the `daily_lesson_limit` 402-handling branches in both clients, and the spec's daily-cap wording on `/categories/{id}/phrases/{lang}` GET/POST are now dead code paths. All deliberately kept for installed-build back-compat; strip them in a build-27+ cleanup once old mobile builds age out |
| Unlimited Free web phrase-appends (July 30, 2026, Task 849) | With the daily cap retired, a Free web user can append phrases on a topic page without any daily ceiling (only the replenisher's per-topic `FREE_PHRASE_CEILING` applies to background top-ups, not manual adds). If AI cost becomes an issue, the guard to add is a per-topic append ceiling, not a daily meter |
| RESOLVED July 30, 2026 — lesson-group unlock filtering now server-side on the phrase endpoints | Fixed per the approved D2 filtering spec: the shared guard `lib/lessonGroupAccess.ts` (see section 4) enforces sequential unlock on `/categories/:id/phrases/:lang`, `/categories/:id/sentences/:lang`, and `/lesson-groups/:id/phrases` (403 `lesson_group_locked`), with the journey listing refactored onto the same guard. Serve rule: unlocked group ∨ `lessonGroupId` NULL ∨ prior attempt (retake exemption). Entitlement 402s always run first. `/categories` listing counts stay whole-category by decision (mobile b26 progress % may trail what's practicable — cosmetic; add an additive `unlockedPhraseCount` if it matters later) |
| Test-out has no web UI (July 29, 2026) | Deferred from the D1b journey map build as follow-up task #819 (slotted with the B1/B2 small-task batch). Backend routes and generated client hooks (`useGetLessonGroupTestout` family) already exist; the journey map's progression-lock dialog is the intended entry point. NOTE for mobile: the bolo-mobile journey map at build 26 must include test-out from day one, since the web flow will exist by then |
| Zone sizes drift from the D1b layout assumption (July 29, 2026) | Decision 1 assumed 3-6 phrase and 0-6 sentence stations per zone; real data already violates both edges (gu zone 1 has 7 sentence groups after C1 top-ups, gu zone 3 has only 2 phrase groups). Cosmetic only: the rail lays out with flexGrow and renders fine at 29-59 stations per language. Revisit only if a zone grows enough to hurt scanability |
| C1 rollout WIP swept into and removed from the task-813 merge (July 29, 2026) | Uncommitted in-flight C1 rollout work (generateSentenceTopupsRollout.ts, qaSentencesRollout.ts, qa-c1-rollout-report.json ~21k lines, curatedSentencesC1Rollout.json ~31k lines, plus seedData/seedContent wiring: language-aware `sentenceCount` rollout branch and `curatedLessonsWithC1`) was auto-committed into the task-813 (mobile polish) branch, then removed in the follow-on commit because it fails its own coverage tests (seedData.test.ts expects 51 sentences per rollout language x category, frozen file has 8 — the wiring changed the count contract without updating the direct `validateCuratedLessons(loadCuratedLessons())` test path) and the rollout past Gujarati is gated on user confirmation (see the C1 pilot row). Recoverable from git history at commit `6a39953`; the original uncommitted copies likely still sit in the main workspace tree. Whoever ships the rollout must route the seedData.test.ts direct-validation calls through the merge accessor |
| Web practice barge-in hold (Task 907, July 31, 2026) | One hold on the mascot at ANY moment stops whatever audio is playing (example phrase OR spoken feedback) and starts recording on the same gesture — deliberately STRONGER than chat's stop-only barge-in (chat's pointer-down during playback only stops; practice records too, by explicit user decision). Mechanics in `practice.tsx`: `bellyActive` now includes `playing_coach` and `result` (evaluating/error/summary still unmount the zone); `startRecording` accepts those states and synchronously pauses `audioRef`/`feedbackAudioRef` + nulls the pending feedback clip before preparing the recorder; the recording state is set only AFTER the recorder resolves, so a hold that cannot record (mic pending/denied) never fakes a listening state; interrupted audio never auto-resumes. Old tests asserting "belly hidden during playing_coach" were inverted to the new contract (practice-silent-mode-prefetch, practice-stop-mode); new coverage in `practice-barge-in.test.tsx` (6 tests) |
| `transcriptRomanized` on the pronunciation eval response (Task 907, July 31, 2026) | Deterministic, display-only romanization of the STT transcript for the "We heard" line, ZERO LLM/prompt involvement (`PRONUNCIATION_RUBRIC_PROMPT` and the eval request are byte-identical; scoring provably untouched). Helper: `artifacts/api-server/src/lib/romanizeTranscript.ts` using `@indic-transliteration/sanscript` (script detected from the text's Unicode block, never the language code; Tamil MUST use the `tamil_extended` scheme — plain `tamil` emits garbage); IAST → ASCII strip → garbage guard (>15% unmapped glyphs → "") → card-style schwa deletion for hi/gu/mr/pa/bn/as/mai/doi/kok/brx (script-based fallback for Gujarati/Gurmukhi/Bengali scripts when no languageCode, e.g. client-target requests without phraseId). Coverage: 17 of 22 languages; ks/sd/ur (Perso-Arabic), sat (Ol Chiki), mni (Meetei Mayek) return "" and clients hide the line. All 5 eval `res.json` sites carry the field (nocatch/empty paths send ""); optional in openapi.yaml (mobile back-compat), never stored on attempts or in the evaluation token. Both result cards render it under the raw "We heard" line and hide it when empty OR when it equals the transcript case-insensitively (Latin passthrough would just repeat) |
| ~~Mobile practice barge-in parity PENDING~~ | **Mostly resolved (July 31, 2026, build 31 batch, #913/#914/#890).** Practice + review record buttons are `disabled` only while `evaluating` (hold during coach playback stops the audio and records; "Listen first" hint removed), review's "We heard" line gained the romanized form, and the chat mascot barges in during `playing`. RESIDUAL gaps vs web: mobile's record target is still the separate mic button, not the bird itself (#883, see the "bird is not the record button" row), so there is no hold target at all during the `result` phase; and the listen buttons remain `disabled={coachPlaying}` (harmless — the record button is the barge-in path). Audible/gesture feel is device-verify-only (#889) |
| Suite baselines after Task 907 (July 31, 2026) | Web vitest 38 files / 278 tests, all green (was 37 / 272). API node:test 648 tests, 634 pass / 14 fail — exactly the #782 enumerated baseline, no new failures (was 639 / 14 before this task's 9 romanize unit tests and 3 in-place fast-path assertions). Mobile jest 60 suites / 380 tests, all green (was 59 / 377). UPDATE July 31, 2026 (build 31 batch): mobile jest is now 64 suites / 401 tests, all green (+5 suites, +31 tests from the batch incl. 2 post-review tear-guard regressions; intervening merges account for the small drift from the 380 figure). UPDATE July 31, 2026 post-#906/#919 merges + duplicate-screen fix: 68 suites / 422 tests all green. UPDATE July 31, 2026 (Task #947): API node:test is now **681 tests, 675 pass, 6 fail** — baseline retired; 6 remaining are freePhraseReplenish (3, mock.module isolation) + teaser 4-state model (3), owned by separate tasks; see the "api-server suite baseline" row |
| Duplicate Screen registration crashed mobile on mount (July 31, 2026, post-#906 merge) | The #906 rebase left `<Stack.Screen name="phrasebook" />` registered TWICE in `app/(app)/_layout.tsx`; expo-router throws "Screen names must be unique" at mount, so the app crashed instantly while every jest suite stayed green (component tests never mount the real layouts). Duplicate removed; `__tests__/layout-screen-names-unique.test.ts` now statically scans every `_layout` file under `app/` and fails on any duplicate `<*.Screen name>` registration (plus a capture-count check so a name in a later attribute slot can't dodge the regex). Any future layout merge fallout of this class fails the suite |
| Pronunciation scoring is accent-blind (audit, July 31, 2026, report-only) | Investigation confirmed the eval pipeline is transcript-only: no acoustic signal (confidence, logprobs, timing) is requested or read anywhere, and gpt-4o-mini-transcribe resolves heavily accented attempts to the exact target text, so the five bands sit on an effectively binary signal for accent/delivery quality (phoneme substitutions ARE still caught via the transcript). Sandbox evidence + options analysis delivered in chat and in agent memory (pronunciation-scoring-signal.md): STT logprobs are a dead end; gpt-audio grading via the AI proxy separates quality but needs a 3-run median; Azure PA covers only hi/ta of 22; IndicMFA is the only full-coverage phoneme-level path. No code changed | |
| ~~Web journey map lacks the directional rail pulse~~ | **RESOLVED (Task 917, July 31, 2026).** Web now pulses the active run (current stop to next station) with a tie-mark opacity wave: accent dots sampled on the rail beziers with delay fractions growing toward the next stop. Deliberately NOT mobile's dash-offset loop; the web constraint is transforms/opacity only, and stroke-dashoffset is a paint property. See the Task 917 entry in the header list |
| Group-mode session start-position (Task 954, July 31, 2026) | When a learner taps a journey station (`?group=<id>` practice mode, web only), the session opens at the **first phrase whose `mastered` flag is false** (bestScore < 80), not always at index 0. Logic lives entirely in `practice.tsx`'s auto-start `useEffect` (deps: `[phrases, state]`): when `isGroup && !isTestout`, `phrases.findIndex(p => !p.mastered)` is the candidate; if > 0, `setCurrentIndex` is called in the same batch as `setState("idle")`. **All-mastered → index 0** (deliberate review). **Zero-attempts → index 0** (all `mastered: false`, `firstUnmastered = 0`). **Teaser stations → naturally inert** (same zero-attempts path). Mastered phrases are NOT removed from the session array; they remain reachable by back-navigation. `skipMastered` (notification deep-link) is unchanged. Mobile parity is a separate follow-up task. `serializePhrase()` in `learning.ts` already emits `mastered: boolean` per phrase for group-mode responses. Tests: 5 new cases in `practice-start-position.test.tsx`; 10 existing practice test files gained `useListCategories` stub to fix a Task 948 breakage. Suite baseline: 49 files / 335 tests, all green |

---

## 9. Working rules for the agent

Paste this block into every spec.

1. Do not run test suites automatically after each edit. Make the full set of changes, run once, report all failures together, fix in one pass.
2. Verify every edit by reading the file back. Edits have silently failed to persist in this codebase.
3. Do not batch string injection across files. Read, edit, verify, move on. Whitespace differences cause silent misses.
4. Treat a failing test as a question, not an answer. Check whether the test or the code is right before changing either. Do not change a test to match the code without confirming the code matches the spec.
5. Never hand-edit generated or dist files. Change `openapi.yaml` and regenerate.
6. New API response fields are optional, always, for mobile back-compat.
7. Never decode `evaluationToken` on the client.
8. Never introduce a second date-bucketing or reduced-motion implementation.
9. Zero output from typecheck means zero errors. Do not re-run to confirm.
10. Mobile count-up chips use the ReText pattern (animated `TextInput`, `editable={false}`). Tests must assert via `accessibilityLabel` or `getByLabelText`, never `getByText`.
11. If a named file, component, or line does not match this document, STOP and ask. Then report the discrepancy so this document can be corrected.
12. Before submitting any schema-bearing publish, read the generated-migrations panel and confirm the pending delta is exactly what you expect. Migration-stage publish failures produce NO deployment logs, so the panel is the only pre-flight visibility.
13. The lesson-group scope trigger DDL lives in TWO places: migration 0030 and the api-server startup guard (which reads the migration file as its single source of truth). Never edit the trigger functions in one place only, and never reorder the boot sequence so the guard runs after the lesson-group backfill.

---

### Journey map design decision (July 28, 2026)
D1b direction decided: **Gujarat Express (Mockup C) merged** — rail-line layout base, postcards from B as the collectible layer, scenic markers from A as garnish. Full record: `docs/BOLO-Journey-Map-Design-Decision.md`. Mockups live in `artifacts/mockup-sandbox/src/components/mockups/Journey*.tsx` (task #794, merged). Prerequisite: D1a lesson grouping.

## 10. Spec status

| Spec | State |
|---|---|
| Spec 0 v2, Task 1 (backend + backfill) | Built |
| Spec 0 v2, Task 2 (band UI + score deprecation) | Built |
| Five-band scoring display (Task 901, July 31, 2026) | Built, web + mobile. Three bands (nailed/close/retry) replaced by the five-band ladder (perfect/great/good/almost/retry) as a pure DISPLAY layer — see section 3 "Band thresholds". Server: `scoreBands.ts` single config; openapi.yaml enums + regenerated orval clients. Result cards render `BandLadder` on both platforms (labels only, achieved band filled, nocatch never shows it). Deliberate polish: nocatch pill/flash went neutral (was destructive). Behavioral consumers (XP, Elo, FSRS, streaks, mastery, test-out) migrated to credit-group helpers, byte-for-byte identical outcomes; legacy stored bands normalize at read time (no data migration). Per-attempt confetti is perfect-only. Mobile flash-color copies migrated IN PLACE in practice + review (extraction still the section 8 debt row). Mobile device verification of the ladder deferred to a follow-up device pass |
| Spec 1a v3 (XP counter + sound preference) | Built |
| Spec 1 v3 (motion engine) | Built |
| Spec D2 v2 (speaking system) | Built (register column, speaking streak, live waveforms + mascot amplitude on web and mobile) |
| Spec D1 (map and journey) | Not written. Blocked on whether a lesson map screen exists |
| Pronunciation scoring v2 (gpt-audio ensemble hybrid, July 31, 2026) | Design spec only -- `docs/specs/pronunciation-scoring-v2.md`. No code changed. Implementation is build 32+, gated on build 31 beta approval and a real-voice pilot. Pilot cancelled (Task 928); task to be re-raised when recordings are available. Key decisions spec'd: audio ensemble replaces the gpt-5.4-mini text judge for hi+gu (combined variant); fast path disabled for audio-graded languages; guards run on transcript not audio grade; 3-call parallel median with 5s timeout; fallback to transcript scoring on < 2 calls; per-user/per-process rate limits; schema addition (`audio_grade_raw`, `audio_grade_median`, `scoring_path`) listed as build-32 migration, not executed; low-resource languages (ks/sat/brx/mni) stay transcript-scored indefinitely |
| Spec D1b (journey map mockups) | Built — 3 static mockups in `artifacts/mockup-sandbox/src/components/mockups/` (`JourneyWindingPath`, `JourneyRegionChapters`, `JourneyGujaratExpress`), shared mock data in `src/lib/journeyData.ts`, mascot PNGs copied (256px) to `artifacts/mockup-sandbox/public/mascot/`. Exploration only; no production screens touched. Second round (July 29, 2026): `mockups/journey-upgrade/` adds 4 sandbox-only variants — two serpentine-track curve treatments (`SerpentineSubtle`, `SerpentinePronounced`; shared renderer `_shared/Serpentine.tsx` with per-variant `stationX`/`cardBox` config, twin-rail + sleeper-tie SVG segments, locked segments faded-dashed) and two boarding-pass ticket treatments (`TicketMinimal`, `TicketFull`) covering both the home hero pass and the map header pass. User picked pronounced curve + full ticket; both were ported to production `journey.tsx`/`home.tsx` the same day (see the section 5 serpentine + full-ticket paragraph) |
| Spec D1b v2 (journey map UI, web) | Built July 29, 2026 (web only, bolo-mobile untouched). API: showroom exception + additive listing fields + sentence-stage gate on group phrases (section 4). Web: `/journey` map, `?group=` practice mode, featured home card (section 5). Verified live in a real browser: teaser showroom (Free caller, locked gu), Plus full map (gu, 59 stations) and small-zone map (mni, 29), Free normal map (hi) with sentence and progression dialogs, latched completed/tested_out state rendering (express stamp, done count, successor unlock). QA harness: `qa/journey-map-e2e.mjs`. Showroom web entry point wired July 29, 2026 (picker to showroom, section 8 row resolved). The deferred test-out web UI shipped July 31, 2026 in Build 31 (Task 906) — see the section 5 Build 31 entry |
| Spec D1b-M (journey map, mobile port) | Built July 30, 2026 (headlines build 28; `app.json` stays 27 — EAS autoIncrement bumps at build trigger). Mobile-only translation of the web map: journey screen, home boarding-pass hero, `?group=` practice scope with 403 locked-stop card, showroom via language picker, locked dialog originally without test-out (approved ruling) — test-out added to the dialog July 31, 2026 in Build 31 (Task 906), see the section 6 Build 31 entry. See section 6 |
| Spec M1 (language teaser, 3 free phrases) | Built (api-server gating + web upgrade surface; bolo-mobile out of scope, journey-map rendering out of scope). See section 4, "Language access gating, three-state model" |
| Spec D3 (Rishta Tree, real-world quests) | Not written |
| Spec B1 (language selection onboarding) | Built July 30, 2026 (web + API); mobile parity shipped later the same day. **Gate removed later that same day by product decision (both platforms): B1 gate removed; selection screen retained as a route; flag infra retained.** Fresh accounts land directly on home defaulted to Hindi (seed behavior); tour fires on first home load. Chosen-signal column + one-way PATCH flag + communityReviewed provenance (sections 2 and 4) all live; `/choose-language` is a normal navigable route (sections 5 and 6) |
| Spec B2 (phrase report button) | Built July 30, 2026 (see the section 2 `phrase_reports` entry and the section 4 `routes/phraseReports.ts` row) |
| Spec E (copy and voice) | Not written |
| Spec F (progress and accomplishment) | Not written |
| Spec G (social) | Not written |

---

### openai.ts merge-scramble recovery (July 31, 2026)

**What happened:** Task #952 (pilot capture: tee pronunciation audio to R2) auto-merged onto Task #948 (progression completion: Polish card + zone capstone chat). Both tasks modified `openai.ts` in overlapping regions. Git's 3-way merge garbled ~400 lines, mixing greeting-TTS context with phrase-TTS context. The merged file was syntactically valid TypeScript and typechecked clean (compatible types), so no gate caught it. Root cause of the gate gap: #952's task-agent typecheck ran against its isolated pre-merge branch; no post-merge boot or typecheck ran on the combined result.

**Recovery:** Restored from HEAD~1 (c21397a, Task #948 baseline) as the clean base, then applied exactly three legitimate #952 deltas: `import { teeAudioToPilot }`, first `teeAudioToPilot` call (fast-path STT branch), second call (LLM evaluation branch), plus `rawBuffer` hoist above the try block so both call sites can reach it.

**`buildUserPrompt` fix (parrotChat.ts):** Function had three spec gaps vs `docs/specs/progression-completion.md §4`: scenario block was placed before `Language:` (should be after), `History:` label was omitted, scenario title was omitted from the scenario block. All three fixed.

**Boot smoke test added:** `artifacts/api-server/src/test/server-boot.test.ts` — two tests: app module import + openai route module import. Any parse-level corruption in any route file surfaces here before the server is manually started.

**`@workspace/db` mock completeness rule:** `openai.pronunciation.language-hint.test.ts` mocks `@workspace/db` via `mock.module`. Its `namedExports` list must include every table that `openai.ts`'s import line references. When `zoneConversationStampsTable` was added to `openai.ts` (Task #948) the mock was not updated, causing 9 test failures. Fix: add the missing entry to the mock's namedExports. **Do not add workarounds to production files to accommodate stale test mocks.** Known mock gaps (tables in schema, absent from mock): `dailyQuizCompletionsTable`, `dailyQuizzesTable`, `lessonGroupProgressTable`, `lessonGroupTestoutsTable`, `phraseReportsTable`, `scriptTraceProgressTable`, `userItemMemoryTable`. Phantom entry (in mock, not in schema): `phraseScheduleTable`. Task #922's release slot is the correct home for a shared mock factory.

### Task #922 stale-branch merge reverted whole (July 31, 2026)

**What happened:** Task #922's merge (508e05f, linear commit) landed a branch forked at b78d828 — before the freeze-exit reconstruction. It replayed the pre-fix `openai.ts` byte-for-byte (breaking the build with a parse error), deleted `lib/db/drizzle/meta/0033_snapshot.json`, and regressed the lockfile's `@aws-sdk/client-s3` specifier. Post-merge frozen install failed on the specifier mismatch.

**Recovery (product decision: undo whole, no salvage):** `git revert 508e05f` → `360b45a`. Revert (not reset) preserved the two bookkeeping commits between c5fad2c and the merge (release-notes attachment; `.replit` `PILOT_CAPTURE_USER_IDS` line). Verified `git diff c5fad2c HEAD` shows only those two files. #922's scope (shared api-client mock helpers) is to be redone via reconstruction on a fresh branch.

**Suite baseline note:** With `PILOT_CAPTURE_USER_IDS` set workspace-wide (pilot capture active), `openai.pronunciation.pilot-capture.test.ts` "unlisted userId" fails — it asserts the allowlist Set is empty when the env var is absent, but the var IS present and the Set parses at module init. Proven env-induced: 3/3 pass with the var scrubbed, 2/3 with it present. Until the test scrubs the var in its own setup, the canonical suite reads 676 pass / 4 fail (showroom ×3 + pilot ×1) instead of the documented 675/6; freePhraseReplenish (old Group A ×3) passes as of this run.

### Shared test-mock factories (redo of Task #922 scope, July 31, 2026)

**api-server `@workspace/db` mock factory:** `artifacts/api-server/src/test/dbMock.ts` exports `createDbMockExports(overrides)`. Completeness is typecheck-enforced: the base object must `satisfies { [K in keyof typeof Db]: unknown }` over a TYPE-ONLY import of `@workspace/db` (never executes the module, so no pg Pool). Schema barrel additions/removals now fail typecheck in this one file instead of failing a test file at ESM link time. All previously documented mock gaps closed; phantom `phraseScheduleTable` removed; the cargo-cult drizzle-helper stubs (`eq`, `inArray`, …) dropped — the barrel never exported them. Consumer: `openai.pronunciation.language-hint.test.ts` (only route test that mocks `@workspace/db`).

**gujarati-coach `@workspace/api-client-react` mock base:** `artifacts/gujarati-coach/src/test/api-client-mock.ts` exports `baseApiClientMock()` which derives the export surface from `vi.importActual` at runtime: `get*QueryKey` → stable key fns, `use*` → `idleHook()` (covers both query and mutation shapes), everything else passes through real (ApiError, getChatTurnUrl, setBaseUrl…). New generated hooks are covered automatically — the pattern that broke 16 test files (93 tests) when practice/journey/chat gained `useListCategories` / `useListZoneStamps` / `useGetScenario` cannot recur. All 38 full-replacement factories now spread the base FIRST so per-file overrides win.

**scenario-chat.test.tsx corruption repaired:** `interface PhraseChi psProps` (parse error) existed on main BEFORE the #922 merge — a scramble artifact that survived because `artifacts/gujarati-coach/tsconfig.json` excludes `src/test/**` from typecheck. Fixed to `PhraseChipsProps`.

**freePhraseReplenish FK-safe cleanup:** the test's `resetPhrases()` and `after()` now delete `user_item_memory`/`phrase_reports` rows referencing the test language's phrases before deleting phrases, and clear `user_item_memory`/`xp_ledger`/`user_ability` rows for the test user before deleting the user (Scoring Core startup backfill seeds memory rows from attempts at threshold=1). All dependent deletes are guarded with `to_regclass` so they no-op on a database where those tables aren't provisioned.

**Suite state after this work:** web suite 48 files / 330 tests all pass (was 93 fail). api canonical suite 680 tests, 676 pass / 4 fail — showroom `allTopBand` ×3 (route emits a key the test forbids; production-side, fenced) + pilot-capture ×1 (env-induced while `PILOT_CAPTURE_USER_IDS` is set; test-setup hardening was ticketed separately). Boot smoke tests pass.

### Task #954 station resume, reconstructed from current main (July 31, 2026)

**practice.tsx auto-start:** journey stations (`?group=<id>`, not test-out/polish/phraseIds/explicit `?phrase=`) now resume at the first phrase below the 80 credit edge (`bestScore == null || bestScore < 80` — the same threshold lesson-group completion uses; `mastered` flag deliberately NOT used here). Fallback to index 0 when every phrase is at 80+. Only the start index changes: the phrase set is never filtered and `backHref` stays `/journey`. The category `?skipMastered` branch is untouched.

**Teaser inertness + review-visit pins (fence constraints):** the M1 teaser taste set is INERT to resume — teaser-state responses carry per-phrase `teaser` progress, and the resume branch bails (`isTeaserSet`) so the fixed free set always plays from the top (skipping an attempted phrase would shorten the taste → upsell flow). An all-mastered station is a deliberate review visit pinned by test: starts at phrase 1 with the full set. Implementation locus: **practice loader** (the auto-start effect in practice.tsx computes resume from the loaded phrase payload's `bestScore`), NOT a journey link param — mobile parity (#966) must mirror this.

**New test file:** `practice-start-position.test.tsx` (6 tests) consumes `baseApiClientMock()` with only two per-file overrides (`useListLessonGroupPhrases`, `useListCategoryPhrases`); no framer-motion mock needed (siblings run real framer-motion on the setup.ts jsdom stubs). **Testing trap discovered:** the practice session chrome (header, belly button "Hold to speak") already renders during the transient `intro` state, BEFORE the auto-start effect commits its index jump — the effect's updates can land after `render()` returns. Assertions about start position must flush effects (`await act(async () => {})`) and wait on the expected phrase text, never on session chrome.

**Old #954 branch delta, re-evaluated:** its 13 sibling test files adding `useListCategories`/`useListZoneStamps`/`useGetScenario` stubs are ALL redundant — the runtime-derived base mock covers those hooks in every one of the 38 migrated factories (proven empirically: full web suite green with zero sibling edits). Its scenario-chat syntax repair was already absorbed. Nothing from the old branch was merged; the fix was re-applied by intent on current main.

**Suite state after this work:** web 49 files / 335 tests all pass (330 baseline + 5 new). api canonical suite unchanged: 680 tests, 676 pass / 4 fail (same fenced showroom ×3 + pilot-capture ×1, byte-identical by name). Typecheck clean; both boot smokes pass.

### Showroom allTopBand omission (July 31, 2026)

**Ruling + route fix:** the polish-stamp refactor had added `allTopBand` unconditionally to the lesson-group listing, breaking the showroom counts-and-statuses-only contract (the showroom test's `assertNoPhraseContent` allowlist correctly flagged it). Ruled route-side: in `learning.ts` the field is now spread-conditional on `!showroom` — allowed callers keep it byte-identical, showroom (teaser/exhausted) groups omit the key entirely. It carried no cross-account data (derived from the caller's own stats, same source as `attemptedCount`/`masteredCount`) but was meaningless for showroom callers (computed over the full group set a locked caller can never attempt; journey.tsx renders nothing with it for locked stations) — dead surface, so the tight contract won. Schema-safe: `allTopBand` is optional/additive in the OpenAPI shape. The three showroom tests pass UNMODIFIED.

**Suite state after this fix:** api canonical suite 680 tests, 679 pass / 1 fail — pilot-capture "unlisted userId" (env-induced) is now the ONLY fenced failure. Web untouched at 49 files / 336 tests green. Typecheck clean; boot smokes 2/2; unlock-filtering, lesson-groups-unlock, and scenario suites green within the canonical run (no suite named "zone-stamps" exists on main — closest is `learning.lesson-groups-unlock.test.ts`).

### Tear hand-off overlay + gust exit (Task #905, July 31, 2026)

**Problem:** the boarding-pass tear's final beat (stub opacity fade, last ~100ms of `--tear-duration` 600ms) was cut when navigation at `--tear-nav-delay` (500ms) unmounted home; the body half simply vanished mid-recoil at the swap.

**Mechanism (web only, home.tsx + index.css):** in the SAME beat as `navigate("/journey")` inside the nav timer, `spawnTearHandoffOverlay(body, stub)` clones BOTH mid-tear halves into a `position:fixed` container appended to `document.body` (`[data-tear-overlay]`, `z-60`, `pointer-events:none`, `aria-hidden`; deliberately not React-owned — a portal would unmount with home). Each clone resumes its tear keyframes via inline `animation-delay: calc(-1 * var(--tear-nav-delay))` (inline outranks the class shorthand's implicit 0s), inside a `.animate-gust-away` wrapper that sweeps the piece off over the incoming journey route — stub first, body `--gust-stagger` later, each along its own tear trajectory via per-half inline `--gust-x`/`--gust-r` = ± the `:root` constants. No morph toward the journey header ticket. New `:root` tuning constants: `--gust-drift` 120px, `--gust-drop` 22px, `--gust-rotate` 9deg, `--gust-duration` 420ms, `--gust-stagger` 120ms, `--tear-overlay-cleanup` 900ms (measured from the hand-off; JS fallback constant 900ms).

**Cleanup:** removal fires on the FINAL wrapper's (`[data-gust-final]`, the body half) `animationend` filtered by `animationName === "gust-away"` — the clones' own tear-keyframe ends bubble through the same listener and must not remove the overlay early — plus an idempotent `--tear-overlay-cleanup` timeout fallback (backgrounded tabs/jsdom never fire animation events).

**Placement correctness:** clones are placed by layout-true offsets (`offsetParent` rect + `offsetLeft/Top`, sizes from `offsetWidth/Height`) so the current tear transform never skews the box; the ticket-breathe wrapper class is additionally gated on `!tearing` so no ancestor scale skews measurement at the hand-off. jsdom degrades to 0-rects harmlessly (no `offsetParent` there).

**Invariants preserved:** navigation timing unchanged (`--tear-nav-delay` still 500ms) and never blocked — overlay spawn sits in its own try/catch inside the timer with `navigate()` after it unconditionally; activation-path failure (`getComputedStyle` on documentElement throwing) still navigates immediately and now provably creates zero overlay nodes (the spawn reads `--tear-overlay-cleanup` BEFORE any DOM write); reduced motion = instant native Link nav, no tear, no gust; transforms/opacity only.

**Tests:** `home-boarding-pass.test.tsx` 8 frozen pins untouched + new describe "tear hand-off overlay (task 905)" (4 pins: spawn-at-navigation-only + final-gust-animationend removal with bubbling-tear-end immunity; timeout-fallback removal (real timers — jsdom's lack of animation events IS the fallback path); reduced-motion no-overlay; failure-path no-orphan). **Test hygiene trap:** any test that lets the tear reach navigation now spawns the overlay, whose jsdom removal is time-based (~900ms real timer) and whose body clone duplicates the pass CTA text — a file-level `afterEach` purges `[data-tear-overlay]` or later tests' `getByText` finds multiple matches. `qa/task899-tear-probe.mjs` MODE=after extended: overlay presence over /journey right after the slowed nav, gone after animation end, and gone ≤1.2s post-nav at stock timing (real-browser run itself is Task #960's job).

**Suite state after this work:** web 49 files / 340 tests all pass (336 baseline + 4 new). Typecheck clean; boot smokes 2/2 (standalone run). api canonical run: exit 1 with the pilot-capture "unlisted userId" env-induced fence PLUS one intermittent failure in `feedbackTts.test.ts` ("b64_FROM_PREWARM==" assertion) that passes 6/6 standalone — full-suite interference (prewarm cache/TTS-provider adjacent, see Task #953's scope), not caused by this web-only change.
